# Band-divergence allowlist.
#
# checkBandDivergence diffs the working tree against the branch this band was minted from (mint_parent in
# gradle.properties) and against dev, over every tracked path except gradle.properties, this file and the
# per-band plug (common/src/main/java/world/thearchive/wdl/adapter/impl/). A path that differs from both must
# match a pattern below; a pattern that matches no path differing from dev is stale; core/ main sources may not
# differ from the parent and are never listed here, which along the mint chain keeps them one blob on every band.
# A path equal to the parent's is inherited and a path equal to dev's is not this band's divergence; an inherited
# entry stays live while its file differs from dev and is kept. Each entry records why the file legitimately
# carries band-local code, so neither the gate nor a future reader mistakes its difference for a lost fix or a
# bug. Anchor a reason to a version rather than to "this band", say "deleted" for a file dev has and this band
# does not, and re-read every inherited reason at a mint: the gate checks that a pattern is live, never that
# its reason is true. Patterns match as gitignore(5) matches: the last matching pattern is the one credited and a
# directory pattern covers its subtree, so a pattern nested under a registered directory is never credited and
# reads as stale.
#
# Format: gitignore-style per gitignore(5), one path pattern per line, blank lines and # comments ignored.
# Every pattern carries a # reason directly above it (checkBandDivergence fails on a pattern without one), so
# a shared reason across several files is repeated per line rather than written once over a group.

# Below 1.16 there is no RegistryAccess: the tests read the static RegistryNamespaced tables 1.11.2's Bootstrap.register
# fills once per JVM. 1.11.2's WorldInfo.updateTagCompound and EntityPlayer.writeEntityToNBT call
# FMLCommonHandler.getDataFixer, whose sidedDelegate only FML's own lifecycle sets, so the bootstrap installs a headless
# IFMLSidedHandler by reflection that wraps DataFixesManager.createFixer.
common/src/test/java/world/thearchive/wdl/testsupport/TestRegistries.java

# Below 1.21.2 vanilla has no SerializableChunkData.SectionData record, so the seam defines its own SectionData carrier.
# 1.11.2 has no ChunkStatus or Heightmap type: heightmaps() is the flat int[256] a pre-Flattening save writes under
# HeightMap, there is no status() accessor, lightCorrect maps onto the LightPopulated boolean, biomes() widens the
# byte[256] column array, and the carrier holds ExtendedBlockStorage and NibbleArray rather than LevelChunkSection and
# DataLayer.
common/src/main/java/world/thearchive/wdl/adapter/ChunkSnapshotSource.java

# Below 1.21.2 the captured section is read through ChunkSnapshotSource.SectionData (no
# SerializableChunkData.SectionData). Below the 1.21 update a playable disc is an ItemRecord instance (no
# jukebox_playable component) and the captured jukebox carries IsPlaying, RecordStartTick and TickCount, keys a 1.11.2
# BlockJukebox.TileEntityJukebox never writes (it saves RecordItem alone). 1.11.2 has no item components, UseOnContext,
# BlockPlaceContext or BlockEntityType: the use-block hook takes an EnumHand and a RayTraceResult, the placement gate
# reproduces ItemBlock.onItemUse's cell choice under EntityPlayer.canPlayerEdit and World.mayPlace, the disc is written
# with ItemStack.writeToNBT, a placed shulker's contents are read from the stack's BlockEntityTag via getSubCompound and
# ItemStackHelper.loadAllItems, and its block-entity id is TileEntity.getKey(TileEntityShulkerBox.class). Java 8 has no
# sealed interfaces or pattern switch, so Candidate is a plain interface and confirm is an instanceof chain.
common/src/main/java/world/thearchive/wdl/adapter/InteractionCapture.java

# Below the 1.21 update a captured jukebox's play state is the IsPlaying note-particle flag rather than the
# ticks_since_song_started sidecar, so the RecordItem carry-forward field carries IsPlaying. Below 1.20.5 the beehive
# occupants key is Bees rather than bees, so the field carries that key (1.11.2 has no beehive, so on it the key matches
# nothing). 1.11.2's NBT is classic MCP: the client-default tags are NBTTagInt, NBTTagShort, NBTTagByte and
# NBTTagIntArray and the field-presence check reads getTag(key) instanceof NBTTagCompound.
common/src/main/java/world/thearchive/wdl/adapter/CapturedBlockField.java

# Below 1.21.2 the entity position wire is the absolute-only teleport packet (SPacketEntityTeleport at 1.11.2) with
# byte-encoded rotations the capture decodes. 1.11.2 has no EntityType: the spawn is split across SPacketSpawnObject,
# SPacketSpawnMob, SPacketSpawnPainting and SPacketSpawnExperienceOrb, discrimination runs on the spawn-object type id
# and EntityList, reconstruction goes through EntityList.createEntityByID and createEntityByIDFromName (the minecart
# through EntityMinecart.create, the item frame, painting and leash knot through their positioned constructors, the
# item frame's facing decoded with EnumFacing.getHorizontal, the 32-1.11 name of 1.12.2's byHorizontalIndex), a mob's
# synched data arrives inline via SPacketSpawnMob.getDataManagerEntries, the entered dimension is DimensionType.getById
# on the SPacketRespawn and SPacketJoinGame ids, the lightning SPacketSpawnGlobalEntity is left unread, and the data and
# math types are EntityDataManager.DataEntry, MathHelper and Vec3d.
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketCapture.java

# The Java 8 floor has no records, so this MC-free packet-position carrier, a record from 1.17.1 up, is a final class
# with explicit fields and accessors.
common/src/main/java/world/thearchive/wdl/adapter/EntityPos.java

# Reads ChunkSnapshotSource.SectionData (no SerializableChunkData.SectionData below 1.21.2). Below 1.20.5 there is no
# SimpleRegionStorage and below 1.15 no IOWorker, so the writer is opened through WorldPaths.openRegionStorage, the
# mod's own region store; below 1.17 entities live inside the region chunk, so the writer is built with the region
# opener alone; below 1.20.3 there is no Crafter menu, so the crafter legs are dropped. 1.11.2 has no lectern (1.14), so
# the lectern bind arm is dropped and nothing feeds the lectern stash; no EntityType, unified Registry or ChestType: a
# block entity's id is TileEntity.getKey(be.getClass()), the client chunk is ChunkProviderClient.getLoadedChunk (no
# status), the double chest is found by the pre-Flattening neighbor probe with the lower-coordinate half taking slots 0
# to 26, and the trades are the MerchantRecipeList of GuiMerchant.getMerchant().getRecipes; no Chunk.isUnsaved: the
# dirty poll is needsSaving(false), whose entity arm compares the world time against a last save time nothing writes on
# a client, so the re-arm stamps setLastSaveTime beside setModified(false), bounding the residual to one re-encode per
# entity-bearing chunk per 600 ticks. Its save API is classic MCP: Minecraft.getSaveLoader is an ISaveFormat whose
# getSaveLoader(name, false) is the ISaveHandler the level.dat write targets, the saves directory is mcDataDir/saves
# (the 32-1.11 name of 1.12.2's gameDir), CompressedStreamTools.readCompressed takes an InputStream, map data is
# World.getMapStorage keyed by the map_ prefix, the leash is rebound with EntityLiving.setLeashedToEntity
# (setLeashHolder at 1.12.2), Minecraft is no Executor (finishes are scheduled with addScheduledTask), the stats are
# EntityPlayerSP.getStatFileWriter and getServerBrand is on the player, the camera guard reads camera.world, and
# ServerData.serverMOTD is a null-guarded String. Below 1.12 there are no advancements, so the progress snapshot is the
# statistics alone with no DataVersion (1.12.2 also reads NetHandlerPlayClient.getAdvancementManager), and an empty
# NBTTagList is tested with hasNoTags, the 32-1.11 name of 1.12.2's isEmpty. encodeQuiesced is not volatile: with no
# Fabric tree, every path that takes or reads the hold is the client thread.
common/src/main/java/world/thearchive/wdl/adapter/LiveCaptureSession.java

# Absent from dev: 1.11.2's Chunk has no isUnsaved reader, so the dirty poll uses the should-save query
# needsSaving(false), whose entity arm compares the world's total time against a last save time nothing writes on a
# client. This pins that LiveCaptureSession.rearmUnsaved's last-save-time stamp is what quiets that arm, that the arm
# returns once at 600 ticks, and that the chunk is built through Chunk(World, int, int) with setHasEntities and the
# clock pushed through WorldInfo.setWorldTotalTime.
common/src/test/java/world/thearchive/wdl/adapter/RecaptureRearmTest.java

# Below 1.12 there is no vanilla toast host (net.minecraft.client.gui.toasts arrives at 1.12 with Minecraft.getToastGui,
# getToasts from 1.13 through 1.21.1, getToastManager from 1.21.2 through 26.1 and Gui.toastManager at 26.2), so both
# toast streams go to the mod's own WdlToastOverlay.show, the job-done body and the refusal alike, where 1.12.2 hands
# them to SystemToast and WdlMultilineToast. 1.11.2 has no Brigadier: the /wdl command is a CommandBase (execute and
# getTabCompletions over an ICommandSender) the loader registers, not a LiteralArgumentBuilder tree; the pause-menu row
# is GuiButton subclasses dispatched through the loader's action-performed hook, its settings button drawing its own
# hover label from the three-argument drawButton(Minecraft, int, int) (1.12.2 adds a partial-tick float) since GuiButton
# takes no tooltip; chat and toast text is ITextComponent built from TextComponentString and TextComponentTranslation
# with Style.setColor(TextFormatting), and the nearest-named-color quantizer recomputes each color's RGB from
# getColorIndex, TextFormatting exposing no RGB accessor. isConnectionClosed reads the play connection's
# NetworkManager.isChannelOpen the way PlayerControllerMP does, beside isRemoteWorld; the saves directory is
# mcDataDir/saves (gameDir at 1.12.2), 1.11.2 having no LevelStorageSource.
common/src/main/java/world/thearchive/wdl/platform/AbstractPlatformBridge.java

# The chunk fixtures build ChunkSnapshotSource.SectionData (no SerializableChunkData.SectionData below 1.21.2). 1.11.2
# has no ChunkStatus or Heightmap: each section is an ExtendedBlockStorage(int, boolean), the heightmap is one flat
# int[256] under HeightMap, the biomes are the byte-per-column ids Biome.getIdForBiome yields, and lightCorrect maps
# onto the LightPopulated boolean.
common/src/test/java/world/thearchive/wdl/testsupport/SyntheticChunks.java

# A headless overworld World for the entity fixtures (the EntityLiving constructor binds a world). At 1.11.2 it goes
# through the five-argument World constructor (a null ISaveHandler, a WorldInfo built from WorldSettings,
# DimensionType.OVERWORLD.createDimension and a Profiler) and answers the two abstract members, createChunkProvider and
# isChunkLoaded, with none; the map-data, game-event, tick-rate and potion-brewing overrides dev's fixture carries name
# members 1.11.2's World lacks.
common/src/test/java/world/thearchive/wdl/testsupport/HeadlessLevel.java

# Below 1.16 there is no RegistryAccess to smoke-test, so this proves the vanilla bootstrap ran by resolving
# minecraft:stone both ways through the static Block.REGISTRY (getObject and getNameForObject), pinning the class
# mapping on cobblestone (a bare Block at 1.11.2, where stone is the BlockStone host of the pre-Flattening metadata
# variants), and round-tripping an NBTTagCompound and a BlockPos.
common/src/test/java/world/thearchive/wdl/testsupport/RegistriesSmokeTest.java

# At 1.11.2 Item.REGISTRY is a RegistryNamespaced with no default, so an item resolves through getObject (null for an
# unknown id) rather than Registry.get; there is no ResourceLocation.parse, so an id is built with the
# ResourceLocation(String) constructor. Below 1.20.5 there are no item components: a custom name is set via
# ItemStack.setStackDisplayName, a stored stack is written with ItemStack.writeToNBT (a {id, Count, Damage, tag}
# compound with Count as a byte), a written book's title, author and pages live on the item's tag in the shape
# ItemWrittenBook.resolveContents leaves, and ItemStackHelper.saveAllItems takes no registries argument.
common/src/test/java/world/thearchive/wdl/testsupport/ItemFixtures.java

# Below 1.14 there is no ChunkSerializer, so the round trip proves self-consistency by decoding each section's Blocks,
# Data and Add arrays through 1.11.2's BlockStateContainer.setDataFromNBT, and pins the pre-Flattening chunk keys:
# HeightMap is the flat int[256], lightCorrect maps onto the LightPopulated boolean, and no isLightOn key (1.14) is
# written.
common/src/test/java/world/thearchive/wdl/adapter/ChunkRoundTripTest.java

# Deleted: the plug's chunk-codec test dev carries exists from 1.18.2 up and is absent at 1.17.1 and below, 1.11.2
# included.
common/src/test/java/world/thearchive/wdl/adapter/impl/ChunkCodecImplTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the round trip opens the mod's own
# WdlRegionStorage through WorldPaths.openRegionStorage; below 1.17 there is no separate entities region, so it seeds a
# host region chunk, folds the entity carrier into its Level.Entities through RegionChunkWriter.foldEntitiesIntoRegion
# and reads the entity back from that chunk. The sink's carrier is a bare Entities list with no DataVersion or Position,
# and the NBT and dimension names are classic MCP (NBTTagCompound, NBTTagList, DimensionType.OVERWORLD).
common/src/test/java/world/thearchive/wdl/adapter/EntityRoundTripTest.java

# Below 1.21.11 GameRules has no codec, so the rules under test are read back with GameRules.readFromNBT from the
# GameRules compound of the level data WorldInfo.cloneNBTCompound writes, where dev parses them with GameRules.codec
# over NbtOps from the data/minecraft/game_rules.dat file its test reads. The pass-through case drives doFireTick, since
# doLimitedCrafting, the rule 1.12.2's case drives, is a 1.12 addition that 1.11.2's GameRules rejects as unknown.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatWorldOutputTest.java

# The headless entity double overrides 1.11.2's three abstract Entity members, entityInit, readEntityFromNBT and
# writeEntityToNBT, where dev's overrides hurtServer, defineSynchedData and the addAdditionalSaveData pair, and the seat
# helper sets the ride link by reflection on 1.11.2's ridingEntity and riddenByEntities fields, named vehicle and
# passengers from 1.13 up.
common/src/test/java/world/thearchive/wdl/adapter/CaptureAnchorTest.java

# The headless entity doubles override 1.11.2's abstract entityInit rather than hurt or defineSynchedData; below 1.20.5
# there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer and the read-back open the mod's own
# WdlRegionStorage through WorldPaths.openRegionStorage; 1.11.2 has no MerchantOffers or ItemCost, so a trade is a
# MerchantRecipe of plain ItemStacks in a MerchantRecipeList, and no EntityType, so the spawn packet is an
# SPacketSpawnObject built from a positioned source entity (setUniqueId, setLocationAndAngles); the NBT is classic MCP
# (NBTTagCompound, NBTTagList).
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossTallyTest.java

# Added, absent from dev: drives LiveCaptureSession.bindOpenedContainer against a 1.11.2 TileEntityFurnace and its
# ContainerFurnace menu, pinning that a TileEntityLockable that is no TileEntityLockableLoot (furnace, brewing stand,
# beacon) still binds as a CONTAINER at its own position rather than being refused by the size-match guard; the headless
# EntityPlayerSP takes the four-argument constructor, the RecipeBook fifth argument being a 1.12 addition.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLockableContainerBindTest.java

# 1.11.2 has no EntityType, so the vehicle is a real EntityPig built over the HeadlessLevel (the EntityLiving
# constructor binds a world) and the rider a headless EntityPlayer built from a GameProfile, seated with startRiding
# and read back through isBeingRidden and getRecursivePassengers, where dev's test builds RiddenVehicleEntity and
# RiddenMountMob doubles over EntityTypes.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkRootVehicleTest.java

# 1.11.2 has no EntityType, so a mob type is its EntityList registry name as a ResourceLocation and the slot is the
# classic EntityEquipmentSlot; the drowned's trident, the wandering trader and the 1.14 raid-captain banner carve-out
# are absent at 1.11.2 and dropped from the pinned table, and the geared mobs are real EntityZombie and EntityPig
# instances built over the HeadlessLevel.
common/src/test/java/world/thearchive/wdl/adapter/impl/NaturalEquipmentTest.java

# Added, absent from dev: the vanilla-key byte gate for the pre-Flattening chunk shape, asserting the keys
# AnvilChunkLoader writes at 1.11.2 (DataVersion 922, not the 1343 of 1.12.2; Blocks, Data, TerrainPopulated and
# LightPopulated) and no post-Flattening Status, Heightmaps, Palette or BlockStates key, bootstrapped by TestRegistries.
common/src/test/java/world/thearchive/wdl/adapter/impl/ChunkVanillaKeyShapeTest.java

# Absent from dev, present at 1.20.4 and below: below 1.20.5 mount armor sits in the horse inventory the client never
# receives (an equipment slot from 1.20.5 up), so this pins the synced tier and color written as the plain item, at
# 1.11.2 on a real EntityHorse (getHorseArmorType) and EntityLlama (DATA_COLOR_ID as the carpet's Damage).
common/src/test/java/world/thearchive/wdl/adapter/impl/MountArmorCaptureTest.java

# Absent from dev, present at 1.21.4 and below: below 1.21.5 a saddle sits in the horse inventory the client never
# receives (an equipment slot from 1.21.5 up), so this pins the synced saddle flag written as the plain item, at 1.11.2
# through AbstractHorse.isHorseSaddled and setHorseSaddled on a real EntityHorse, the horseChest read by reflection.
common/src/test/java/world/thearchive/wdl/adapter/impl/SaddleCaptureTest.java

# At 1.11.2 the plug's named-mob persistence test builds its entity tags as classic NBTTagCompound and checks keys with
# hasKey rather than CompoundTag and contains.
common/src/test/java/world/thearchive/wdl/adapter/impl/NamedMobPersistenceTest.java

# Below 1.16 there is no namespaced dimensions tree to scan, so the plug's paths test pins only the fixed set 1.11.2's
# three-constant DimensionType registers, saved at the world root or a DIM plus id folder and keyed by
# DimensionType.getName, the display names Overworld, Nether and The End (1.12 lowercases them to overworld and so on).
common/src/test/java/world/thearchive/wdl/adapter/impl/WorldPathsImplTest.java

# Below the 1.21 update the HUD render callback carries a float partial tick rather than a DeltaTracker, so the
# overlay's render entry point takes a float. 1.11.2's client types are classic MCP: the text goes through FontRenderer,
# the peek key through KeyBinding.isPressed and isKeyDown, the flattened strings through
# ITextComponent.getUnformattedText, and the math through MathHelper.
common/src/main/java/world/thearchive/wdl/client/WdlHudOverlay.java

# Absent from dev, whose settings screen uses the vanilla CycleButton added at 1.17: a value-cycling button present at
# 1.16.5 and below. At 1.11.2 it extends the pre-onPress GuiButton, so a hit advances the value in an overridden
# mousePressed rather than through an onPress callback.
common/src/main/java/world/thearchive/wdl/client/WdlCycleButton.java

# Below 1.20 there is no FaviconTexture, so the draw seam's blitFavicon takes the world icon's registered
# ResourceLocation rather than dev's FaviconTexture handle; the draw surface itself (GuiGraphics from 1.20 through
# 1.21.11, GuiGraphicsExtractor from 26.1) is reimplemented in the plug. 1.11.2's text types are classic MCP, so the
# seam names FontRenderer and ITextComponent rather than Font and Component.
common/src/main/java/world/thearchive/wdl/adapter/RenderSurface.java

# At 1.11.2 the screen is built on the pre-1.13 GuiScreen event model, which has no Screen.children, onPress or
# AbstractSelectionList: its buttons are GuiButton subclasses dispatched through actionPerformed (custom-drawn ones on a
# WidgetButton base), the name field is a GuiTextField driven from keyTyped and updateScreen, and the download list is a
# GuiListExtended of IGuiListEntry rows whose getScrollBarX is overridden to follow the row right edge, GuiSlot's
# default being the fixed width / 2 + 124 that lands inside a wider row. The world icon is decoded through ImageIO into
# the BufferedImage DynamicTexture takes (no NativeImage), Rect2i is a local int-rect holder, and folder and browser
# opens go through java.awt.Desktop (no Util.getPlatform). Below 1.12 the row draw and button draw carry no partial
# tick (IGuiListEntry.drawEntry and GuiButton.drawButton gain the float at 1.12, and setSelected is the 1.12
# updatePosition), and there is no NarratorChatListener or ChatType, so a row selection is not narrated. The screen
# also draws WdlToastOverlay from its own render, 1.11.2 having no vanilla toast host.
common/src/main/java/world/thearchive/wdl/client/WdlDownloadsScreen.java

# At 1.11.2 the screen is built on the pre-1.13 GuiScreen event model: it extends GuiScreen, its tabs and footer are
# GuiButton actions dispatched through actionPerformed, the row controls (WdlCycleButton, slider, seed field) share only
# the Gui base and are narrowed at each site, the slider rides the GuiButton mouseDragged and getHoverState idiom rather
# than dev's AbstractSliderButton, and the list is a GuiListExtended of IGuiListEntry rows with keys forwarded from
# keyTyped to the focused GuiTextField and getScrollBarX overridden to follow the row right edge, GuiSlot's default
# being the fixed width / 2 + 124 that cuts through a wider row. Below 1.12 the row and control draws carry no partial
# tick, setSelected is the 1.12 updatePosition, and GuiTextField.mouseClicked returns void, so a field hit is read back
# from isFocused; captureAdvancements joins the hidden rows, advancements being a 1.12 addition; and the screen draws
# WdlToastOverlay from its own render, 1.11.2 having no vanilla toast host.
common/src/main/java/world/thearchive/wdl/client/WdlSettingsScreen.java

# Added, absent from dev and 1.12.2: below 1.12 there is no vanilla toast host (net.minecraft.client.gui.toasts arrives
# at 1.12), so this tray draws both notification streams itself through FontRenderer and the RenderSurface seam: three
# top-right slots, the refusal dedup a vanilla toast id gives from 1.12 up (the SystemToast.Type enum at 1.12.2,
# SystemToastIds from 1.13 through 1.20.2 and the SystemToastId class at 1.20.4 and dev), drawn by the HUD pass or
# screen.
common/src/main/java/world/thearchive/wdl/client/WdlToastOverlay.java

# JourneyMap 5.5.2, the build for 1.11.2, bundles the 1.x journeymap.client.api (1.11.2-1.4, the pinned
# journeymap_api_coordinate) and no journeymap.api.v2, so the entry point implements journeymap.client.api.IClientPlugin
# under the ClientPlugin annotation with an onEvent push callback, where dev's is the 2.0 API's under JourneyMapPlugin.
common/src/main/java/world/thearchive/wdl/compat/journeymap/WdlJourneyMapPlugin.java

# The driver binds the 1.x journeymap.client.api: IClientAPI.subscribe with mapping events on the plugin's onClientEvent
# push, where dev drives the 2.0 ClientEventRegistry.MAPPING_EVENT; the dimension is the classic DimensionType off
# mc.world.provider.getDimensionType, its int id passed to PolygonOverlay, and the logger is log4j's LogManager.
common/src/main/java/world/thearchive/wdl/compat/journeymap/JourneyMapOverlayDriver.java

# The polygon builder binds the 1.x journeymap.client.api.model MapPolygon and ShapeProperties rather than dev's
# journeymap.api.v2.client.model over the classic net.minecraft.util.math.BlockPos, and, the 1.x API having no
# holed-polygon type, feeds JourneyMap one convex polygon per coverage rectangle rather than the tracer's holed hulls.
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygons.java

# The shared coverage-polygon tracer is public with public HoledRing accessors, where dev's is package-private, and its
# empty list is a Guava ImmutableList.of for the Java 8 floor (no List.of below Java 9).
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygonTracer.java

# At 1.11.2 the dimension is the classic net.minecraft.world.DimensionType enum, so the rebind is driven with its
# OVERWORLD and NETHER constants rather than a ResourceKey<Level> built from a ResourceLocation, and the rebound id
# asserted is the display name Nether that 1.11.2's DimensionType.getName returns (the_nether from 1.12).
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionDimensionRebindTest.java

# At 1.11.2 a player tag's Dimension is the classic DimensionType's int id (getId) rather than a ResourceKey<Level> id
# string, so the dimension cases assert the id written for DimensionType.NETHER and an id outside the three; below
# 1.21.5 the armor and offhand live in Inventory, so no equipment compound is built or asserted; and the ender-chest
# list is read back with ItemStackHelper.loadAllItems, the tags being classic MCP NBTTagCompound and NBTTagList whose
# emptiness is hasNoTags, the 32-1.11 name of 1.12.2's isEmpty.
common/src/test/java/world/thearchive/wdl/adapter/PlayerTagTest.java

# At 1.11.2 there is no BlockEntityType registry: a fixture's producer is looked up as the ITileEntityProvider block's
# createTileEntity, a saved tag is re-read through the static TileEntity.create(World, NBTTagCompound), the fidelity
# shape is TileEntity.writeToNBT, the block table is the static Block.REGISTRY, and item lists go through
# ItemStackHelper.loadAllItems and saveAllItems (no registries argument below 1.20.5).
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelity.java

# At 1.11.2 a jukebox block entity saves RecordItem alone (the IsPlaying, RecordStartTick and TickCount sidecar is
# written from 1.19.4's tree, not yet at 1.19), so the jukebox merge fixtures set only that key; below 1.20.5 the
# occupants key CapturedBlockField carries is Bees, and since 1.11.2 has no beehive the Bees fixture rides an
# ender-chest tag through the unchecked malformedChunkTagWith rather than the fidelity-checked chunkTagWith. Below 1.18
# the merge reads the chunk block entities under Level.TileEntities, an empty NBTTagList is hasNoTags (the 32-1.11 name
# of 1.12.2's isEmpty), and Minecraft 1.11.2 bundles fastutil 7.0.12, which has no LongSet.of, so the empty set is
# LongSets.EMPTY_SET.
common/src/test/java/world/thearchive/wdl/adapter/ChunkMergeTest.java

# Below the 1.21 update a captured jukebox is marked playing via IsPlaying rather than the ticks_since_song_started
# sidecar, so the round trip asserts that flag; below 1.20.5 there are no item components, so the merged RecordItem is
# read back through 1.11.2's ItemStack(NBTTagCompound) constructor, and the chunk's block entities sit under
# Level.TileEntities (1.18 renamed the key). The beehive occupant case dev carries is dropped, 1.11.2 having no beehive.
common/src/test/java/world/thearchive/wdl/adapter/InteractionMergeRoundTripTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer drains against the mod's own
# WdlRegionStorage, opened per dimension through a StorageOpener; below 1.17 entities live inside the region chunk under
# Level.Entities, so the ENTITIES write task folds into the host chunk through RegionChunkWriter.foldEntitiesIntoRegion
# rather than draining a second entities/ storage, the resume scan reads the same chunk, and a fold with no host chunk
# is a counted loss. The tags are classic MCP NBTTagCompound. At 1.11.2 the pause self-release backstop is 60 seconds
# against 10 on 1.13.2 and dev, sized over the whole client level teardown the hold brackets (other mods' unload
# listeners and a synchronous resource reload), and pauseEncoding logs at INFO how long the hold blocked its caller.
common/src/main/java/world/thearchive/wdl/adapter/AsyncSaveWriter.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the merge-write reads and writes through
# the mod's own WdlRegionStorage load and store. Below 1.17 entities live inside the region chunk, so this carries
# foldEntitiesIntoRegion, the entity fold that unions a captured entity carrier into the host chunk's Level.Entities
# through EntityMerge and writes the chunk back, and the terrain merge-write preserves the on-disk Level.Entities the
# fresh terrain tag lacks. The NBT is classic MCP: the Level compound and its lists are read with getCompoundTag and
# getTag checked against NBTTagCompound and NBTTagList, an empty list being hasNoTags (isEmpty at 1.12.2).
common/src/main/java/world/thearchive/wdl/adapter/RegionChunkWriter.java

# Below 1.20.5 there is no RegionStorageInfo and below 1.15 no IOWorker, so this seam opens the mod's own
# WdlRegionStorage (openRegionStorage). Below 1.17 there is no separate entities/ region, so the entitiesDirectory and
# openEntitiesStorage members the 1.17.1 seam carries are dropped and this maps and opens only region/. At 1.11.2 the
# dimension key is the classic net.minecraft.world.DimensionType enum.
common/src/main/java/world/thearchive/wdl/adapter/WorldPaths.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the drain is proven against a test-local
# subclass of the mod's own WdlRegionStorage rather than one built from RegionStorageInfo and DataFixers; item lists are
# read with ItemStackHelper.loadAllItems (no registries argument below 1.20.5), and the tags are classic MCP
# NBTTagCompound with an empty NBTTagList tested by hasNoTags (isEmpty at 1.12.2). A case dev lacks captures the INFO
# line pauseEncoding logs and pins the blocked-for milliseconds it reports.
common/src/test/java/world/thearchive/wdl/adapter/AsyncSaveWriterTest.java

# Deleted: the whole test exercises chiseled-bookshelf occupancy flush, and ChiseledBookShelfBlock does not exist at
# 1.19 or below, so the test exists from 1.19.4 up and on no older band.
common/src/test/java/world/thearchive/wdl/adapter/BookshelfOccupancyFlushTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the prior write and the read-back open the
# mod's own WdlRegionStorage through WorldPaths; the dimension is the classic DimensionType.OVERWORLD, the block state
# comes from getDefaultState, and the tags are NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/CarryForwardWiringTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer and the entity read-back open
# the mod's own WdlRegionStorage through WorldPaths. Below 1.13 a filled map's id is the item's Damage short rather than
# a map tag entry, so the map-id read is getShort("Damage") on the first stored item; the tags are classic MCP
# NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/EntityVehicleRelocationTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer and the entity read-back open
# the mod's own WdlRegionStorage through WorldPaths, and the prior player tag is written through the ISaveHandler that
# 1.11.2's AnvilSaveConverter(File, DataFixer).getSaveLoader returns, with the difficulty as EnumDifficulty. The scan
# hands the bank a region chunk whose entities sit under Level.Entities below 1.17, so the fixture standing in for a
# prior download's record builds that shape.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionResumedMountReleaseTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the sweep is proven against the mod's own
# WdlRegionStorage, built directly from its region directory; item lists are read with ItemStackHelper.loadAllItems (no
# registries argument below 1.20.5), and the tags are classic MCP NBTTagCompound.
common/src/test/java/world/thearchive/wdl/adapter/OrphanedContainerSweepTest.java

# Below 1.20.5 there is no SimpleRegionStorage or RegionStorageInfo and below 1.15 no IOWorker, so the integration test
# drives the mod's own WdlRegionStorage opened through WorldPaths; the dimension is the classic DimensionType.OVERWORLD
# and the tags are NBTTagCompound and NBTTagList, an empty list tested by hasNoTags (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/RegionIntegrationTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so this opens the mod's own WdlRegionStorage
# directly from a directory rather than the vanilla wrapper, over classic MCP NBTTagCompound tags.
common/src/test/java/world/thearchive/wdl/testsupport/RegionRoundTrip.java

# 1.11.2 has no VoxelShape, unified Registry or ChestType: the outline shape is the state's getBoundingBox unioned with
# its addCollisionBoxToList parts, offset to world (one box alone need not span the model, a brewing stand's being its
# base plate); the block-entity id is TileEntity.getKey(getClass()); and the double-chest partner is the capture side's
# pairing, ContainerCapture.doubleChestPartner, so the outline reflects what an open would capture. The client level is
# WorldClient with a ChunkProviderClient (getLoadedChunk), the container base is TileEntityLockable, the seal test is
# IBlockState.isFullBlock, and a neighbor cell is stepped with EnumFacing.getFrontOffsetX, getFrontOffsetY and
# getFrontOffsetZ, the 32-1.11 names of 1.12.2's getXOffset, getYOffset and getZOffset.
common/src/main/java/world/thearchive/wdl/adapter/OutlineTracker.java

# Minecraft 1.11.2 ships fastutil 7.0.12, which has no static Long2ObjectMaps.fastIterator (the 8.2.1 that 1.13.2
# bundles has), so the draw set walks its sections through long2ObjectEntrySet().iterator().
common/src/main/java/world/thearchive/wdl/adapter/OutlineDrawSet.java

# 1.11.2's NBT is classic MCP (NBTTagCompound, NBTTagList, getTag, removeTag, setTag, and hasNoTags for an empty list,
# the 32-1.11 name of 1.12.2's isEmpty). Below 1.21.5 there is no separate equipment compound, armor and the offhand
# living in the Inventory list, so applyStripKnobs drops only Inventory and SelectedItemSlot; LastDeathLocation (1.19)
# and current_explosion_impact_pos (1.20.5) do not exist yet, so stripDeathLocation is a no-op kept for signature
# parity. The dimension is the classic three-constant DimensionType, written as its int id, and since getById throws for
# an unregistered id dimensionOf compares against the three constants directly; the RootVehicle Attach UUID is written
# through NBTTagCompound.setUniqueId rather than dev's store with UUIDUtil.CODEC (putUUID from 1.13 through 1.21.4).
common/src/main/java/world/thearchive/wdl/adapter/PlayerTag.java

# At 1.11.2 the level-data write targets the classic ISaveHandler (saveWorldInfoWithPlayer with a player tag,
# saveWorldInfo without) rather than a LevelStorage, the built holder is a net.minecraft.world.storage.WorldInfo rather
# than a LevelData, and the prior-player read is an NBTTagCompound.
common/src/main/java/world/thearchive/wdl/adapter/LevelDataWriter.java

# At 1.11.2 the field types are the classic net.minecraft.world.DimensionType, GameType and EnumDifficulty (Difficulty
# from 1.13 up) with an NBTTagCompound tag and a net.minecraft.util.math.BlockPos, rather than the Mojmap
# dimension.DimensionType, level.GameType, Difficulty, CompoundTag and core.BlockPos.
common/src/main/java/world/thearchive/wdl/adapter/CapturedPlayer.java

# At 1.11.2 the local player is serialized through EntityPlayer.writeToNBT(NBTTagCompound), the call
# SaveHandler.writePlayerData itself makes, rather than a saveWithoutId form, and the player type is
# net.minecraft.entity.player.EntityPlayer rather than Player.
common/src/main/java/world/thearchive/wdl/adapter/PlayerSink.java

# Below 1.12 there are no advancements, so the serializer builds the statistics JSON alone with a plain Gson and no
# AdvancementProgress adapter, where 1.12.2 also renders the advancements file. 1.11.2 has no StatType or
# Registry.STAT_TYPE, only the flat StatBase hierarchy, so the stats file is a flat object of statId to count
# enumerated over StatList.ALL_STATS through StatisticsManager.readStat rather than grouped by stat type, the
# achievements riding it as achievement.* keys, and it stamps no DataVersion (a 1.13 addition).
common/src/main/java/world/thearchive/wdl/adapter/PlayerProgressSerializer.java

# Below 1.12 there are no advancements, so the progress snapshot carries the UUID and the statistics blob alone, the
# advancements blob and its accessor 1.12.2 adds being absent, and the live structure it names is StatisticsManager.
common/src/main/java/world/thearchive/wdl/adapter/CapturedProgress.java

# The entity UUID is written with 1.11.2's NBTTagCompound.setUniqueId (the UUIDMost and UUIDLeast long pair below 1.16)
# and the Pos list is built by hand from NBTTagDouble constructors, with the item holder written by
# ItemStackHelper.saveAllItems (no registries argument below 1.20.5).
common/src/test/java/world/thearchive/wdl/testsupport/EntityFixtures.java

# At 1.11.2 the prior level.dat is written through the ISaveHandler that AnvilSaveConverter(File,
# DataFixer).getSaveLoader returns rather than a LevelStorage from a LevelStorageSource, the captured player carries an
# EnumDifficulty, and the tags are classic MCP NBTTagCompound with the UUID written by setUniqueId.
common/src/test/java/world/thearchive/wdl/adapter/PriorPlayerReadRoundTripTest.java

# At 1.11.2 the level.dat round trip writes through the ISaveHandler that AnvilSaveConverter(File,
# DataFixer).getSaveLoader returns rather than a LevelStorage, reads the built data through WorldInfo.cloneNBTCompound
# rather than a codec over RegistryOps, and reads the file back with CompressedStreamTools.readCompressed over an
# InputStream.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatRoundTripTest.java

# 1.11.2's NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase, getTag and setTag rather than get and put), and the
# Java 8 floor cannot bind a pattern variable in a type test, so the carried Xp tag is read with a null check rather
# than dev's instanceof-Tag pattern. 1.11.2's NBTTagList.get answers an out-of-range index with a sentinel NBTTagEnd
# rather than throwing, so the tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/EntityContainerMerge.java

# Below 1.20.5 an item is {id, Count, tag} ({id, Count, Damage, tag} at 1.11.2), so the walk recurses into a shulker's
# nested items via tag.BlockEntityTag.Items and a tag.Items list (the pre-component bundle shape, which 1.11.2 never
# produces, having no bundle). Below 1.13 a filled map's id is the item-level Damage short rather than a tag entry, so
# the walk carries an item-compound view (forEachItem) beside the tag view (walkList and walkItem) the coordinate scrub
# keeps, a filled map carrying no tag at all; the NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase, getTag), and
# since 1.11.2's NBTTagList.get answers an out-of-range index with NBTTagEnd the walks iterate the list.
common/src/main/java/world/thearchive/wdl/adapter/ItemTreeWalk.java

# Below 1.13 a filled map's id is the item-level Damage short (the stack's metadata) rather than an inner map tag entry
# or a map_id component, and Damage is the field every item writes, so the remap gates on the item id being
# minecraft:filled_map before rewriting Damage, over ItemTreeWalk's item-compound view rather than the tag view; the
# tags are classic MCP NBTTagCompound and NBTTagList.
common/src/main/java/world/thearchive/wdl/adapter/MapIdRemap.java

# Below 1.13 a filled map's id is the item-level Damage short (the stack's metadata) rather than an inner map tag entry
# or a map_id component, and Damage is the field every item writes, so the collector gates on the item id being
# minecraft:filled_map before reading Damage, over ItemTreeWalk's item-compound view; the tags are classic MCP
# NBTTagCompound and NBTTagList.
common/src/main/java/world/thearchive/wdl/adapter/MapIdCollector.java

# 1.11.2's NBT is classic MCP, so the on-sight map archive names NBTTagCompound and NBTBase rather than CompoundTag and
# Tag, and the content hash reads the map dimension via getInteger, MapData.writeToNBT storing it as an int; the remap
# table and the manifest are unchanged.
common/src/main/java/world/thearchive/wdl/adapter/MapArchive.java

# At 1.11.2 the map-serialize SPI names the classic net.minecraft.world.storage.MapData and NBTBase rather than
# MapItemSavedData and Tag, and the serialize is MapData.writeToNBT rather than a codec.
common/src/main/java/world/thearchive/wdl/adapter/MapSink.java

# Below 1.20.5 there is no BundleContents component, so the bundle-contents leg of dev's change signal (a third per-slot
# snapshot compared by reference) is absent: the signal compares each slot's stack identity and count alone and reads no
# tag. At 1.11.2 the slot stack is read through the classic Slot.getStack and ItemStack is net.minecraft.item.ItemStack.
common/src/main/java/world/thearchive/wdl/adapter/MenuChangeTracker.java

# 1.11.2 has no MerchantOffers: the trade list is the classic MerchantRecipeList, whose getRecipiesAsTags writes the
# {Recipes:[...]} compound the capture copies rather than encoding through a codec, the villager save carries no
# trade-experience Xp (a 1.14 addition), and the tags are classic MCP with the Recipes list iterated, 1.11.2's
# NBTTagList.get answering an out-of-range index with NBTTagEnd.
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferCapture.java

# Below 1.20.5 an item's data lives in its tag compound rather than a components map, so the scrub addresses the raw
# LodestonePos and LodestoneDimension keys and a beehive item's BlockEntityTag.Bees[].EntityData.FlowerPos and
# BlockEntityTag.FlowerPos; 1.11.2 itself has no lodestone (1.16) or beehive (1.15), so on it those keys match nothing.
# The NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase, getTag, getKeySet and removeTag rather than get, keySet
# (getAllKeys through 1.21.4) and remove), and the lists are iterated, 1.11.2's NBTTagList.get answering an out-of-range
# index with NBTTagEnd.
common/src/main/java/world/thearchive/wdl/adapter/ItemLocationScrub.java

# At 1.11.2 the item lists are read back with ItemStackHelper.loadAllItems (no registries argument below 1.20.5), the
# planks stack is built from Blocks.PLANKS through Item.getItemFromBlock (no per-wood plank items before the
# Flattening), and the tags are classic MCP NBTTagCompound read through getTagList, an empty list tested by hasNoTags
# (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/ContainerMergeRoundTripTest.java

# At 1.11.2 the item lists are read back with ItemStackHelper.loadAllItems (no registries argument below 1.20.5) and the
# tags are classic MCP NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/ContainerRecaptureSynergyTest.java

# At 1.11.2 the item lists are read back with ItemStackHelper.loadAllItems (no registries argument below 1.20.5) and the
# tags are classic MCP NBTTagCompound and NBTTagList, an empty list tested by hasNoTags (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/ContainerStashMergeTest.java

# At 1.11.2 the item lists are read back with ItemStackHelper.loadAllItems (no registries argument below 1.20.5) and the
# tags are classic MCP NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/EntityContainerStashMergeTest.java

# 1.11.2 has no MerchantOffers or ItemCost: a trade is a MerchantRecipe of plain ItemStacks and the offers holder is
# built through MerchantRecipeList.getRecipiesAsTags rather than a codec, the UUID reads are NBTTagCompound.getUniqueId
# and hasUniqueId, and the tags are classic MCP NBTTagCompound and NBTTagList, an empty list tested by hasNoTags
# (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/EntityMergeTest.java

# Below 1.20.5 there are no item components, so a placed shulker's contents live under the item's tag.BlockEntityTag and
# captureRecordItem takes no registries argument. At 1.11.2 the shulker is the per-color Blocks.PURPLE_SHULKER_BOX, the
# discs are Items.RECORD_CAT and RECORD_11, the jukebox state is set with withProperty(BlockJukebox.HAS_RECORD, true) on
# an IBlockState, and the beehive and chiseled-bookshelf cases dev carries are absent, 1.11.2 having neither block.
common/src/test/java/world/thearchive/wdl/adapter/InteractionStashMergeTest.java

# Below 1.20.5 an item's data lives in its tag rather than a components map: the lodestone target is the raw
# LodestonePos, LodestoneDimension and LodestoneTracked keys, a beehive item's occupants and flower position sit under
# tag.BlockEntityTag.Bees[].EntityData.FlowerPos and tag.BlockEntityTag.FlowerPos, and shulker contents under
# tag.BlockEntityTag.Items. 1.11.2 has no lodestone (1.16) or beehive (1.15), so those fixtures are hand-built on a
# plain compass (NBTUtil.createPosTag, the dimension as DimensionType.OVERWORLD.getName) and a chest item; the shulker
# is the per-color PURPLE_SHULKER_BOX, item lists go through ItemStackHelper.loadAllItems, and the tags are classic MCP,
# an empty Bees list tested by hasNoTags (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/ItemLocationScrubTest.java

# Below 1.20.5 an item is {id, Count, tag} ({id, Count, Damage, tag} at 1.11.2), so the walk is driven with hand-built
# NBT keyed by tag.BlockEntityTag.Items (a shulker) and tag.Items (the pre-component bundle shape) rather than the
# component keys, over classic MCP NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/ItemTreeWalkTest.java

# Below 1.20.5 there is no WrittenBookContent component: a written book's title, author, generation, resolved flag and
# pages live on the item's tag, so the fixture builds that shape, its pages as NBTTagString JSON from
# ITextComponent.Serializer.componentToJson, over classic MCP NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/LecternRecaptureSynergyTest.java

# Below 1.20.5 there are no WrittenBookContent or WritableBookContent components: a written book's title, author,
# generation, resolved flag and pages (JSON component strings from ITextComponent.Serializer.componentToJson) live on
# the item's tag, an unsigned book's pages are plain strings, and the read-back is 1.11.2's ItemStack(NBTTagCompound)
# constructor rather than ItemStack.CODEC; the lectern itself is a 1.14 block, so the round trip proves the sink's shape
# without one.
common/src/test/java/world/thearchive/wdl/adapter/LecternSinkRoundTripTest.java

# Below 1.20.5 there is no WrittenBookContent component: a written book's title, author, generation, resolved flag and
# pages live on the item's tag, so the fixture builds that shape, its pages as NBTTagString JSON from
# ITextComponent.Serializer.componentToJson, over classic MCP NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/LecternStashMergeTest.java

# At 1.11.2 the map is a net.minecraft.world.storage.MapData built from its map_ name, read back with
# MapData.readFromNBT (no registries argument below 1.20.5) and compared through its public d and scale fields; the
# dimensionless cases dev carries are absent, a 1.11.2 map dimension being a primitive int rather than a nullable
# ResourceKey.
common/src/test/java/world/thearchive/wdl/adapter/MapSinkRoundTripTest.java

# Below 1.20.5 there is no BundleContents component, so dev's bundle-contents mutation case is absent; at 1.11.2 the
# slots sit over an InventoryBasic (setInventorySlotContents, getSizeInventory) named by a TextComponentString rather
# than a SimpleContainer, and the registries are bootstrapped through TestRegistries.bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/MenuChangeTrackerTest.java

# 1.11.2 has no MerchantOffers or ItemCost, so a trade is a MerchantRecipe of plain ItemStacks in a MerchantRecipeList;
# below 1.20.5 the sell item's fields are read off its tag, the lodestone target being the raw LodestonePos,
# LodestoneDimension and LodestoneTracked keys (hand-built through NBTUtil.createPosTag, 1.11.2 having no lodestone) and
# a filled map's id the item-level Damage short (the stack's metadata) rather than a tag or component entry; an empty
# holder is tested by NBTTagCompound.hasNoTags (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferCaptureTest.java

# 1.11.2 has no MerchantOffers or ItemCost: a trade is a MerchantRecipe of plain ItemStacks and the offers holder is
# built through MerchantRecipeList.getRecipiesAsTags rather than a codec, over classic MCP NBTTagCompound and
# NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/MerchantStashMergeTest.java

# Below 1.20.5 the occupants key CapturedBlockField carries is Bees (capitalized), and the capture writes IsPlaying,
# RecordStartTick and TickCount beside a present RecordItem (keys a 1.11.2 jukebox never writes, its block entity
# saving RecordItem alone), so the scan fixtures carry those too; 1.11.2 has no beehive, so the Bees fixture rides an
# ender-chest tag through the unchecked malformedChunkTagWith rather than the fidelity-checked chunkTagWith. The disc id
# is minecraft:record_cat and the tags are classic MCP.
common/src/test/java/world/thearchive/wdl/adapter/RecoveredScanTest.java

# Below 1.20.5 a renamed block entity's name is the raw CustomName key rather than a custom_name component (at 1.11.2 a
# chest, furnace, banner or enchantment table block entity writes it), and a beehive's occupants are the pre-component
# EntityData, TicksInHive and MinOccupationTicks entries under Bees rather than an Occupant codec encode, a shape 1.11.2
# itself never produces, having no beehive. Below 1.18 the chunk tag wraps its block entities under Level.TileEntities
# rather than the root block_entities, and the text and NBT types are ITextComponent and NBTTagCompound.
common/src/test/java/world/thearchive/wdl/testsupport/BlockEntityFixtures.java

# Below 1.20.5 an item's count is the raw Count byte rather than a components-map int, so a hand-built entry without it
# is silently dropped by 1.11.2's ItemStackHelper.saveAllItems rather than round-tripping with one named key missing;
# the tags are classic MCP NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelityTest.java

# Below 1.20.5 there are no item components, so a shulker's nested contents live under tag.BlockEntityTag.Items; below
# 1.13 a filled map's id is the item-level Damage short (the stack's metadata), so the fixture builds the map with the
# ItemStack(Items.FILLED_MAP, 1, id) constructor, and the shulker is the per-color Blocks.PURPLE_SHULKER_BOX, 1.11.2
# having no colorless shulker box.
common/src/test/java/world/thearchive/wdl/testsupport/MapHolderFixtures.java

# Below 1.20.3 there is no Crafter block, block entity or menu, so the crafter open-bind leg, the crafter stash and the
# crafter menu-only ContainerData are dropped. Below 1.20 the entity's level is a public field, so the spectator-reach
# guard reads player.world. 1.11.2 has no BlockHitResult, ChestType property or ChestMenu.getRowCount: the crosshair is
# Minecraft.objectMouseOver, a RayTraceResult told apart by typeOfHit; a large chest is told by the first slot's
# inventory size of 54 on a ContainerChest over IInventory-backed slots (inventorySlots, Slot.getStack); and a double
# chest is recognized by the neighbor probe over two TileEntityChest of the same getChestType kind (doubleChestPartner).
common/src/main/java/world/thearchive/wdl/adapter/ContainerCapture.java

# Absent from dev, present at 1.20.4 and below: from 1.20.5 up vanilla numbers a saved chest stack chest-relative, while
# below 1.20.5 a mount's saddle slot, armor slot and chest share one absolutely addressed inventory (a
# ContainerHorseChest at 1.11.2), so a captured chest stack must keep the slot offset by the two worn slots, and this
# reloads through vanilla's own entity reader (readEntityFromNBT) rather than counting what came back. At 1.11.2 the
# mounts are real EntityDonkey and EntityLlama over the HeadlessLevel (the chest put on through replaceItemInInventory,
# the llama driven at every strength getInventoryColumns clamps to), the menu is ContainerHorseInventory, and the
# headless EntityPlayerSP takes the four-argument constructor (a RecipeBook fifth argument arrives at 1.12).
common/src/test/java/world/thearchive/wdl/adapter/MountChestSlotsTest.java

# At 1.11.2 the NBT round trip is classic CompressedStreamTools rather than NbtIo (writeCompressed for the gzip map
# file, write and read over data streams for idcounts), the map file's data envelope carries no DataVersion (a 1.13
# addition), and data/idcounts.dat is a root {map: short} written uncompressed with no data wrapper, the bytes
# MapStorage's getUniqueDataId and loadIdCounts write and read, so the write, read and serialize are branched together
# and the resume read never faults on a 1.11.2 file.
common/src/main/java/world/thearchive/wdl/adapter/MapDataWriter.java

# At 1.11.2 the compressed NBT round trip is classic CompressedStreamTools.writeCompressed and readCompressed over
# streams (no NbtAccounter argument below 1.20.3) rather than NbtIo, over NBTTagCompound.
common/src/main/java/world/thearchive/wdl/adapter/StashHolder.java

# 1.11.2 has no Screen or ConfirmScreen, so the resume confirms are the classic GuiYesNo built with plain-string title,
# message and labels through ITextComponent.getFormattedText and getUnformattedText, the amber styling riding the
# section codes getFormattedText emits from a TextFormatting color; and the vanilla gui.proceed key is a 1.13 addition,
# so the Continue verb resolves through the wdl-owned wdl.screen.downloads.proceed_action key.
common/src/main/java/world/thearchive/wdl/client/ResumeConfirm.java

# Below 1.20.3 there is no Crafter menu or block entity, so the crafter capture tests are dropped; the tags are classic
# MCP (NBTTagCompound, NBTTagByte, NBTTagShort, getKeySet and getTag rather than keySet (getAllKeys through 1.21.4) and
# get).
common/src/test/java/world/thearchive/wdl/adapter/ContainerCaptureTest.java

# Below 1.20.3 there is no Crafter block entity, so the flush-plan test builds its state-bearing block-entity fixtures
# on the brewing stand (BrewTime, Fuel) rather than the crafter; the disc id is minecraft:record_cat and the tags are
# classic MCP NBTTagCompound, an empty Items list tested by hasNoTags (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/ChunkFlushPlanTest.java

# At 1.11.2 the level.dat is written through the ISaveHandler that AnvilSaveConverter(File, DataFixer).getSaveLoader
# returns rather than a LevelStorage, read back with CompressedStreamTools.readCompressed over an InputStream (no
# NbtAccounter below 1.20.3), and the game type is compared by GameType.getID with the difficulty as EnumDifficulty,
# whose id is getDifficultyId (the 32-1.11 name of 1.12.2's getId).
common/src/test/java/world/thearchive/wdl/adapter/LevelDatPlayerRoundTripTest.java

# At 1.11.2 the round trip is classic CompressedStreamTools rather than NbtIo, the map file's data envelope carries no
# DataVersion (a 1.13 addition), and the idcounts assertions are the uncompressed root {map: short} the branched
# writeIdCounts and readIdCounts round-trip.
common/src/test/java/world/thearchive/wdl/adapter/MapDataWriterRoundTripTest.java

# Added, absent from dev: the idcounts byte-check gate. Below 1.13 data/idcounts.dat is a root {map: short} written
# uncompressed through CompressedStreamTools with no data wrapper and no DataVersion, so this asserts that raw shape
# against a regression to the 1.13.2 gzip envelope of {data: {map: int}} with a DataVersion.
common/src/test/java/world/thearchive/wdl/adapter/MapIdCountsShapeTest.java

# Added, absent from dev: the container-map loss gate. Below 1.13 a filled map's id is the item-level Damage short
# behind the minecraft:filled_map id gate, so this drives hand-built classic-MCP item NBT to prove the chest-nested and
# shulker-nested maps collect and remap while a damaged non-map item's Damage field is left untouched.
common/src/test/java/world/thearchive/wdl/adapter/ContainerMapCollectionTest.java

# At 1.11.2 the write-task test reads the written map back through classic CompressedStreamTools rather than NbtIo, over
# NBTTagCompound.
common/src/test/java/world/thearchive/wdl/adapter/MapWriteTaskTest.java

# Deleted: advancements are a 1.12 addition (net.minecraft.advancements has no class at 1.11.2), so the listener that
# harvests the client's advancement progress for the advancements file, present from 1.12.2 up, has nothing to bind; the
# achievements of 1.11.2 ride the statistics file as achievement.* keys.
common/src/main/java/world/thearchive/wdl/adapter/AdvancementSnapshot.java

# Below 1.20.2 there is no configuration phase, so the tee never observes a start-configuration packet and the
# backend-transfer signal (signalTransfer, consumeTransferSignal) stays wired but is never raised. At 1.11.2 the packet
# types are classic MCP: the connection is NetworkManager, the relative-move packet is SPacketEntity, whose int deltas
# the loader tee narrows to the contract's short, and the ridden-vehicle open request is CPacketEntityAction.
common/src/main/java/world/thearchive/wdl/adapter/ConnectionTee.java

# At 1.11.2 the shared mount-menu read names the classic net.minecraft.entity.Entity and
# net.minecraft.inventory.Container rather than the Mojmap world.entity.Entity and
# world.inventory.AbstractContainerMenu.
common/src/main/java/world/thearchive/wdl/adapter/MountMenuReader.java

# Below 1.12 there are no advancements, so the advancement case dev and 1.12.2 drive (AdvancementProgress.update) is
# absent and the guard covers the stats JSON alone. 1.11.2 has no StatType: the stats are granted through
# StatisticsManager.increaseStat on StatList.getBlockStats and PLAY_ONE_MINUTE, and the assertion is the flat statId to
# count object with no DataVersion stamp rather than the by-type grouping.
common/src/test/java/world/thearchive/wdl/adapter/PlayerProgressSerializerTest.java

# Added, absent from dev: the legacy JSON mod manifest FML reads below the Forge 25 (1.13.2) TOML floor, its version
# token expanded by the island's processResources, where 1.13.2's island ships META-INF/mods.toml and dev no Forge
# island at all.
forge/src/main/resources/mcmod.info

# Absent from dev: the island's access transformer in the classic SRG field_NNNNN_ scheme (TSRGv2 f_NNNNN_ from 1.17
# up), widening the SPacketEntity entity id and the ContainerHorseInventory horse alone. Against 1.13.2's it drops the
# NetworkManager channel widening, 1.11.2's NetworkManager exposing a public channel() getter, and legacy FML does not
# discover the file by its META-INF path, so the shipped jar's manifest names it in an FMLAT attribute.
forge/src/main/resources/META-INF/accesstransformer.cfg

# Absent from dev: the Forge tee reads the connection's pipeline through 1.11.2's public NetworkManager.channel()
# (1.13.2's tee reads the widened channel field), reads the SPacketEntity entity id reflectively by its MCP name with
# the searge field_149074_a as the fallback (the access transformer being a runtime-only FMLAT with no compile-time
# widening), and narrows the packet's int move deltas (public getX, getY and getZ) to the shared ConnectionTee
# contract's short, exact since the deltas are read as a sign-extended short off the wire.
forge/src/main/java/world/thearchive/wdl/forge/ForgeConnectionTee.java

# Absent from dev: the Forge mount-menu read names the non-public ContainerHorseInventory.horse the access transformer
# widens (field_111242_f at 1.11.2), the one member MountMenuReader needs that the menu exposes no getter for.
forge/src/main/java/world/thearchive/wdl/forge/ForgeMountMenuReader.java

# Added, absent from dev: the mods-list config-screen hook below the Forge 25 ExtensionPoint floor. Legacy FML
# instantiates the IModGuiFactory the @Mod guiFactory attribute names and enables the mods-list config button only for
# a factory whose hasConfigGui answers true, where 1.13.2 registers ExtensionPoint.CONFIGGUIFACTORY from WdlForge
# itself; Forge 13.20's IModGuiFactory still declares the deprecated mainConfigGuiClass and getHandlerFor that Forge
# 14.23 drops, answered with null as FML's own DefaultGuiFactory does.
forge/src/main/java/world/thearchive/wdl/forge/ForgeConfigGuiFactory.java

# Absent from dev: the island's resource pack.mcmeta, whose pack_format is 3 at 1.11.2, the one value
# ResourcePackRepository accepts, where 1.13.2 pins 4.
forge/src/main/resources/pack.mcmeta

# Absent from dev: the island's reobf fixtures. At 1.11.2 Unimined reobfuscates the shipped jar MCP to searge natively
# (defaultRemapJar), so a wrong compiled Minecraft reference cannot compile and the fixtures instead carry reflective
# SRG-shaped string literals (a real 1.11.2 searge id, and a malformed one) that checkShipJar and checkReobfNegative
# resolve against the pinned mcp srg joined.srg oracle (mcp_config's joined.tsrg at 1.12.2 and 1.13.2), where 1.13.2's
# fixtures reference Mojmap members for its hand-rolled reobfJar to rewrite.
forge/src/reobftest/**

# Absent from dev: the island's own gradle.properties, carrying the JVM args alone at 1.11.2 as at 1.13.2, the Unimined
# island having no Architectury Loom and so no loom.platform switch (1.14.4's island sets loom.platform=forge).
forge/gradle.properties

# Absent from dev: the island's daemon toolchain pin. At 1.11.2 it is Java 17 as at 1.13.2, the floor mod-publish-plugin
# declares, where 1.14.4's island pins 21 for Architectury Loom and the root daemon runs 21; the compile toolchain stays
# java_version 8.
forge/gradle/gradle-daemon-jvm.properties

# Absent from dev: the island wrapper pins Gradle 8.14.5, the newest 8.x line, as 1.13.2's does; at 1.11.2 the pin holds
# because Unimined 1.4.1 is untested on Gradle 9 and 8.8 is the oldest 8.x with the
# ConfigurableFileCollection.convention overload mod-publish-plugin 2.2.0 calls.
forge/gradle/wrapper/gradle-wrapper.properties

# Absent from dev: the island build, provisioning Forge 13.20 through Unimined's minecraftForge loader, reobfuscating
# natively (defaultRemapJar) against the mcp srg zip's joined.srg oracle (mcp_config's joined.tsrg at 1.12.2 and
# 1.13.2), and bundling the fastutil 7.0.12 classes the trimmed cut Minecraft 1.11.2 ships lacks, gated by
# checkFastutilBundle.
forge/build.gradle.kts

# Absent from dev: the island's settings. At 1.11.2 the island resolves the Unimined plugin from WagYourTail's mavens
# and Forge from MinecraftForge's, excluding log4j from the plugin portal, where 1.13.2's plain-java island declares no
# plugin repositories.
forge/settings.gradle.kts

# Deleted: the whole Fabric tree (the loader entry points, the platform bridge, the loader metadata and the gametest
# suite) dev carries. 1.11.2 is Forge-only below the Mojmap floor, as 1.13.2 is, and its root build includes no fabric
# subproject.
fabric/

# 1.11.2 has no EntityType or unified Registry: an entity's vanilla-ness is read from its EntityList registry name
# (ResourceLocation.getResourceDomain, the 32-1.11 name of 1.12.2's getNamespace), the villager profession is the
# classic int getProfession, the lockable-container recognizer is ILockableContainer, and the ender-chest obstruction
# test is IBlockState.isNormalCube, the test BlockEnderChest itself applies at 1.11.2 (the per-face
# doesSideBlockChestOpening is a Forge 14.23 addition); the world, hit and player types are World, RayTraceResult and
# EntityPlayer.
common/src/main/java/world/thearchive/wdl/adapter/OpenClickTracker.java

# At 1.11.2 DimensionType is the classic three-constant net.minecraft.world enum with an instance getName returning a
# plain String, so forType compares the type against NETHER and THE_END directly and forId compares an id against each
# constant's getName, where dev classifies a registry-keyed type and resolves a ResourceKey.
common/src/main/java/world/thearchive/wdl/adapter/VanillaDimensions.java

# At 1.19 and below there is no chiseled bookshelf (ChiseledBookShelfBlock is present from 1.19.4's tree), so the
# per-chunk bookshelf-occupancy producer is dropped and the read-merge composer passes an empty occupancy map
# (ChunkMerge.occupancyMap), the per-slot union staying as unused code. At 1.11.2 BlockPos and ChunkPos are the classic
# net.minecraft.util.math types, a packed position unpacks through BlockPos.fromLong, and the tags are NBTTagCompound.
common/src/main/java/world/thearchive/wdl/adapter/ChunkFlushPlan.java

# Below 1.17 the merge folds into the in-chunk Level.Entities rather than a separate entities region, its Entities
# envelope in and out being band-agnostic. 1.11.2's NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase, getTag,
# appendTag), the captured-entity UUID is the UUIDMost and UUIDLeast long pair read through hasUniqueId and getUniqueId
# rather than a four-int array codec, the Xp default carry is an NBTTagInt, and the tag-list walks iterate the list,
# 1.11.2's NBTTagList.get answering an out-of-range index with NBTTagEnd.
common/src/main/java/world/thearchive/wdl/adapter/EntityMerge.java

# Dev's Wdl.initialize constructs the XaeroPlus integration; 1.11.2's does not (XaeroPlus publishes no 1.11.2 build).
# It wires the loader's level-teardown edge: onLevelTeardown runs the controller's onDisconnect (with the writer hold
# and flush) when the bridge's isConnectionClosed is true at fire time and nothing on a dimension change, where 1.13.2
# runs controller.stop on both routes. 1.11.2 has no SharedConstants class, so the game version is
# RealmsSharedConstants.VERSION_STRING; the saves directory is mcDataDir/saves (no LevelStorageSource, and gameDir is
# the 39-1.12 name of the field); Minecraft is no Executor, so the completion poke goes through addScheduledTask; and
# the client API is classic MCP (Minecraft.getMinecraft, displayGuiScreen, currentScreen, getRenderViewEntity,
# getPositionEyes, getCurrentServerData().serverName).
common/src/main/java/world/thearchive/wdl/Wdl.java

# At 1.11.2 the resume and confirm flow names the classic Minecraft API: Minecraft.getMinecraft, displayGuiScreen for
# the confirm screens (setScreen from 1.13 up), and the saves directory resolved as mcDataDir/saves (gameDir at 1.12.2),
# there being no LevelStorageSource.
common/src/main/java/world/thearchive/wdl/ResumeFlow.java

# Deleted with the rest of the XaeroPlus binding, absent from 1.18.2 down (XaeroPlus publishes nothing for 1.11.2, its
# game-version floor being 1.12.2): the plugin binding that reads the coverage index into XaeroPlus.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/XaeroPlusBinding.java

# Deleted with the rest of the XaeroPlus binding: the launch-time integration hook dev's Wdl.initialize calls.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/XaeroPlusIntegration.java

# Deleted with the rest of the XaeroPlus binding: the package's NullMarked marker.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/package-info.java

# Deleted: ChiseledBookShelfBlock does not exist at 1.19 or below, so the chiseled-bookshelf slot helper is dropped; the
# MC-free core bookshelf helpers (CapturedContainers, OutlineClassifier.classifyBookshelf) stay byte-identical.
common/src/main/java/world/thearchive/wdl/adapter/BookshelfSlots.java

# The view-layer literal inventory is band-specific: at 1.11.2 the GUI writes user-facing strings through the new
# TextComponentString(...) constructor rather than Component.literal, so the needle and the acknowledged-literals map
# (including the empty no-phase HUD placeholder in WdlHudOverlay) reflect the 1.11.2 set. The loader-root loop is
# skip-if-absent for a loader root rather than dev's hard assertTrue, since 1.11.2 ships neither a fabric nor a neoforge
# tree; common's own root stays required and scanned.
common/src/test/java/world/thearchive/wdl/UiLiteralEnrollmentTest.java

# Deleted: the chiseled-bookshelf slot-mask helper it tests (BookshelfSlots) is dropped because ChiseledBookShelfBlock
# does not exist at 1.19 or below.
common/src/test/java/world/thearchive/wdl/adapter/BookshelfSlotsTest.java

# 1.11.2 has no EntityType: the spawn packet is an SPacketSpawnObject built from a positioned source entity with an
# object type id, the move packets are the SPacketEntity.S16PacketEntityLook and S17PacketEntityLookMove forms, the
# headless doubles override the abstract entityInit, and a case dev lacks pins that a reconstructed EntityItemFrame
# keeps the facing the packet's data carries (EnumFacing.getHorizontalIndex), built against the HeadlessLevel; the tags
# are classic MCP NBTTagCompound.
common/src/test/java/world/thearchive/wdl/adapter/EntityPacketCaptureTest.java

# Four of the nine band-neutral curated game-rule specs have no rule at 1.11.2 (doVinesSpread, doWardenSpawning,
# doTraderSpawning and doPatrolSpawning are later additions), so the runtime filter surfaces five and the layout skips
# their order slots alongside the fire-spread row, whose rule fire_spread_radius_around_player exists only from 1.21.11.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatCuratedGameRulesTest.java

# At 1.11.2 the dimension is the classic net.minecraft.world.DimensionType enum, so the cases drive forType and forId
# with its OVERWORLD, NETHER and THE_END constants and their plain names rather than registry-keyed types.
common/src/test/java/world/thearchive/wdl/adapter/VanillaDimensionsTest.java

# Absent from dev: the Forge 13.20 (legacy FML) entry point. FML carries one event bus, so it registers itself on
# MinecraftForge.EVENT_BUS with @SubscribeEvent methods from a no-arg constructor discovered through @Mod (useMetadata
# reads the display fields from mcmod.info, dependencies carries the forge_version_min floor 13.20.0.2262 by hand,
# acceptedMinecraftVersions pins [1.11.2], guiFactory names ForgeConfigGuiFactory); the keybind registers through
# ClientRegistry.registerKeyBinding in the constructor, the HUD draws from a RenderGameOverlayEvent.Post handler on
# ElementType.ALL and draws WdlToastOverlay there while no blocking screen is open (1.11.2 has no vanilla toast host),
# the connection tee is installed off the client-tick edge, and PlayerInteractEvent.RightClickBlock exposes
# getEntityPlayer, getWorld, getHitVec, getFace, getPos and getHand rather than getEntity and getLevel.
forge/src/main/java/world/thearchive/wdl/forge/WdlForge.java

# Absent from dev: the Forge 13.20 (legacy FML) bridge. FML's one bus has no functional addListener, so the bridge
# registers itself once and polls every keybind and tick-end, connect and disconnect callback from a single
# TickEvent.ClientTickEvent END listener; keys register through ClientRegistry.registerKeyBinding at once, there being
# no client-setup event; the pause-menu row is built on GuiScreenEvent.InitGuiEvent.Post (getButtonList) and dispatched
# through GuiScreenEvent.ActionPerformedEvent.Pre, a GuiButton having no onPress; the join and disconnect edge is the
# tick-edge probe of Minecraft.getConnection, Forge 13.20 having no ClientPlayerNetworkEvent; the /wdl command registers
# through ClientCommandHandler as a classic ICommand; the config directory is mcDataDir/config (gameDir at 1.12.2); and
# onLevelTeardown fires from WorldEvent.Unload, gated on the client world, which Minecraft.loadWorld posts at its head,
# ahead of the FMLClientHandler.handleClientWorldClosing registry revert and of the player it nulls later in that
# method.
forge/src/main/java/world/thearchive/wdl/forge/ForgePlatformBridge.java

# The screen-open warm gate is WorldType.VOID or needsWorldgenReconstruction, where dev's is needsWorldgenReconstruction
# alone: below 1.19 the void generator reconstructs the vanilla worldgen registries too, its biome needing a registry
# name the client's synced biome lacks, so the gate matches the level-data build's own reconstruction branch.
common/src/main/java/world/thearchive/wdl/WorldgenWarmup.java

# Below 1.19 the void generator reconstructs the vanilla worldgen registries too, so the screen-open warm submits for
# VOID; this test asserts VOID warms, where dev's asserts the void default uses the synced registries and does not warm.
common/src/test/java/world/thearchive/wdl/WorldgenWarmupTest.java

# Below 1.18 a chunk's block entities are nested under Level.TileEntities rather than the root block_entities, so the
# carry-forward read navigates the Level compound first. 1.11.2's NBT is classic MCP (NBTTagCompound, NBTTagList,
# NBTBase, NBTTagInt, NBTTagString, getTag rather than get), a BlockPos packs through toLong rather than asLong, and the
# tag-list walks iterate the list, 1.11.2's NBTTagList.get answering an out-of-range index with NBTTagEnd.
common/src/main/java/world/thearchive/wdl/adapter/ChunkMerge.java

# Below 1.18 the recovered-container scan reads block entities from Level.TileEntities rather than the root
# block_entities, and below 1.17 the recovered-entity scan reads the in-chunk Level.Entities rather than a separate
# entities region chunk. 1.11.2's NBT is classic MCP, so hasCoordinates checks getTag(key) instanceof NBTTagInt,
# coordinates are read via getInteger, the slot-occupancy check reads hasKey("Slot") (NBTTagCompound has no contains),
# and the tag-list walks iterate the list, 1.11.2's NBTTagList.get answering an out-of-range index with NBTTagEnd.
common/src/main/java/world/thearchive/wdl/adapter/RecoveredScan.java

# com.mojang.logging.LogUtils is a 1.18 addition and slf4j enters the library set at 1.17, so at 1.11.2 the per-class
# logger is acquired through log4j2's LogManager directly.
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketAccumulator.java

# com.mojang.logging.LogUtils is a 1.18 addition and slf4j enters the library set at 1.17, so at 1.11.2 the per-class
# logger is acquired through log4j2's LogManager directly; below 1.12 there are no advancements, so the writer lands
# the stats/<uuid>.json file alone where 1.12.2 also writes advancements/<uuid>.json.
common/src/main/java/world/thearchive/wdl/adapter/PlayerProgressWriter.java

# LogUtils is a 1.18 addition and slf4j enters the library set at 1.17, so the logger is log4j2's LogManager. At 1.11.2
# the line pass uses the classic fixed-function render forms: the shared Tessellator buffer begun on
# DefaultVertexFormats.POSITION_COLOR, GlStateManager blend, texture and line-width state, an ICamera frustum, and the
# camera interpolation read from the classic lastTickPosX, lastTickPosY, lastTickPosZ and posX, posY, posZ fields with
# getRenderViewEntity and getRenderPartialTicks; the draw set is walked through long2ObjectEntrySet().iterator(), the
# fastutil 7.0.12 Minecraft 1.11.2 ships having no Long2ObjectMaps.fastIterator.
common/src/main/java/world/thearchive/wdl/client/WdlOutlineRenderer.java

# com.mojang.logging.LogUtils is a 1.18 addition and slf4j enters the library set at 1.17, so at 1.11.2 the per-class
# logger is acquired through log4j2's LogManager directly.
common/src/main/java/world/thearchive/wdl/compat/bobby/BobbyChunkFilter.java

# com.mojang.logging.LogUtils is a 1.18 addition and slf4j enters the library set at 1.17, so at 1.11.2 the per-class
# logger is acquired through log4j2's LogManager directly.
common/src/main/java/world/thearchive/wdl/compat/flashback/FlashbackReplayProbe.java

# JsonParser.parseString is a gson 2.8.6 static, absent from the gson 2.2.4 Minecraft 1.11.2 bundles, so the release
# index parses through the instance form (new JsonParser().parse).
common/src/main/java/world/thearchive/wdl/update/ReleaseIndex.java

# The Java 8 floor has no java.net.http HTTP client (Java 11) nor its Flow-based body subscribers, so the update
# transport is written on a java.net.HttpURLConnection with a manual bounded read rather than HttpClient and
# HttpResponse.BodySubscriber.
common/src/main/java/world/thearchive/wdl/update/HttpTransport.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps the static registries through TestRegistries.bootstrap
# rather than building the RegistryAccess.Frozen dev's test reads, over classic MCP NBTTagCompound tags.
common/src/test/java/world/thearchive/wdl/adapter/ChunkReencodeFidelityTest.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps the static registries through TestRegistries.bootstrap
# rather than building the RegistryAccess.Frozen dev's test reads; the dimension is the classic DimensionType.OVERWORLD
# and the tags are NBTTagCompound.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionMapTallyTest.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps the static registries through TestRegistries.bootstrap
# rather than building the RegistryAccess.Frozen dev's test reads; 1.11.2 has no MerchantOffers, so the offers holder is
# built from a MerchantRecipeList of MerchantRecipe through getRecipiesAsTags, the dimension is the classic
# DimensionType.OVERWORLD, and the tags are NBTTagCompound and NBTTagList.
common/src/test/java/world/thearchive/wdl/adapter/RiddenMountContentsFoldTest.java

# Below 1.18 the merge test reads chunk block entities from Level.TileEntities rather than the root block_entities;
# below 1.14 there is no barrel, so the typed-container fixture is a shulker box; the tags are classic MCP
# NBTTagCompound, an empty Items list tested by hasNoTags (isEmpty at 1.12.2).
common/src/test/java/world/thearchive/wdl/adapter/ContainerTypeGateMergeTest.java

# Minecraft 1.11.2 ships fastutil 7.0.12, which has no LongSet.of factory, so the test builds sets via LongOpenHashSet;
# the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava ImmutableSet.of. It also
# pins that a second onDisconnect after the first one finished finishes nothing further (1.13.2's case drives stop), the
# order the Forge 13.20 loader's tick-edge disconnect arrives in behind the flush the wiring runs at the level-teardown
# edge.
common/src/test/java/world/thearchive/wdl/core/CaptureControllerTest.java

# Absent from dev: pins the Forge 13.20 loader's level-teardown edge through the wiring that translates it: a teardown
# with the connection still open holds nothing and ends no download, one with it already closed holds the writer ahead
# of the registry revert and finishes the download, and the loader's own disconnect callback afterwards finishes nothing
# further (1.13.2's edge finishes without holding the writer). The test and the edge it pins are absent from 1.14.4 up,
# as PlatformBridge.onLevelTeardown is.
common/src/test/java/world/thearchive/wdl/WdlLevelTeardownTest.java

# Minecraft 1.11.2 ships fastutil 7.0.12, which has no LongSet.of factory, so the test builds sets via LongSets and
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/CapturedContainersTest.java

# Minecraft 1.11.2 ships fastutil 7.0.12, which has no LongSet.of factory, so the test builds sets via LongSets and
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/OutlineClassifierTest.java

# Minecraft 1.11.2 ships fastutil 7.0.12, which has no LongSet.of factory, so the test builds sets via LongSets and
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/RecoveredCoverageTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected lists are Guava
# ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/ChatCopyTest.java

# Below 1.17 the Java floor is 8, which has no Set.of collection factory (Java 9), so the moved-keys set is a Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/ConfigSchemaTest.java

# Below 1.17 the Java floor is 8, where Optional has no isEmpty (Java 11), so the crafter-drop assertions negate
# isPresent instead; the packed-position comments name 1.11.2's BlockPos.toLong rather than asLong.
common/src/test/java/world/thearchive/wdl/core/ContainerAssociationTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected drain order is a
# Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/ReadyLatchTest.java

# Below 1.17 the Java floor is 8, which has no List.of or Set.of collection factories (Java 9), so the derived-prefix
# list and the empty undeclared-key set use Guava ImmutableList.of and ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/SettingsLangTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected tab-id list is a
# Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/SettingsLayoutTest.java

# Below 1.17 the Java floor is 8, which has no List.of or Map.of collection factories (Java 9), so the expected lists
# use Guava ImmutableList.of and the seven-entry refusal map uses ImmutableMap.builder (past ImmutableMap.of's five-pair
# limit).
common/src/test/java/world/thearchive/wdl/core/ToastCopyTest.java

# Below 1.17 the Java floor is 8: no Files.readString or writeString (Java 11), no records (Java 16), no List.of (Java
# 9), so the config reads and writes go through readAllBytes and write with StandardCharsets.UTF_8, DefaultOnBoolean is
# a final class with explicit accessors, and the toggle list is a Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/WdlConfigTest.java

# Below 1.17 the Java floor is 8, which has no no-argument Optional.orElseThrow (Java 10) nor List.of (Java 9), so the
# entry lookup uses Optional.get and the empty SaveChunks lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/browse/DownloadCatalogTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected managed-folder list
# is a Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/browse/DownloadFoldersTest.java

# Below 1.17 the Java floor is 8, where there is no Path.of factory (Java 11), so the save path is built with Paths.get.
common/src/test/java/world/thearchive/wdl/core/browse/SinglePlayerTaintTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected surviving-paths
# lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/export/RestoreOperationTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected moved-back,
# relocated and deferred lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/export/RestoreSweepTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the DownloadCounts, SaveChunks
# and render-input lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/report/DownloadReportFormatterTest.java

# Below 1.17 the Java floor is 8: no List.of (Java 9) nor Files.readString or writeString (Java 11), so the SaveChunks
# lists are Guava ImmutableList.of and the corrupt-timestamp round trip reads and writes via readAllBytes and write with
# StandardCharsets.UTF_8.
common/src/test/java/world/thearchive/wdl/core/report/DownloadReportLogTest.java

# Below 1.17 the Java floor is 8, which has no no-argument Optional.orElseThrow (Java 10) nor List.of (Java 9), so
# SemVer.parse is unwrapped with Optional.get and the release and game-version lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/update/LatestReleaseTest.java

# Absent from dev: below Forge 40 the world-render hook is RenderWorldLastEvent (no render stage, no frustum), not the
# RenderLevelStageEvent added at 1.18.2, so the outline draws on it through the shared renderer's null-frustum path; at
# Forge 13.20 the bus has no functional addListener, so the registrar registers itself and listens through a
# @SubscribeEvent instance method.
forge/src/main/java/world/thearchive/wdl/forge/ForgeOutlineRegistrar.java

# Absent from dev, present at 1.17.1 and below: a downgrade proxy can hand a 1.11.2 client sections outside the 0..255
# column, and a section outside 0..15 crashes vanilla's unchecked section index on load; this pins that the codec writes
# only the block sections inside 0..15 and drops every out-of-range or section-less entry, light being section-resident
# in the ExtendedBlockStorage and NibbleArray carrier with no light-pad column to keep.
common/src/test/java/world/thearchive/wdl/adapter/ChunkSectionClampTest.java

# Below 1.18 a chunk's block entities nest under Level.TileEntities rather than the root block_entities, so the
# in-session stash merge locates a block entity by x, y and z through the Level compound. 1.11.2's NBT is classic MCP
# (NBTTagCompound, NBTTagList, NBTBase, getTag, setTag, getCompoundTag, getKeySet and tagCount rather than get, put,
# keySet (getAllKeys through 1.21.4) and size).
common/src/main/java/world/thearchive/wdl/adapter/ContainerMerge.java

# 1.11.2's NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase, getTag rather than get), and since 1.11.2's
# NBTTagList.get answers an out-of-range index with a sentinel NBTTagEnd rather than throwing, the passenger walk
# iterates the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/EntityTreeWalk.java

# 1.11.2's NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase and NBTPrimitive, with getTag, setTag and appendTag
# rather than get, put and add, and hasNoTags for an empty list, the 32-1.11 name of 1.12.2's isEmpty), a slot index is
# read via NBTPrimitive.getByte, BlockPos is net.minecraft.util.math, and the tag-list walks iterate the list, 1.11.2's
# NBTTagList.get answering an out-of-range index with NBTTagEnd.
common/src/main/java/world/thearchive/wdl/adapter/NbtMerge.java

# At 1.11.2 the chunk-codec seam names the classic Chunk and NBTTagCompound rather than LevelChunk and CompoundTag; the
# encode contract stays the shared seam over the pre-Flattening numeric section shape the plug writes.
common/src/main/java/world/thearchive/wdl/adapter/ChunkCodec.java

# Absent from dev, present at 1.13.2 and below: the section-coordinate long packing the outline draw set keys its
# buckets by, replacing SectionPos (a 1.14.4 class), shared by the producer and the per-band renderer so the encode and
# decode cannot drift; at 1.11.2 the block-to-section step round-trips through BlockPos.toLong and BlockPos.fromLong.
common/src/main/java/world/thearchive/wdl/adapter/SectionKey.java

# Absent from dev, present at 1.14.4 and below as the synchronous region store where 1.15 brought IOWorker: 1.11.2 also
# lacks RegionFileStorage (1.14.4's base), so the store owns a RegionFile cache keyed by region and reads and writes
# through the classic RegionFile.getChunkDataInputStream and getChunkDataOutputStream with CompressedStreamTools rather
# than NbtIo.
common/src/main/java/world/thearchive/wdl/adapter/WdlRegionStorage.java

# At 1.11.2 the container-capture seam names the classic NBTTagCompound, net.minecraft.item.ItemStack and
# net.minecraft.util.NonNullList rather than CompoundTag and the Mojmap ItemStack and core NonNullList; the on-disk
# Items shape underneath is unchanged.
common/src/main/java/world/thearchive/wdl/adapter/ContainerSink.java

# At 1.11.2 the entity-capture seam names the classic net.minecraft.entity.Entity, NBTTagCompound and
# net.minecraft.util.math.ChunkPos rather than the Mojmap Entity, CompoundTag and ChunkPos; the entities carrier shape
# is unchanged.
common/src/main/java/world/thearchive/wdl/adapter/EntitySink.java

# At 1.11.2 the per-tick entity buffer names the classic NBTTagCompound and net.minecraft.util.math.ChunkPos rather than
# CompoundTag and the Mojmap ChunkPos.
common/src/main/java/world/thearchive/wdl/adapter/EntityBuffer.java

# At 1.11.2 the equipment payload names the classic EntityEquipmentSlot (net.minecraft.inventory) and
# net.minecraft.item.ItemStack rather than EquipmentSlot and the Mojmap ItemStack.
common/src/main/java/world/thearchive/wdl/adapter/EquipmentEntry.java

# At 1.11.2 the lectern seam names the classic NBTTagCompound and net.minecraft.item.ItemStack; the lectern is a 1.14
# block absent at 1.11.2, so the axis is a compiling no-op behind the band-stable SPI, a documented limit.
common/src/main/java/world/thearchive/wdl/adapter/LecternSink.java

# At 1.11.2 the injected rim-primitive interface names the classic box type AxisAlignedBB rather than AABB.
common/src/main/java/world/thearchive/wdl/adapter/RimRenderer.java

# At 1.11.2 the per-frame outline context names the classic render types: the shared line buffer is
# net.minecraft.client.renderer.VertexBuffer (the 32-1.11 name of the class 39-1.12 calls BufferBuilder at 1.12.2),
# the frustum is ICamera rather than a Culler, and the camera position is a Vec3d.
common/src/main/java/world/thearchive/wdl/adapter/OutlineRenderContext.java

# At 1.11.2 the rim holder names the classic box type AxisAlignedBB and, BlockPos having no static getX, getY and
# getZ(long) unpackers (1.14.4), unpacks each packed cell through BlockPos.fromLong and reads it off the instance.
common/src/main/java/world/thearchive/wdl/adapter/OutlineRim.java

# At 1.11.2 the shared text helper measures against the classic FontRenderer (getStringWidth) rather than Font.width.
common/src/main/java/world/thearchive/wdl/client/ClientText.java

# 1.11.2 has no Screen or ConfirmScreen, so the delayed confirm extends the classic GuiYesNo (the six-argument
# constructor taking a GuiYesNoCallback, and setButtonDelay), applies its amber title through
# ITextComponent.getFormattedText, and takes a java.util.function.Consumer<Boolean> callback, the fastutil 7.0.12
# Minecraft 1.11.2 ships having no BooleanConsumer; its drawScreen also draws WdlToastOverlay, 1.11.2 having no vanilla
# toast host.
common/src/main/java/world/thearchive/wdl/client/WdlCaptureDisableConfirmScreen.java

# The loader-root loop is skip-if-absent for a loader root rather than dev's hard assertTrue, since 1.11.2 ships neither
# a fabric nor a neoforge tree; common's own root stays required and scanned. key.wdl.peek_hud is enrolled as an
# external consumer, the island's WdlForge constructing it outside the scanned roots, and the keys the 1.11.2 screens
# never reach (two button tooltips, two screen titles, a text field's message, and the downloads list narration, there
# being no NarratorChatListener below 1.12) sit in BAND_DROPPED_KEYS with their en_us strings kept so the catalog
# matches dev's. The file also carries the Java 8 forms the test tree uses throughout (Guava immutable collections,
# Paths.get).
common/src/test/java/world/thearchive/wdl/LangKeyCoverageTest.java

# Added, absent from dev: the boot smoke test of the bootsmoke source set common/build.gradle.kts declares at 1.11.2. It
# runs 1.11.2's Bootstrap.register against the Unimined-provisioned MCP-named jar on the Java 8 toolchain and resolves
# stone through the static Block.REGISTRY, proving the classic-MCP compile view and the runtime jar agree.
common/src/bootsmoke/java/world/thearchive/wdl/bootsmoke/BootSmokeTest.java

# Added, absent from dev: the NullMarked package marker of the bootsmoke source set.
common/src/bootsmoke/java/world/thearchive/wdl/bootsmoke/package-info.java

# At 1.11.2 the per-tick entity-buffer test names the classic NBTTagCompound and net.minecraft.util.math.ChunkPos,
# mirroring EntityBuffer.
common/src/test/java/world/thearchive/wdl/adapter/EntityBufferTest.java

# At 1.11.2 the passenger-walk test names the classic NBTTagCompound rather than CompoundTag, mirroring EntityTreeWalk.
common/src/test/java/world/thearchive/wdl/adapter/EntityTreeWalkTest.java

# At 1.11.2 DimensionType is the classic three-constant net.minecraft.world enum, so the completion-record test feeds
# DimensionType.OVERWORLD rather than a registry-keyed dimension.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionCompletionRecordTest.java

# At 1.11.2 the captured player's field types are the classic DimensionType.OVERWORLD, GameType, EnumDifficulty,
# NBTTagCompound and BlockPos.ORIGIN, so the fail-soft test builds its captured player from those.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionFailSoftTest.java

# At 1.11.2 DimensionType is the classic three-constant net.minecraft.world enum, so the finish-loss-line test feeds
# DimensionType.OVERWORLD.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionFinishLossLineTest.java

# At 1.11.2 the NBT is classic MCP (NBTTagCompound and NBTTagList with setString, setByteArray and setTag rather than
# put) and DimensionType is the classic three-constant enum, so the loss-log wiring test names those.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossLogWiringTest.java

# At 1.11.2 the session fixture names the classic DimensionType.OVERWORLD and opens the writer's save through
# AnvilSaveConverter(File, DataFixer) rather than a LevelStorageSource, the saves directory passed in, there being no
# LevelStorageSource.getBaseDir.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionReportStampFailSoftTest.java

# At 1.11.2 the map-archive test names the classic NBTTagCompound, NBTBase and NBTTagByteArray, writes the map dimension
# as an int via setInteger and reads a map stack's id through ItemStack.getMetadata, mirroring MapArchive.
common/src/test/java/world/thearchive/wdl/adapter/MapArchiveTest.java

# Below 1.13 a filled map's id is the item-level Damage short behind the minecraft:filled_map id gate rather than a map
# tag entry, so the collector test drives that shape through the MapHolderFixtures over classic MCP NBTTagCompound,
# mirroring MapIdCollector.
common/src/test/java/world/thearchive/wdl/adapter/MapIdCollectorTest.java

# Below 1.13 a filled map's id is the item-level Damage short behind the minecraft:filled_map id gate rather than a map
# tag entry, so the remap test drives that shape through the MapHolderFixtures over classic MCP NBTTagCompound and
# NBTTagList, mirroring MapIdRemap.
common/src/test/java/world/thearchive/wdl/adapter/MapIdRemapTest.java

# At 1.11.2 the NBT is classic MCP (NBTTagCompound and NBTTagString with setTag, hasKey, getTagList, tagCount and getTag
# rather than put, contains, getList and get), so the NBT-merge test names those, mirroring NbtMerge.
common/src/test/java/world/thearchive/wdl/adapter/NbtMergeTest.java

# At 1.11.2 a block outline is the classic box type AxisAlignedBB rather than AABB, so the draw-set test builds its rims
# from AxisAlignedBB, mirroring OutlineDrawSet and OutlineRim.
common/src/test/java/world/thearchive/wdl/adapter/OutlineDrawSetTest.java

# At 1.11.2 a rim box is the classic net.minecraft.util.math.AxisAlignedBB rather than AABB, so the outline-tracker test
# names AxisAlignedBB, mirroring OutlineTracker.
common/src/test/java/world/thearchive/wdl/adapter/OutlineTrackerTest.java

# Absent from dev, present at 1.14.4 and below: below 1.15 ItemStack.writeToNBT puts the live stack's own tag compound
# into its output rather than a copy, so this pins that the finish-time inventory capture hands the map-id remap a copy
# and not the held stacks. At 1.11.2 the player is the classic EntityPlayer (inventory.mainInventory) and a filled map's
# id is the item-level Damage short (ItemStack.getMetadata) rather than a tag entry.
common/src/test/java/world/thearchive/wdl/adapter/PlayerCaptureAliasTest.java

# Absent from dev, present at 1.13.2 and below with the SectionKey it tests: at 1.11.2 the packed block key is built
# with the classic BlockPos.toLong rather than a core-package asLong.
common/src/test/java/world/thearchive/wdl/adapter/SectionKeyTest.java

# At 1.11.2 the NBT is classic MCP (NBTTagCompound, setString), so the stash-holder test names those, mirroring
# StashHolder, whose compressed round trip is CompressedStreamTools.
common/src/test/java/world/thearchive/wdl/adapter/StashHolderTest.java

# The MC-free core test names the classic accessor in its prose: at 1.11.2 a packed position is BlockPos.toLong() rather
# than the core-package BlockPos.asLong() from 1.13 up, so the Javadoc and the opaque-packed-key comment say toLong().
common/src/test/java/world/thearchive/wdl/core/OpenClickIntentTest.java

# Every locale carries an added wdl.screen.downloads.proceed_action entry, right after restore_action, that no band from
# 1.13.2 up has: vanilla's gui.proceed key is a 1.13 addition, so 1.11.2's resume-confirm Continue button reads a
# wdl-owned key whose value is copied from each locale's own gui.proceed translation. The keybind category key is
# key.categories.wdl.downloader, vanilla's shape up to 1.21.8 (key.category from 1.21.9), where dev's catalogs carry
# key.category.wdl.downloader.
common/src/main/resources/assets/wdl/lang/*.json

# The Forge 13.20 loader detects a disconnect on a client-tick edge, one tick past both the registry revert
# Minecraft.loadWorld runs through FMLClientHandler.handleClientWorldClosing and the player it nulls, so the bridge SPI
# carries a second edge and a predicate dev's has no equivalent of, absent from 1.14.4 up: onLevelTeardown, fired from
# the loader's own world-unload signal while the block-state ids a chunk encode reads are still the ones the capture
# took them against and the local player is still live, and isConnectionClosed, which tells a subscriber which kind of
# teardown it is being called for; the Javadoc at 1.11.2 names the registry revert the hold protects, 1.13.2's only the
# player.
common/src/main/java/world/thearchive/wdl/platform/PlatformBridge.java

# Pins the pause-menu row placement against 1.11.2's GuiButton over the 200-wide column of the 1.11.2 pause screen
# (1.14.4's Button column is 204 wide), where 1.13.2's test builds the unnamed class_356 buttons.
common/src/test/java/world/thearchive/wdl/platform/AbstractPlatformBridgeTest.java

# Implements the PlatformBridge.onLevelTeardown of 1.13.2 and below as the same no-op registration every other arm of
# this fake bridge is, and answers its isConnectionClosed false: no headless test drives a teardown through the bridge,
# the wiring's own cases calling Wdl.onLevelTeardown directly.
common/src/test/java/world/thearchive/wdl/testsupport/HeadlessPlatformBridge.java

# Its own fake bridge implements the PlatformBridge.onLevelTeardown of 1.13.2 and below as a no-op like its neighbors,
# and answers isConnectionClosed false, no case of the test driving a teardown through the bridge.
common/src/test/java/world/thearchive/wdl/ResumeFlowTest.java

# The Java 8 floor cannot bind a pattern variable in a type test, so each instanceof over an NBT read is a local, an
# unbound instanceof and a cast rather than the pattern form dev uses; 1.11.2's NBT is classic MCP (NBTTagCompound,
# NBTTagList, NBTBase, getTag, hasKey and removeTag rather than get, contains and remove).
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferScrub.java

# The Java 8 floor cannot bind a pattern variable, so each instanceof over a tag is a local, a test and a cast; below
# 1.17 entities live inside the region chunk, so the resume scan hands the bank that whole chunk and the record side
# reads Level.Entities where dev reads a top-level Entities, while applyTo takes the flat Entities envelope the fold
# passes; the NBT is classic MCP (NBTTagCompound, NBTTagList, NBTBase, NBTTagInt, getTag and setTag).
common/src/main/java/world/thearchive/wdl/adapter/RecoveredEntityContent.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps through TestRegistries.bootstrap; 1.11.2 has no
# MerchantOffers or offers codec, so the fixture builds its offer as a MerchantRecipe of plain ItemStacks and serializes
# through MerchantRecipeList.getRecipiesAsTags; and the NBT is classic MCP (NBTTagCompound, NBTTagList, hasKey,
# getTagList, getCompoundTagAt and tagCount rather than contains, getList and size).
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferScrubTest.java

# Below 1.17 entities live inside the region chunk, so the scanned-chunk fixture wraps its entities under Level.Entities
# while the fresh-chunk fixture keeps the flat envelope, the two shapes the bank's two sides take; the merchant fixture
# is 1.11.2's MerchantRecipe of plain ItemStacks serialized through MerchantRecipeList.getRecipiesAsTags; and the later
# getIntOr, getListOrEmpty, getCompoundOrEmpty and getStringOr accessors are spelled in the classic MCP forms
# (getTagList, getCompoundTag, hasKey), getIntOr as a local intOr helper because getInteger reads an absent key back as
# a real zero.
common/src/test/java/world/thearchive/wdl/adapter/RecoveredEntityContentTest.java

# Below 1.17 entities live inside the region chunk, so the on-disk oracle reads the host chunk's Level.Entities through
# the mod's own WdlRegionStorage rather than a separate entities store (no IOWorker below 1.15, no SimpleRegionStorage
# below 1.20.5), and the fixture seeds a host chunk before each download because a fold with no host on disk is a
# counted loss. At 1.11.2 the merchant fixture is a MerchantRecipe of plain ItemStacks serialized through
# MerchantRecipeList.getRecipiesAsTags, the save is opened through AnvilSaveConverter(File, DataFixer), the dimension is
# DimensionType.OVERWORLD, and the accessors are the classic getTagList, getCompoundTag and getString.
common/src/test/java/world/thearchive/wdl/adapter/EntityResumeContentCarryTest.java

# Below 1.12 the pitest allowlist has no AdvancementSnapshot entry, that class being absent with the advancements it
# harvested; the reference is a string literal, so only this test's own staleness check sees it.
common/src/test/java/world/thearchive/wdl/PitestAllowlistEnrollmentTest.java

# Below 1.12 the progress writer lands one file rather than two, advancements being a 1.12 addition, so the cases that
# exercise two files at 1.12.2 exercise the stats file alone, CapturedProgress taking a UUID and one blob, and the
# cross-surface fail-soft case is a single swallowed-failure case.
common/src/test/java/world/thearchive/wdl/adapter/PlayerProgressWriterTest.java

# Minecraft 1.11.2 ships log4j 2.0-beta9 (2.8.1 at 1.12.2), whose Configuration has no addLogger or removeLogger and
# LogEvent no toImmutable, so the capture adds its appender to the Logger itself, clears the additive flag so captured
# output stops reaching the inherited appenders, and copies each event's level, message and throwable out on arrival.
common/src/test/java/world/thearchive/wdl/testsupport/LogCapture.java

# Java 8 bands gate NullAway behind errorProneUsable (java_version at least 21, the pinned Error Prone 2.50.0 being a
# Java 21 build the compile toolchain cannot load) and run annotations-only, where dev runs the checker unconditionally
# and carries no such gate; the loader-merge sentence names no wdl.common-merge plugin, 1.11.2 having no loader
# subproject and no such plugin.
build-logic/src/main/kotlin/wdl.nullness-conventions.gradle.kts

# Below Java 11 Checkstyle 10 cannot run on a Java 8 band's own toolchain, so this points the Checkstyle tasks at a
# modern Java 17 launcher, and PITest at the Java 8 toolchain where mcp_mappings_version marks a classic-MCP band
# (legacy FML cannot static-initialize past Java 8) or at the modern launcher otherwise; dev, a Java 25 band, carries no
# launcher block.
build-logic/src/main/kotlin/wdl.java-conventions.gradle.kts

# The root includes only common, 1.11.2 being Forge-only with its jar built by the island under forge/, where dev
# includes common, fabric and neoforge; its pluginManagement resolves the Unimined plugin from WagYourTail's mavens and
# the Forge loader and universal jars from MinecraftForge's, in place of the fabricmc, neoforged and parchment ones, and
# excludes log4j from the plugin portal. Against 1.13.2's it carries no tools/mojmap-bridge included build, 1.11.2
# building on classic MCP names with no Mojmap bridge to generate.
/settings.gradle.kts

# The apply-false plugin block declares only the catalog-pinned Spotless plugin, the one a subproject of the Forge-only
# 1.11.2 root applies, and its notes name that applier, where dev declares the set for common, fabric and neoforge
# (loom, moddev and mod-publish-plugin too).
/build.gradle.kts

# No NeoForm, NeoGradle or Mojang mapping exists for 1.11.2, so common builds on Unimined over the classic MCP mappings
# with the Forge loader in view (defaultRemapJar off, a bootsmoke source set, the 1.x journeymap-api from Maven Central,
# the untrimmed fastutil 7.0.12 on the compile classpath, javac's -Xmaxerrs raised), where dev builds on ModDevGradle.
common/build.gradle.kts

# The catalog carries only the plugins some build of the Forge-only 1.11.2 root applies, so it has no loom, moddev or
# mod-publish-plugin entry (dev's carries the three for its fabric and neoforge subprojects; 1.13.2's keeps loom for its
# Loom-built common); the tool-library entries match dev's.
gradle/libs.versions.toml

# Deleted: the source-merge consumer plugin's only appliers are dev's fabric and neoforge subprojects, neither of which
# the Forge-only 1.11.2 root includes, and the island merges common by direct path.
build-logic/src/main/kotlin/wdl.common-merge.gradle.kts

# The root wrapper pins Gradle 8.14.5, the newest 8.x line, where 1.13.2 and dev pin 9.7.1: the root's Unimined 1.4.1
# provision is untested on Gradle 9, and the pin matches the island's so both wrappers on 1.11.2 run one Gradle version.
gradle/wrapper/gradle-wrapper.properties

# The deep-band era of the workflow: the Forge island's own build step, a Forge artifact upload, and per-loader upload
# gates where dev uploads its jars ungated; against 1.13.2's the root and island builds each retry once after wiping the
# .gradle/unimined caches when a build fails before compileJava, the Unimined provision failing cold and passing warm.
.github/workflows/ci.yml

# The byte-reproducibility job reads the loader set off settings.gradle.kts's include lines and exits early with a
# message on a root that includes no fabric or neoforge subproject, as the Forge-only 1.11.2 root does not, where dev's
# job byte-diffs the two loader jars unconditionally; the Forge island's modJar is not re-diffed.
.github/workflows/reproducibility.yml

# Added, absent from dev: the Apache 2.0 license text. Minecraft 1.11.2 ships a 355-class trimmed cut of fastutil 7.0.12
# lacking nine of the types the mod binds, so the Forge island bundles those classes into the ship jar and copies this
# text beside them as META-INF/APACHE-2.0-fastutil.txt, fastutil's jar carrying no NOTICE file.
APACHE-2.0.txt
