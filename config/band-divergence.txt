# Band-divergence allowlist.
#
# checkBandDivergence diffs the working tree against the branch this band was minted from (mint_parent in
# gradle.properties) and against dev, over every tracked path except gradle.properties, this file and the
# per-band plug (common/src/main/java/world/thearchive/wdl/adapter/impl/). A path that differs from both must
# match a pattern below; a pattern that matches no path differing from dev is stale; core/ main sources may not
# differ from the parent and are never listed here, which along the mint chain keeps them one blob on every band.
# A path equal to the parent's is inherited and a path equal to dev's is not this band's divergence; an inherited
# entry stays live while its file differs from dev and is kept. Each entry records why the file legitimately
# carries band-local code, so neither the gate nor a future reader mistakes its difference for a lost fix or a
# bug. Anchor a reason to a version rather than to "this band", say "deleted" for a file dev has and this band
# does not, and re-read every inherited reason at a mint: the gate checks that a pattern is live, never that
# its reason is true. Patterns match as gitignore(5) matches: the last matching pattern is the one credited and a
# directory pattern covers its subtree, so a pattern nested under a registered directory is never credited and
# reads as stale.
#
# Format: gitignore-style per gitignore(5), one path pattern per line, blank lines and # comments ignored.
# Every pattern carries a # reason directly above it (checkBandDivergence fails on a pattern without one), so
# a shared reason across several files is repeated per line rather than written once over a group.

# Below 1.16 there is no RegistryAccess (a 1.16 addition), so the test registries are the static Registry tables the
# vanilla bootstrap populates: TestRegistries only runs that bootstrap, and a test reads a biome from the static
# Registry.BIOME. The registry-free forms the tests call (TestRegistries.bootstrap, no registries argument) are this
# class's.
common/src/test/java/world/thearchive/wdl/testsupport/TestRegistries.java

# Fabric registration is Fabric-API-coupled and moves across bands: Fabric API 0.28.5+1.15 registers the keybind
# through KeyBindingHelper.registerKeyBinding and the HUD through HudRenderCallback, where dev's release uses
# KeyMappingHelper and HudElementRegistry.
fabric/src/main/java/world/thearchive/wdl/fabric/WdlFabricClient.java

# Fabric API 0.28.5+1.15 ships no fabric-screen-api-v1 (ScreenEvents, Screens) and its fabric-command-api-v1 1.0.3
# has no ClientCommandManager, so the bridge exposes the pause-menu row as a factory the PauseScreenMixin reads and
# registers no /wdl client command; Fabric API 0.42.0+1.16 carries both.
fabric/src/main/java/world/thearchive/wdl/fabric/FabricPlatformBridge.java

# The fabric-rendering-v1 module of Fabric API 0.28.5+1.15 (1.2.1) has no WorldRenderEvents or WorldRenderContext
# (present in 0.42.0+1.16's 1.6.1), so the registrar subscribes nothing and the in-world outline does not draw on
# 1.15.2 Fabric.
fabric/src/main/java/world/thearchive/wdl/fabric/FabricOutlineRegistrar.java

# Added: fabric-networking-api-v1 1.0.1 (Fabric API 0.28.5+1.15) fires ClientPlayConnectionEvents.DISCONNECT only
# from ClientPacketListener.onDisconnect, which a pause-menu disconnect never reaches, where 0.42.0+1.16's 1.0.5 also
# fires it from Connection.channelInactive; the mixin auto-saves from the head of Minecraft.clearLevel instead.
fabric/src/main/java/world/thearchive/wdl/fabric/mixin/MinecraftDisconnectMixin.java

# Added: Fabric API 0.28.5+1.15 ships no fabric-screen-api-v1, so the pause-menu row is injected at the tail of
# PauseScreen.init through this mixin rather than ScreenEvents.AFTER_INIT.
fabric/src/main/java/world/thearchive/wdl/fabric/mixin/PauseScreenMixin.java

# Added with the PauseScreenMixin: the accessor for Screen's buttons and children lists the row is added to.
fabric/src/main/java/world/thearchive/wdl/fabric/mixin/ScreenAccessor.java

# Added with the two mixins: the mixin package's @NullMarked marker.
fabric/src/main/java/world/thearchive/wdl/fabric/mixin/package-info.java

# Added: the mixin configuration for the three Fabric mixins, at compatibility level JAVA_8.
fabric/src/main/resources/wdl.mixins.json

# Fabric API 0.28.5+1.15, the release pinned for 1.15.2, ships its own fabric.mod.json under the id fabric, where dev's
# later releases renamed it to fabric-api; the manifest names wdl.mixins.json and carries no journeymap entrypoint,
# no Fabric JourneyMap existing for 1.15.2.
fabric/src/main/resources/fabric.mod.json

# The villager-trade capture gametest carries the deep-band client forms: villager types are the flat npc package,
# EntityType is not yet EntityTypes, Minecraft.setScreen is not relocated into Minecraft.gui, the crosshair pick is
# GameRenderer.pick rather than LocalPlayer.raycastHitResult, an entity is resolved by UUID over entitiesForRendering
# rather than Level.getEntity(UUID), and below 1.21.5 the CompoundTag and ListTag accessors getCompound, getInt, getList
# and getString return the value, where from 1.21.5 they return Optional and dev reads through the defaulting
# getCompoundOrEmpty, getIntOr, getListOrEmpty and getStringOr.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlMerchantCaptureTest.java
# The shared capture driver reads the camera through GameRenderer.getMainCamera().getPosition(), the pre-26.2,
# pre-1.21.11 forms.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/CaptureDriver.java

# Below the 1.21.2 chunk-serialization cut vanilla has no SerializableChunkData.SectionData record, so this
# chunk-snapshot seam defines its own band-stable SectionData carrier for the static-write codec. Below 1.20.5
# ChunkStatus is in net.minecraft.world.level.chunk, not the .status subpackage added at that update.
common/src/main/java/world/thearchive/wdl/adapter/ChunkSnapshotSource.java

# Below 1.16 there is no RegistryAccess, so the capture and encode steps take no registries argument (1.15.2's chunk
# biomes come from the static biome registry).
common/src/main/java/world/thearchive/wdl/adapter/ChunkCodec.java

# Below 1.16 there is no RegistryAccess, so captureItems takes no registries argument.
common/src/main/java/world/thearchive/wdl/adapter/ContainerSink.java

# Below 1.16 there is no RegistryAccess, so the live encodeChunk and captureRootVehicle take no registries argument.
common/src/main/java/world/thearchive/wdl/adapter/EntitySink.java

# Below 1.16 there is no RegistryAccess, so captureBook takes no registries argument.
common/src/main/java/world/thearchive/wdl/adapter/LecternSink.java

# Below 1.16 there is no RegistryAccess, so serializeMap takes no registries argument.
common/src/main/java/world/thearchive/wdl/adapter/MapSink.java

# Below 1.16 there is no RegistryAccess, so capturePlayer takes no registries argument.
common/src/main/java/world/thearchive/wdl/adapter/PlayerSink.java

# Below 1.16 there is no WorldData, LevelStorageSource.LevelStorageAccess, LevelResource or RegistryAccess: a 1.15.2
# level.dat is a LevelData holding a LevelType and its generatorOptions, written by LevelStorage.saveLevelData with the
# player tag, so the seam builds a LevelData without client registries and saves through a LevelStorage.
common/src/main/java/world/thearchive/wdl/adapter/LevelDataWriter.java

# Reads the captured section through ChunkSnapshotSource's own SectionData rather than the vanilla
# SerializableChunkData.SectionData that exists only at 1.21.2 and above. Below the 1.21 update there is no
# jukebox_playable item component (a playable disc is a RecordItem instance), and a captured jukebox carries the
# pre-JukeboxSongPlayer playing state (IsPlaying, RecordStartTick, TickCount). Below 1.20.5 there are no item
# components: the RecordItem write is ItemStack#save rather than ItemStack.CODEC, a placed shulker's contents are
# read via the stack's BlockEntityTag element (getTagElement) plus ContainerHelper.loadAllItems rather than
# DataComponents.CONTAINER, and a placed beehive's occupants are its BlockEntityTag.Bees list rather than
# DataComponents.BEES. Java 8 form (java_version 8): no sealed interfaces (Java 17) or pattern-matching switch
# (Java 21), so the Candidate type is a plain interface and confirm() an if/instanceof chain; no records (Java 16),
# so HolderCandidate and ChunkBundles are final classes; no Map.of (Java 9), and the pattern instanceof tests
# are unbound. Below 1.17 there is no ShulkerBoxBlockEntity.CONTAINER_SIZE, so the shulker read sizes its list with
# the literal 27, and below 1.17 there is no slf4j, so the logger is log4j2's. Below 1.16 there is no RegistryAccess,
# so the constructor and the captureItems call take no registries, and BlockPlaceContext has only its UseOnContext
# constructor (the four-argument one is a 1.16 addition), so the place context is built from a UseOnContext.
common/src/main/java/world/thearchive/wdl/adapter/InteractionCapture.java

# Below the 1.21 update a captured jukebox's play state is the IsPlaying note-particle flag rather than the
# ticks_since_song_started sidecar, so the RecordItem carry-forward field carries IsPlaying. Below 1.20.5 the
# beehive block entity's occupants key is still Bees (capitalized), the pre-lowercasing form, so this field
# carries that key rather than bees.
common/src/main/java/world/thearchive/wdl/adapter/CapturedBlockField.java

# Below 1.21.2 the entity position wire differs: the absolute-only ClientboundTeleportEntityPacket replaces the
# newer teleport plus ClientboundEntityPositionSyncPacket and PositionMoveRotation, the byte-encoded packet
# rotations are decoded in the capture, and the single BOAT type replaces the per-wood split (the chest boat is a
# 1.19 addition). Below 1.19 there is no VecDeltaCodec, and 1.15.2's Entity has no getPacketCoordinates (a 1.16.5
# accessor absent at 1.16), so the packet-visible base position is the entity's position(). Below 1.20.2 the respawn
# and login packets carry no CommonPlayerSpawnInfo, so the entered dimension is read directly through the two
# packets' getDimension, which below 1.16 returns a DimensionType named through DimensionType.getName (no
# ResourceKey). Below 1.17 there is no GLOW_ITEM_FRAME type, ClientboundRemoveEntitiesPacket.getEntityIds returns an
# int array rather than an IntList, and Entity has no recreateFromPacket (an item frame and a leash knot are rebuilt
# from the add packet's position and data by hand); the relative move takes the three short deltas the loader tee
# reads off the packet's widened protected fields. Below 1.16 the equipment packet is ClientboundSetEquippedItemPacket
# carrying one slot and stack (ClientboundSetEquipmentPacket's slot list is a 1.16 addition), EntityType.chunkRange is
# the tracking range (renamed clientTrackingRange at 1.16), there is no STRIDER type, and the send-range sample names
# the dimension through Level.getDimension().getType(). Java 8 form: every pattern instanceof over the spawn, move
# and remove packets is unbound, and there is no Set.of.
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketCapture.java

# Java 8 form (java_version 8): no records (Java 16), so this MC-free packet-position carrier is a final class with
# explicit fields and accessors rather than the record the bands from 1.17.1 up carry.
common/src/main/java/world/thearchive/wdl/adapter/EntityPos.java

# Java 8 form: no records, so the captured player value is a final class with explicit fields and accessors; below
# 1.16 there is no ResourceKey, so its dimension is the DimensionType itself.
common/src/main/java/world/thearchive/wdl/adapter/CapturedPlayer.java

# Below 1.21.2 there is no chunk unsaved-listener, so re-capture polls isUnsaved over the keep-hot buffer in the
# session; it also reads ChunkSnapshotSource's own SectionData and reconstructs a packet entity through
# EntityPacketCapture.createSpawnEntity, whose EntityType.create takes no spawn reason (dev's takes an
# EntitySpawnReason). Below the 1.21 update leash state lives on Mob rather than the Leashable interface, so the
# reconstruct path rebinds a saved leash via Mob.setLeashedTo. Below 1.20.5 ChunkStatus is in
# net.minecraft.world.level.chunk, not the .status subpackage, and Level.getMapData is keyed by the raw String
# MapItem.makeKey returns rather than a MapId. Below 1.20.5 there is no SimpleRegionStorage, so the writer
# opens the vanilla IOWorker through WorldPaths (openRegionStorage) rather than constructing one directly. Below 1.17
# (era E3) entities live inside the region/ chunk, so the writer is built with the region opener alone (the
# openEntitiesStorage argument is dropped) and the entity flush folds into the host chunk's Level.Entities.
# Below 1.17 LevelChunk.isUnsaved is not the unsaved flag alone: it also returns true while the chunk's last save
# held entities and the game time differs from its last-save time, which nothing writes on a client, so clearing
# the unsaved flag could never quiet it and the dirty poll re-flagged every entity-bearing chunk on every tick.
# The re-arm therefore clears the last-save entity flag alongside the unsaved flag, which states what the
# re-encode wrote (the codec serializes blocks and block entities, never an entity), and leaves the query reading
# the unsaved flag until an entity enters the chunk again.
# Below 1.20.3 there is no Crafter menu or block entity, so the crafter open-bind and stash legs are dropped.
# Below 1.20.2 ClientPacketListener has no serverBrand, so the report environment reads the server brand through
# LocalPlayer.getServerBrand(). Below 1.20 Entity.level is a public field rather than the level() accessor, so the
# camera-anchor level guard reads camera.level directly. Below 1.17 Entity has no chunkPosition, getYRot/getXRot or
# shouldBeSaved (the chunk key is built from the entity's block position, the rotation read from the public
# yRot/xRot fields, and a local shouldSaveEntity stands in), and MapItemSavedData has no locked() snapshot, so the
# archived lock is set on the serialized tag. Below 1.17 there is no slf4j, so the logger is log4j2's. Below 1.16
# there is no ResourceKey or RegistryAccess: the dimension keys and the rebind are DimensionType values named through
# DimensionType.getName, no registries are held or passed to the codec, the sinks or the captures, and ServerData.motd
# is a plain String, left null until a ping fills it, read through an inline null guard rather than a sourceMotd
# helper dereferencing a Component. Below 1.16 there is no LevelStorageSource.LevelStorageAccess or LevelResource:
# the save opens through LevelStorageSource.selectLevel as a LevelStorage whose getFolder is the save root, and it
# holds no session lock to hand the writer (DirectoryLock is a 1.16 addition); Entity has no blockPosition (the
# BlockPos(Entity) constructor stands in), MultiPlayerGameMode has no getPreviousPlayerMode, so a spectator's
# captured game type falls back to survival, and NbtIo.readCompressed takes only a stream, so the prior level.dat is
# read through an InputStream. Java 8 form: no records, switch expressions, pattern instanceof or List.of/Map.of.
common/src/main/java/world/thearchive/wdl/adapter/LiveCaptureSession.java

# Deleted: the test pins the session's sourceMotd helper, which dereferences the Component motd ServerData carries
# from 1.16 up; below 1.16 the motd is a String and the session reads it through an inline null guard.
common/src/test/java/world/thearchive/wdl/adapter/ReportSourceMotdTest.java

# Absent from dev and carried by every band below 1.17 (1.10.2 through 1.16.5): from 1.17 up LevelChunk.isUnsaved
# is the unsaved flag alone, so the re-arm has nothing else to quiet and this gate has nothing to stand in for. Below
# 1.17 the query also returns true while the chunk's last save held entities and the game time differs from its
# last-save time, which nothing but the server's own chunk map ever writes, so the re-arm clears the last-save
# entity flag alongside the unsaved flag and this gate holds it to that: the unsaved clear alone leaves the query
# true, the re-arm leaves it reading the unsaved flag, and an entity entering the chunk re-flags it, the residual
# the poll's contract states.
# The fixture builds the chunk from the Biome-array ChunkBiomeContainer constructor (the registry parameter is
# 1.16.5's, absent at 1.16) and moves the world clock through Level.setGameTime, which 1.16 removed, and carries no
# vanilla bootstrap ahead of the headless level.
common/src/test/java/world/thearchive/wdl/adapter/RecaptureRearmTest.java

# Below 1.21.2 the toast host is Minecraft.getToasts (ToastComponent), not getToastManager. Below 1.20.3
# SystemToast has no custom-id class, so the two toast streams reuse two distinct vanilla SystemToastIds enum
# categories. Below 1.16 there is no MutableComponent or TextColor: a Component is styled in place through
# getStyle().setColor, which takes a ChatFormatting, so a brand color is quantized to the nearest of the sixteen
# named colors; there is no SystemToast.multiline (a 1.16 addition), so a job-done body goes through
# WdlMultilineToast; Button takes a String label and gained its OnTooltip constructor at 1.16, so the settings
# button subclasses Button and draws its hover label from renderButton, which below 1.16 carries no PoseStack.
# Java 8 form: no List.of (Java 9).
common/src/main/java/world/thearchive/wdl/platform/AbstractPlatformBridge.java

# Added at 1.15.2 and carried by 1.12.2 through 1.15.2 and by none from 1.16 up: below 1.16 SystemToast has no multiline
# factory and Toast no width or height hook, its body drawing as one line in a fixed 32-pixel frame, so this toast
# wraps the body and tiles the frame sprite taller.
common/src/main/java/world/thearchive/wdl/platform/WdlMultilineToast.java

# Below 1.16 Button takes a String label rather than a Component; the test drives the lowestColumnButton geometry
# seam alone, the pause-row anchor below 1.19.4 (the named-button anchor is 1.19.4 and above). Java 8 form: no
# List.of (Java 9), so the widget lists are Arrays.asList and Collections singleton and empty lists.
common/src/test/java/world/thearchive/wdl/platform/AbstractPlatformBridgeTest.java

# The chunk fixtures build ChunkSnapshotSource's own SectionData (vanilla has no SerializableChunkData.SectionData
# below 1.21.2). Below 1.17 there is no LevelHeightAccessor, so the section floor is the constant 0 rather than
# getMinSection. Below 1.20.5 ChunkStatus is in net.minecraft.world.level.chunk, not the .status subpackage added at
# that update. Below 1.20 the LevelChunkSection constructor takes a leading section y-index, so each synthetic section
# is built with it. Below 1.16 there is no RegistryAccess, so the fixtures take no registries and read the plains
# biome id from the static Registry.BIOME after the vanilla bootstrap. Java 8 form: no records, List.of or
# List.copyOf, so the Snapshot is a final class and the lists are Guava ImmutableList.
common/src/test/java/world/thearchive/wdl/testsupport/SyntheticChunks.java

# A headless overworld for the entity fixtures, built through the 1.15.2 Level constructor (a LevelData from
# LevelSettings and LevelType, a DimensionType, a chunk-source factory and a ProfilerFiller), the pre-1.16 form:
# below 1.16 there is no RegistryAccess, ResourceKey or ClientLevel.ClientLevelData, Level still carries setGameTime
# (removed at 1.16), and the tag manager is the 1.15.2 TagManager (TagContainer at 1.16.5). Below 1.20.5
# Level.getMapData is keyed by a String rather than a MapId, getFreeMapId returns an int rather than a MapId, and
# Level has no potionBrewing accessor to override. Below 1.20.3 Level has no tickRateManager() accessor to override
# either. Below 1.17 there is no GameEvent or LevelEntityGetter, so the gameEvent and getEntities overrides the
# bands from 1.17 up carry do not exist. Java 8 form: no List.of.
common/src/test/java/world/thearchive/wdl/testsupport/HeadlessLevel.java

# Below 1.20 the LevelChunkSection constructor takes a leading section y-index, supplied by the smoke test. Below
# 1.16 there is no RegistryAccess, so the biome lookup the codec relies on is the static Registry.BIOME after the
# vanilla bootstrap (registryOrThrow and lookupOrThrow are the later composite forms).
common/src/test/java/world/thearchive/wdl/testsupport/RegistriesSmokeTest.java

# Resolves an item by id via Registry.get, the pre-1.21.2 accessor (renamed getValue at 1.21.2). Below the 1.21
# update ResourceLocation has no parse factory, so an id string is built with the public ResourceLocation(String)
# constructor. Below 1.20.5 there are no item components: a custom name is set via ItemStack#setHoverName, a
# single stored stack is written with ItemStack#save (a {id, Count, tag} compound with Count as a byte, as a block
# entity's own save writes it), a written book's title/author/pages live directly on the item's tag, and
# ContainerHelper.saveAllItems takes no registries argument. Below 1.17 there is no Tag.TAG_COMPOUND, so the list
# read passes the raw type id 10.
common/src/test/java/world/thearchive/wdl/testsupport/ItemFixtures.java

# Below 1.21.2 ChunkSerializer.read needs a ServerLevel the headless test lacks, so the round-trip proves
# self-consistency by decoding each section's block-state and biome container directly; below 1.16 there is no
# RegistryAccess, so the encode takes no registries and the biome registry is the static Registry.BIOME.
common/src/test/java/world/thearchive/wdl/adapter/ChunkRoundTripTest.java

# Deleted: the whole test exercises the codec's synthesized blending_data, the 1.18 world-height blending marker
# (BlendingData is a 1.18 addition), which no chunk carries below 1.18.
common/src/test/java/world/thearchive/wdl/adapter/impl/ChunkCodecImplTest.java

# Below 1.20.5 there is no SimpleRegionStorage or RegionStorageInfo, so the round trip opens the vanilla
# IOWorker through WorldPaths (store/load) rather than constructing one directly. Below 1.17 (era E3) there is no
# separate entities/ region: entities live inside the region/ chunk, so the round trip seeds a host region chunk and
# folds the entity carrier into its Level.Entities through RegionChunkWriter.foldEntitiesIntoRegion, then reads the
# entity back from that chunk. The sink's carrier is a bare {Entities:[...]} with no DataVersion or Position (those
# were the E2a entities-region self-description, dropped below 1.17). Below 1.16 the dimension key is the
# DimensionType (no ResourceKey) and TestRegistries only runs the bootstrap. Java 8 form: no List.of.
common/src/test/java/world/thearchive/wdl/adapter/EntityRoundTripTest.java

# Below 1.16 there is no WorldGenSettings, RegistryAccess or com.mojang.serialization (DataFixerUpper 2.0.24 carries
# no Codec, DataResult or DynamicOps): a 1.15.2 level.dat holds RandomSeed, MapFeatures and the generator options
# at its Data root, its GameRules load from the tag through loadFromTag, buildLevelData takes no registries, and the
# world data's createTag takes only the player tag; the Lifecycle assertions (PrimaryLevelData is 1.16's) are dropped.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatWorldOutputTest.java

# Deleted: the test drives the plug's memoized worldgen reconstruction (VanillaWorldgenRegistries), a 1.16
# registry-rework need the 1.15.2 plug has no counterpart for, its warmWorldgen being empty.
common/src/test/java/world/thearchive/wdl/adapter/impl/LevelDataWriterWarmTest.java

# Deleted with the warm test: it exercises VanillaWorldgenRegistries, which the 1.15.2 plug does not carry.
common/src/test/java/world/thearchive/wdl/adapter/impl/VanillaWorldgenRegistriesTest.java

# The headless entity double overrides hurt(DamageSource, float), the pre-1.21.2 damage entry point (the
# ServerLevel-taking hurtServer is 1.21.2 and above). Below 1.20.5 Entity.defineSynchedData is no-arg (the
# SynchedEntityData.Builder parameter is 1.20.5 and above), so the double overrides that form too. Below 1.16
# TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/CaptureAnchorTest.java

# The headless entity double overrides hurt(DamageSource, float), the pre-1.21.2 damage entry point (the
# ServerLevel-taking hurtServer is 1.21.2 and above). Below 1.20.5 Entity.defineSynchedData is no-arg, so the
# double overrides that form too; below 1.20.5 there is no SimpleRegionStorage, so the writer and the
# read-back open the vanilla IOWorker through WorldPaths; and below 1.20.5 there is no ItemCost, so a merchant
# offer's buy cost is a plain ItemStack. Below 1.16 the dimension keys are DimensionType values, the sink doubles
# and the codec take no registries (no RegistryAccess), the entity UUID is the UUIDMost/UUIDLeast pair
# CompoundTag.putUUID writes (no SerializableUUID), and the writer takes no session-lock argument.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossTallyTest.java

# The headless mob double overrides hurt(DamageSource, float) (hurtServer with a ServerLevel is 1.21.2 and above).
# Below 1.16 Entity.saveWithoutId writes this.dimension.getId(), which the constructor sets only from a non-null
# level, so both doubles are built against the HeadlessLevel. Below 1.17 Entity has no shouldBeSaved, so the
# one-player-vehicle double reports isVehicle and hasOnePlayerPassenger instead and the precondition asserts those;
# no Tag.TAG_COMPOUND, so the list reads pass the raw type id 10. Below 1.16 the sink calls take no registries.
# Java 8 form: no List.of.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkRootVehicleTest.java

# The geared mob double is built against the HeadlessLevel (below 1.16 Entity.saveWithoutId writes the dimension id
# the constructor takes from a non-null level). Below 1.16 there is no PIGLIN or ZOMBIFIED_PIGLIN type and no
# netherite or warped-fungus item (the Nether Update), so the natural-gear cases are the ZOMBIE_PIGMAN's gold
# sword and ingot and a zombie's golden chestplate. Java 8 form: no Map.of (Java 9), so the gear maps are Guava
# ImmutableMap.of.
common/src/test/java/world/thearchive/wdl/adapter/impl/NaturalEquipmentTest.java

# Absent from dev and carried by every band from 1.10.2 to 1.20.4. Below 1.17 there is no Entity.getSlot or SlotAccess
# (the mount inventory field is reached by reflection to prove slot 1 empty), no ItemStack.is(Item) or Tag.TAG_COMPOUND,
# and Entity.remove() stands where 1.17 added setRemoved; below 1.16 the sink calls take no registries; no List.of.
common/src/test/java/world/thearchive/wdl/adapter/impl/MountArmorCaptureTest.java

# Absent from dev, carried by 1.10.2 through 1.21.4. Below 1.17 no Entity.getSlot or SlotAccess (the mount inventory
# field is read by reflection to prove slot 0 empty), ItemStack.is(Item) or Tag.TAG_COMPOUND; below 1.16 the fixture
# saddles through AbstractHorse.setSaddled (a flag-only setter 1.16 dropped), the sinks take no registries; no List.of.
common/src/test/java/world/thearchive/wdl/adapter/impl/SaddleCaptureTest.java

# Below the 1.21 update the HUD render callback carries a float partial-tick rather than a DeltaTracker, so the
# shared overlay's render entry point takes a float; below 1.16 the GUI render path carries no PoseStack and
# Font.width takes only a String, and there is no MutableComponent. Java 8 form: no records (Java 16) or switch
# expressions (Java 14), so the Frame is a final class and the phase, glyph, color and anchor switches are statements.
common/src/main/java/world/thearchive/wdl/client/WdlHudOverlay.java

# Absent from dev and carried by every band below 1.17 (1.10.2 through 1.16.5): below 1.17 there is no CycleButton
# widget (it arrived at 1.17 with the narration API), so this cycle button reproduces the value-cycling toggle the bands
# from 1.17 up get from vanilla's CycleButton, without the 1.17 narration API; below 1.16 Button and setMessage take a
# String label rather than a Component.
common/src/main/java/world/thearchive/wdl/client/WdlCycleButton.java

# Below 1.20 there is no FaviconTexture (added by the 1.20 GUI overhaul), so the client draw seam's blitFavicon names
# the world icon's registered ResourceLocation instead of the 1.20+ FaviconTexture handle. The draw surface itself
# (GuiGraphics, likewise 1.20+) is reimplemented in the plug, which the diff excludes.
common/src/main/java/world/thearchive/wdl/adapter/RenderSurface.java

# Below the 1.21 update Util.OS has no openPath(Path), so the open-folder affordances call openFile(File). Below 1.20.5
# ObjectSelectionList.Entry has no mouseClicked override, so a clicked row returns false and the list's
# setFocused-driven selection never fires (1.20.5 and above override it to return true), so the download row overrides
# mouseClicked to select itself. Below 1.20.5 getScrollbarPosition is a fixed width/2 + 124 that ignores the list left
# and the row width, so the list derives it from the row right edge the way vanilla does from 1.20.5 up, restoring hover
# and clicks on the right of a row. Below 1.20.3 AbstractSelectionList takes a six-argument (top, bottom, itemHeight)
# constructor and exposes the list left as the protected x0 field with setLeftPos rather than getX/setX. Below 1.20.2
# EditBox.moveCursorToEnd takes no argument. Below 1.20.2 AbstractSelectionList also fills tiled dirt above and below
# itself, sized as if its width and height were the screen's; setRenderTopAndBottom is 1.16.5's (absent at 1.16 and
# 1.15.2), so the in-world download list overrides renderHoleBackground to a no-op instead. Below 1.17 Screen has no
# addRenderableWidget, removeWidget or clearWidgets, so the buttons and children lists are cleared directly, and the
# name field and primary button take initial focus through setFocused plus EditBox.setFocus(true); there is no per-entry
# updateNarration hook, so the download row narrates through the list's setSelected override calling
# NarratorChatListener.INSTANCE.sayNow, the way vanilla WorldSelectionList narrates its rows at 1.15.2; the shared lang
# key wdl.screen.downloads.narration stays live and identical across all bands. Below 1.16 there is no addWidget (a
# non-button child goes straight into the children list), no CommonComponents (gui.done through I18n), no
# MutableComponent or TextColor (a brand color is set in place as the nearest ChatFormatting), no PoseStack on
# Screen.render, renderButton or the list render, Button and setMessage take String labels and Button has no OnTooltip
# constructor (the download tooltip is dropped), Font.width and Screen.renderTooltip take Strings, and Util.sanitizeName
# is absent (present at 1.16.5), so the icon location is keyed by the folder-name hash alone; the banner link label
# carries no underline style. Java 8 form: no switch expressions (Java 14), no-argument Optional.orElseThrow (Java 10),
# Optional.isEmpty (Java 11) or List.of (Java 9).
common/src/main/java/world/thearchive/wdl/client/WdlDownloadsScreen.java

# Below 1.20.5 getScrollbarPosition is a fixed width/2 + 124 that assumes the 220-wide default row, so with the
# settings list's wider row it lands inside each row and cuts through the controls, so the list derives it from the
# row right edge the way vanilla does from 1.20.5 up. Below 1.20.3 AbstractSelectionList takes a six-argument (top,
# bottom, itemHeight) constructor and exposes the list left as the protected x0 field. Below 1.20.2
# AbstractSelectionList fills tiled dirt above and below itself, which is the mechanism that hides its unclipped row
# overflow at the top and bottom edges (the list does not scissor at 1.15.2), so the settings screen draws the list
# by hand before its own tab strip and footer buttons (from 1.17 up the list is an addRenderableWidget child
# Screen.render paints): the fill hides the row overflow while the buttons draw on top of it. Below 1.17 there is no
# CycleButton, so every toggle and cycle control is a WdlCycleButton, and Screen has no addRenderableWidget or
# clearWidgets, so the buttons and children lists are cleared directly. Below 1.16 there is no addWidget (the list
# goes straight into the children list), no CommonComponents (gui.done, gui.cancel and options.on/off are named by
# key), no MutableComponent or TextColor (a brand color is set in place as the nearest ChatFormatting), no PoseStack
# on the render path, Button takes String labels and has no OnTooltip constructor (the defaults tooltip is dropped),
# and the section label carries no bold style. The hidden row is outlineLineWidthScale (dev hides none): 1.15.2's
# RenderType.lines() is POSITION_COLOR with no width element, so the knob would change nothing. Java 8 form: no
# records (Java 16), Set.of, List.of or Map.entry (Java 9).
common/src/main/java/world/thearchive/wdl/client/WdlSettingsScreen.java

# Below 1.16 ConfirmScreen takes its two button labels as Strings rather than Components, so the labels pass through
# getString; below 1.21.6 ConfirmScreen exposes yesButton and noButton only as the Component labels and its setDelay
# holds every exit button inert together, so the confirm delays both choices through setDelay.
common/src/main/java/world/thearchive/wdl/client/WdlCaptureDisableConfirmScreen.java

# JourneyMap 5.7.0 for 1.15.2 is Forge-only and carries the 1.x API (journeymap.client.api, pinned as
# journeymap_api_coordinate 1.14.4-1.5 since jm.gserv.me publishes no 1.15.2 stem), not the 2.0 API
# (journeymap.api.v2) 1.16.5 and dev bind, so the binding stays under compat/journeymap. This entry point implements
# journeymap.client.api.IClientPlugin under the ClientPlugin annotation JourneyMap's Forge scan discovers.
common/src/main/java/world/thearchive/wdl/compat/journeymap/WdlJourneyMapPlugin.java

# The overlay driver binds the journeymap.client.api 1.x surface (IClientAPI.subscribe/onClientEvent) and constructs
# each PolygonOverlay with the int dimension id the 1.5 API keys overlays by (DimensionType.getId), rather than the
# journeymap.api.v2 ClientEventRegistry and IClientAPI 1.16.5's and dev's driver bind.
common/src/main/java/world/thearchive/wdl/compat/journeymap/JourneyMapOverlayDriver.java

# The polygon builder targets journeymap.client.api.model, which in the 1.5 API has no MapPolygonWithHoles, so it emits
# one convex MapPolygon per coverage rectangle rather than the holed hulls; the 1.14.4-1.5 jar declares MapPolygon's
# List constructor over the MCP-mapped BlockPos, so that constructor is reached reflectively.
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygons.java

# The shared coverage-polygon tracer is public, the visibility it carries on 1.16.5 (where the re-homed
# compat/journeymap/v2 package reaches it) where dev keeps it package-private, and its JDK collection factory is a
# Guava ImmutableList for the Java 8 floor.
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygonTracer.java

# Below 1.16 there is no ResourceKey, so the rebind is asserted with DimensionType values and the server-named
# dimension case (a ResourceKey outside the vanilla three) is dropped; TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionDimensionRebindTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument. Below 1.16 there is no ResourceKey: the
# player Dimension tag is the int DimensionType id, so setDimension is driven with DimensionType values and the
# verbatim-id case is dropped, the RootVehicle Attach UUID is the UUIDMost/UUIDLeast pair (no SerializableUUID), and
# the sink calls take no registries.
common/src/test/java/world/thearchive/wdl/adapter/PlayerTagTest.java

# Below the 1.21 update ResourceLocation has no parse factory, so a block-entity id string is built with the public
# ResourceLocation(String) constructor. Below 1.20.5 loadStatic takes no Provider and ContainerHelper.loadAllItems and
# saveAllItems take no registries argument; the save is BlockEntity.save(CompoundTag), saveWithFullMetadata being
# present at 1.18.2 and absent at 1.17.1. Below 1.17 EntityBlock.newBlockEntity takes a BlockGetter rather than a
# position and state (the position goes through setLevelAndPosition); below 1.16 BlockEntity.loadStatic takes the
# tag alone (the BlockState parameter is a 1.16 addition) and TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelity.java

# Below 1.19 a captured jukebox carries only RecordItem: vanilla's JukeboxBlockEntity.saveAdditional writes no
# IsPlaying or tick sidecar (absent at 1.19, present by 1.19.4), so the jukebox merge fixtures set only that key.
# Below 1.20.5 a beehive block entity's occupants key is still Bees (capitalized), the pre-lowercasing form, so these
# merge fixtures use that key and the beehive fixture goes through the unchecked malformedChunkTagWith instead of the
# fidelity-checked chunkTagWith. Below 1.18 the merge reads the chunk block entities under Level.TileEntities, and
# fastutil 8.2.1 has no LongSet.of() so the empty set is LongSets.EMPTY_SET.
common/src/test/java/world/thearchive/wdl/adapter/ChunkMergeTest.java

# Below the 1.21 update a captured jukebox is marked playing via IsPlaying rather than the ticks_since_song_started
# sidecar, so this round-trip asserts the playing flag. Below 1.20.5 there are no item components: the RecordItem
# write is ItemStack#save (read back via vanilla's own ItemStack.of) and the beehive occupants are the
# pre-component {EntityData, TicksInHive, MinOccupationTicks} entries under Bees rather than Occupant.LIST_CODEC.
# Below 1.16 TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/InteractionMergeRoundTripTest.java

# Below 1.20.5 there is no SimpleRegionStorage or RegionStorageInfo, so the region storage this writer
# drains against is the vanilla IOWorker (store/load/synchronize/close), opened per dimension through
# WorldPaths. Below 1.17 (era E3) entities live inside the region/ chunk under Level.Entities, not in a separate
# entities/ region, so the ENTITIES write task folds into the host region chunk through
# RegionChunkWriter.foldEntitiesIntoRegion rather than draining a second entities/ storage: the entities opener
# and Storages are dropped, the resume scan reads the same region/ chunk, and a fold with no host chunk is a
# counted loss. Below 1.16 there is no ResourceKey, so the tasks and observers are keyed by DimensionType (named
# through DimensionType.getName), and no DirectoryLock: LevelStorage holds no OS lock, so the writer takes no
# AutoCloseable session access to close after the drain.
common/src/main/java/world/thearchive/wdl/adapter/AsyncSaveWriter.java

# Below 1.20.5 there is no SimpleRegionStorage, so the merge-write reads and writes through the vanilla
# IOWorker's load and store rather than read and write. Below 1.17 (era E3) entities live inside the region/ chunk,
# so this carries foldEntitiesIntoRegion, the entity fold that unions a captured entity carrier into the host chunk's
# Level.Entities through EntityMerge and writes the chunk back, the entity sibling of the container rewrite path; and
# the terrain merge-write preserves the on-disk Level.Entities the fresh terrain tag lacks, so a routine terrain
# re-flush of a revisited chunk does not drop the entities a prior fold saved there.
common/src/main/java/world/thearchive/wdl/adapter/RegionChunkWriter.java

# Below 1.20.5 there is no RegionStorageInfo, so instead of dev's regionStorageInfo and entitiesStorageInfo
# suppliers this seam opens the vanilla IOWorker directly (openRegionStorage). Below 1.17 (era E3) there is no
# separate entities/ region: entities live inside the region/ chunk, so the seam has no entitiesDirectory (dev's,
# carried from 1.17.1 up) and maps and opens only region/. Below 1.16 there is no ResourceKey, so the dimension key
# is the DimensionType itself.
common/src/main/java/world/thearchive/wdl/adapter/WorldPaths.java

# Added: at 1.15.2 the IOWorker and RegionFileStorage constructors are package-private (1.16 made IOWorker's
# protected), so the tests open a region storage through this shim in the vanilla package rather than a test-local
# IOWorker subclass.
common/src/test/java/net/minecraft/world/level/chunk/storage/WdlRegionStorage.java

# Added with the WdlRegionStorage shim: its package's @NullMarked marker.
common/src/test/java/net/minecraft/world/level/chunk/storage/package-info.java

# Below 1.20.5 there is no SimpleRegionStorage, so the drain is proven against the vanilla IOWorker, reached
# through the WdlRegionStorage shim (the constructor is package-private at 1.15.2) rather than constructing one via
# RegionStorageInfo/DataFixers. Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument. Below 1.16
# the tasks are keyed by DimensionType, the codec and sinks take no registries, and the writer takes no session-lock
# argument, so the output-order test asserts the export against the level.dat finalizer alone; the entity UUID is
# read back through EntityMerge.readUuid (the UUIDMost/UUIDLeast pair).
common/src/test/java/world/thearchive/wdl/adapter/AsyncSaveWriterTest.java

# Deleted: the whole test exercises chiseled-bookshelf occupancy flush, and ChiseledBookShelfBlock does not exist
# below 1.19.
common/src/test/java/world/thearchive/wdl/adapter/BookshelfOccupancyFlushTest.java

# Below 1.20.5 there is no SimpleRegionStorage, so the prior-write and read-back open the vanilla IOWorker
# through WorldPaths rather than constructing one directly. Below 1.16 the dimension key is the DimensionType, the
# codec and sinks take no registries, and the writer takes no session-lock argument.
common/src/test/java/world/thearchive/wdl/adapter/CarryForwardWiringTest.java

# Below 1.20.5 there is no SimpleRegionStorage, so the writer and the entity read-back open the vanilla
# IOWorker through WorldPaths. Below 1.20.5 a filled map's id is the raw int tag."map", not a components entry.
# Below 1.16 the dimension key is the DimensionType, the sink calls take no registries, the entity UUID is written
# through CompoundTag.putUUID (the UUIDMost/UUIDLeast pair, no SerializableUUID), and the writer takes no
# session-lock argument.
common/src/test/java/world/thearchive/wdl/adapter/EntityVehicleRelocationTest.java

# Below 1.20.5 there is no SimpleRegionStorage, so the writer and the entity read-back open the vanilla
# IOWorker through WorldPaths. The scan hands the bank a region chunk, whose entities sit under Level.Entities below
# 1.17, so the fixture that stands in for a prior download's record builds that shape, and the writer takes the
# region opener alone, so each test seeds the host chunk the mount folds into. Below 1.16 the dimension keys are
# DimensionType values, the prior level.dat is written through the LevelStorage that LevelStorageSource(Path, Path,
# DataFixer).selectLevel returns (no LevelStorageAccess or createDefault), the player UUID is the UUIDMost/UUIDLeast
# pair CompoundTag.putUUID writes, and the sink doubles and the writer take no registries or session lock.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionResumedMountReleaseTest.java

# Below 1.17 (era E3) entities live inside the region/ chunk, so the writer is built with the region opener alone: the
# entities-opener argument that must not open is dropped. Below 1.16 the session is built with DimensionType keys and
# no registries, the captured player carries a DimensionType, and the writer takes no session-lock argument.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionFailSoftTest.java

# Below 1.17 (era E3) the writer takes the region opener alone, so the entities-opener argument is dropped. Below 1.16
# the session and the holder fixtures take no registries, the session keys are DimensionType values, and the writer
# takes no session-lock argument. Java 8 form: no Files.writeString (Java 11), so the blocking file is Files.write.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossLogWiringTest.java

# Below 1.16 the session is built with DimensionType keys and no registries (TestRegistries only runs the
# bootstrap). Java 8 form: no Map.of (Java 9), so the expected loss breakdowns are Collections.singletonMap and
# emptyMap.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionCompletionRecordTest.java

# Below 1.16 the session is built with DimensionType keys and no registries, and LevelStorageSource has no
# createDefault factory, so the source is constructed with its saves and backups paths and DataFixers.getDataFixer().
# Java 8 form: no Files.readString (Java 11), so the pending report is read through readAllBytes.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionReportStampFailSoftTest.java

# Below 1.16 the session is built with DimensionType keys and no registries (TestRegistries only runs the
# bootstrap).
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionFinishLossLineTest.java

# Below 1.20.5 there is no SimpleRegionStorage, so the sweep is proven against the vanilla IOWorker, reached
# through the WdlRegionStorage shim (the constructor is package-private at 1.15.2). Below 1.20.5
# ContainerHelper.loadAllItems takes no registries argument. Below 1.16 the dimension key is the DimensionType, the
# codec and sinks take no registries, and the writer takes no session-lock argument.
common/src/test/java/world/thearchive/wdl/adapter/OrphanedContainerSweepTest.java

# Below 1.20.5 there is no SimpleRegionStorage or RegionStorageInfo, so this integration test drives the
# vanilla IOWorker opened through WorldPaths (store/load/synchronize/close; loadAsync is present at 1.17.1 and absent
# at 1.16.5) rather than constructing one directly. Below 1.16 the dimension key is the DimensionType and the codec
# and sinks take no registries.
common/src/test/java/world/thearchive/wdl/adapter/RegionIntegrationTest.java

# Below 1.20.5 there is no SimpleRegionStorage, so this drives the vanilla IOWorker directly rather than that
# wrapper, through the WdlRegionStorage shim, the IOWorker constructor being package-private at 1.15.2 (1.16 made
# it protected).
common/src/test/java/world/thearchive/wdl/testsupport/RegionRoundTrip.java

# Below 1.20.5 ChunkStatus is in net.minecraft.world.level.chunk, not the .status subpackage added at that update.
common/src/main/java/world/thearchive/wdl/adapter/OutlineTracker.java

# Below 1.16 there is no ResourceKey or SerializableUUID: the player's Dimension tag is the int DimensionType id
# (read back through DimensionType.getById) and the RootVehicle Attach UUID is written through CompoundTag.putUUID,
# the UUIDMost/UUIDLeast long pair.
common/src/main/java/world/thearchive/wdl/adapter/PlayerTag.java

# Below 1.20.2 there is no AdvancementProgress.CODEC, so the advancement value is serialized through vanilla's own
# AdvancementProgress.Serializer Gson adapter rather than encoded through a codec.
common/src/main/java/world/thearchive/wdl/adapter/PlayerProgressSerializer.java

# Below 1.16 there is no SerializableUUID, so the fixture's entity UUID is written through CompoundTag.putUUID, the
# UUIDMost/UUIDLeast long pair.
common/src/test/java/world/thearchive/wdl/testsupport/EntityFixtures.java

# Below 1.16 the player UUID is written through CompoundTag.putUUID (the UUIDMost/UUIDLeast pair), the captured
# player carries a DimensionType, buildLevelData takes no registries, and the write goes through the LevelStorage
# that LevelStorageSource(Path, Path, DataFixer).selectLevel returns (no LevelStorageAccess or createDefault).
common/src/test/java/world/thearchive/wdl/adapter/PriorPlayerReadRoundTripTest.java

# Below 1.16 a level.dat carries no WorldGenSettings or dimensions compound (the generator is a LevelType and its
# options at the Data root), buildLevelData takes no registries, the write goes through the LevelStorage that
# LevelStorageSource(Path, Path, DataFixer).selectLevel returns (no LevelStorageAccess or createDefault), and
# NbtIo.readCompressed and writeCompressed take only a stream (the File overloads present at 1.16.5 are absent at
# 1.16 as well); DataFixerUpper 2.0.24 carries no com.mojang.serialization, so the ops-based GameRules read is gone.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatRoundTripTest.java

# Below 1.17 there is no slf4j, so the logger is log4j2's, and the locator Javadoc names the Level.TileEntities list a
# pre-1.18 chunk nests its block entities under. The pre-1.21.5 getList(key, 10) replaces getListOrEmpty. Java 8 form:
# a type test cannot bind a pattern variable (Java 16), so each instanceof over an NBT read is an unbound instanceof and
# a cast, and the Xp carry-forward reads the tag with a null check.
common/src/main/java/world/thearchive/wdl/adapter/EntityContainerMerge.java

# Below 1.20.5 an item is {id, Count, tag}; the walk recurses into a shulker's nested items via
# tag.BlockEntityTag.Items and a bundle's via tag.Items, rather than the minecraft:container/minecraft:bundle_contents
# components.
common/src/main/java/world/thearchive/wdl/adapter/ItemTreeWalk.java

# Below 1.20.5 a filled map's id is the raw int tag."map", not a minecraft:map_id component, so the remap reads
# and writes that key directly rather than through MapId.CODEC. Java 8 form: no pattern instanceof.
common/src/main/java/world/thearchive/wdl/adapter/MapIdRemap.java

# Below 1.20.5 a filled map's id is the raw int tag."map", not a minecraft:map_id component, so the collector
# reads that key directly rather than through MapId.CODEC. Java 8 form: no pattern instanceof.
common/src/main/java/world/thearchive/wdl/adapter/MapIdCollector.java

# Below 1.17 there is no bundle item (BundleItem is a 1.17 addition), so the bundle-nested map fixture is dropped and
# the collector test reads a top-level and a shulker-nested id; below 1.16 the holder fixtures take no registries.
# Java 8 form: no Set.of (Java 9), so the expected id sets are Guava ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/adapter/MapIdCollectorTest.java

# Below 1.17 there is no bundle item, so the bundle-nested fixture is dropped from the remap tests; below 1.16 the
# holder fixtures take no registries. Java 8 form: no Set.of or Map.of (Java 9), so the expected id sets and the
# remap table are Guava ImmutableSet.of and ImmutableMap.of.
common/src/test/java/world/thearchive/wdl/adapter/MapIdRemapTest.java

# Below 1.16 there is no com.mojang.serialization (DataFixerUpper 2.0.24 carries no Codec or NbtOps), so the map
# content hash reads colors, scale and dimension off the data tag with the plain NBT getters; the dimension is the
# int id 1.15.2's MapItemSavedData writes (1.16 stores a ResourceKey string), hashed by its string form.
common/src/main/java/world/thearchive/wdl/adapter/MapArchive.java

# Below 1.16 the map dimension is the int DimensionType id (MapItemSavedData.dimension is a DimensionType), so the
# hash fixtures write it as such, and the holder fixtures take no registries (no RegistryAccess). Java 8 form: no
# Set.of (Java 9), so the expected archive-id sets are Guava ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/adapter/MapArchiveTest.java

# Below 1.17 there is no bundle item (BundleItem is a 1.17 addition), so the change signal carries no bundle-contents
# leg and compares per-slot stack identity and count alone.
common/src/main/java/world/thearchive/wdl/adapter/MenuChangeTracker.java

# Below 1.20.5 MerchantOffers has no CODEC, so the offers holder is written via the vanilla
# MerchantOffers.createTag (a lenient NBT write that never rejects an offer) rather than encoding through
# RegistryOps; the per-villager isolation of a rejecting serialize is inert below 1.20.5 but kept because the
# caller is band-stable. Java 8 form: no pattern instanceof.
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferCapture.java

# Below 1.20.5 an item's data lives in its tag compound rather than a components map: the lodestone target is the
# raw LodestonePos/LodestoneDimension keys (the lodestone compass itself is a 1.16 addition, absent at 1.15.2), and
# a beehive's occupants and the hive's own flower position (carried by a pre-component item's whole block-entity
# copy) are tag.BlockEntityTag.Bees[].EntityData.FlowerPos and tag.BlockEntityTag.FlowerPos, both scrubbed by the
# same pass. Java 8 form: every pattern instanceof over the item, entity, equipment, passenger and bee reads is
# unbound.
common/src/main/java/world/thearchive/wdl/adapter/ItemLocationScrub.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument; below 1.16 captureItems takes none either.
common/src/test/java/world/thearchive/wdl/adapter/ContainerMergeRoundTripTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument; below 1.16 captureItems and the codec take
# none either.
common/src/test/java/world/thearchive/wdl/adapter/ContainerRecaptureSynergyTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument; below 1.16 captureItems takes none either.
common/src/test/java/world/thearchive/wdl/adapter/ContainerStashMergeTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument; below 1.16 captureItems takes none either.
common/src/test/java/world/thearchive/wdl/adapter/EntityContainerStashMergeTest.java

# Below 1.20.5 there is no ItemCost, so a merchant offer's buy cost is a plain ItemStack, and MerchantOffers has
# no CODEC, so the offers holder is built via MerchantOffers.createTag rather than encoding through RegistryOps.
# Below 1.16 the entity UUID is matched through CompoundTag.getUUID (the UUIDMost/UUIDLeast pair, no
# SerializableUUID) and TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/EntityMergeTest.java

# Below 1.20.5 there are no item components: a placed shulker's contents and a placed beehive's occupants live
# under the item's tag.BlockEntityTag (Items / Bees) rather than DataComponents.CONTAINER/BEES, and
# InteractionCapture.captureRecordItem/captureBees take no registries argument. Below 1.19 there is no chiseled
# bookshelf, so the bookshelf occupancy and insert tests are dropped. Below 1.16 the capture is built without
# registries and the ground-truth edit is a diamond (no netherite ingot before the Nether Update). Java 8 form: no
# Optional.isEmpty or List.of.
common/src/test/java/world/thearchive/wdl/adapter/InteractionStashMergeTest.java

# Below 1.20.5 there are no item components: the lodestone target is the raw LodestonePos/LodestoneDimension/
# LodestoneTracked keys (hand-written on a plain compass, the lodestone compass being a 1.16 addition), a beehive
# item's occupants and the hive's own flower position are tag.BlockEntityTag.Bees[].EntityData.FlowerPos and
# tag.BlockEntityTag.FlowerPos, shulker contents live under tag.BlockEntityTag.Items, and
# ContainerHelper.loadAllItems takes no registries argument. Below 1.17 there is no bundle item, so the
# bundle-holding fixtures and their assertions are dropped, and no Tag.TAG_COMPOUND, so the list reads pass the raw
# type id 10. Below 1.16 there is no com.mojang.serialization, so the stacks round-trip through ItemStack#save and
# ItemStack.of, the lodestone dimension is DimensionType.getName's string, and captureItems takes no registries.
# Java 8 form: no Optional.isEmpty (the empty assertions negate isPresent) and no pattern instanceof.
common/src/test/java/world/thearchive/wdl/adapter/ItemLocationScrubTest.java

# Below 1.20.5 an item is {id, Count, tag}; the walk drives hand-built NBT keyed by tag.BlockEntityTag.Items (a
# shulker) and tag.Items (a bundle) rather than the component keys, and there is no sulfur cube to recurse into.
common/src/test/java/world/thearchive/wdl/adapter/ItemTreeWalkTest.java

# Below 1.20.5 there is no WrittenBookContent component; a written book's title/author/generation/resolved and its
# pages live directly on the item's tag, so the fixture and the read-back build/read that shape; below 1.16
# captureBook and the codec take no registries.
common/src/test/java/world/thearchive/wdl/adapter/LecternRecaptureSynergyTest.java

# Below 1.20.5 there are no WrittenBookContent/WritableBookContent components; a written book's
# title/author/generation/resolved and its pages (JSON-component strings) live on the item's tag, an unsigned
# book's pages are plain strings, and the read-back is vanilla's own ItemStack.of rather than ItemStack.CODEC;
# below 1.16 captureBook takes no registries.
common/src/test/java/world/thearchive/wdl/adapter/LecternSinkRoundTripTest.java

# Below 1.20.5 there is no WrittenBookContent component; a written book's title/author/generation/resolved and its
# pages live directly on the item's tag, so the fixture and the read-back build/read that shape; below 1.16
# captureBook takes no registries.
common/src/test/java/world/thearchive/wdl/adapter/LecternStashMergeTest.java

# Below 1.17 MapItemSavedData has no createForClient or static load factory and no locked() snapshot: the client map
# is the public MapItemSavedData(String) instance with its public scale, dimension and locked fields set and is read
# back through the instance load, and two tests pin the serialize of a null-dimension map, the shape a
# version-bridging proxy relays. Below 1.16 that dimension field is a DimensionType (no ResourceKey) and
# serializeMap takes no registries.
common/src/test/java/world/thearchive/wdl/adapter/MapSinkRoundTripTest.java

# Below 1.17 there is no bundle item, so the in-place bundle-mutation test dev carries is dropped; below 1.16
# TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/MenuChangeTrackerTest.java

# Below 1.20.5 there are no item components: the lodestone target is the raw LodestonePos/LodestoneDimension/
# LodestoneTracked keys (hand-written, the lodestone compass being a 1.16 addition), a filled map's id is the raw
# int tag."map", and there is no ItemCost, so a merchant offer's buy cost is a plain ItemStack. The sell item's
# fields are read off its tag rather than its components map, and below 1.16 the lodestone dimension is
# DimensionType.getName's string.
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferCaptureTest.java

# Below 1.20.5 there is no ItemCost, so a merchant offer's buy cost is a plain ItemStack, and MerchantOffers has
# no CODEC, so the offers holder is built via MerchantOffers.createTag rather than encoding through RegistryOps;
# below 1.16 TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/MerchantStashMergeTest.java

# Below 1.20.5 a beehive block entity's occupants key is still Bees (capitalized), the pre-lowercasing form, so the
# beehive fixture goes through the unchecked malformedChunkTagWith instead of the fidelity-checked chunkTagWith; the
# jukebox fixture carries IsPlaying, RecordStartTick and TickCount beside RecordItem (keys vanilla writes by 1.19.4,
# 1.15.2's JukeboxBlockEntity writing RecordItem alone); below 1.19 no block hosts the chiseled bookshelf, so its
# fixture is unhosted. Below 1.17 (era E3) the scanned entity chunk wraps its carrier under Level.Entities, and
# below 1.16 the scan is keyed by DimensionType.
common/src/test/java/world/thearchive/wdl/adapter/RecoveredScanTest.java

# Below 1.20.5 only a BaseContainerBlockEntity carries an arbitrary custom name, written as the raw CustomName
# JSON key rather than the minecraft:custom_name component, and a beehive's occupants are the pre-component
# {EntityData, TicksInHive, MinOccupationTicks} entries under Bees rather than an Occupant.LIST_CODEC encode. Below
# 1.18 the chunk tag wraps its block entities under Level.TileEntities rather than the flat root block_entities.
# Below 1.16 captureItems takes no registries.
common/src/test/java/world/thearchive/wdl/testsupport/BlockEntityFixtures.java

# Below 1.20.5 an item's count is the raw Count byte (not a components-map int), so a hand-built entry without it
# is silently dropped by ContainerHelper.saveAllItems rather than round-tripping with a single named key missing.
# Below 1.17 there is no Tag.TAG_COMPOUND, so the list read passes the raw type id 10; below 1.16 TestRegistries
# only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelityTest.java

# Below 1.20.5 there are no item components: a filled map's id is the raw int tag."map", and a shulker's nested
# contents live under tag.BlockEntityTag.Items. Below 1.17 there is no bundle item, so the bundle-holding fixture is
# dropped; below 1.16 the holder builders take no registries (no RegistryAccess).
common/src/test/java/world/thearchive/wdl/testsupport/MapHolderFixtures.java

# Below 1.20.3 there is no Crafter block, block entity or menu (the Bats and Pots update added them, experimental),
# so the crafter open-bind leg, the crafter stash and the crafter menu-only ContainerData are dropped; the
# core CrafterSlots and ContainerAssociation.openCrafter stay as unused-but-compiling code. Below 1.20 Entity.level
# is a public field rather than the level() accessor, so the spectator-reach guard reads player.level directly.
# Below 1.17 Player.inventory is a public field (no getInventory()) and Slot has no getContainerSlot, so the chest-slot
# walks count their own index. Below 1.16 there is no RegistryAccess, so the capture holds none and captureItems
# takes none. Java 8 form: no records or pattern instanceof.
common/src/main/java/world/thearchive/wdl/adapter/ContainerCapture.java

# 1.15.2's NbtIo.readCompressed and writeCompressed take only a stream (the File overloads present at 1.16.5 are
# absent at 1.16 as well), so the compressed write and read go through Files.newOutputStream and newInputStream; the
# OutputStream write overload the id-counts staging uses is unchanged.
common/src/main/java/world/thearchive/wdl/adapter/MapDataWriter.java

# Below 1.20.3 NbtIo.readCompressed takes an InputStream with no NbtAccounter argument.
common/src/main/java/world/thearchive/wdl/adapter/StashHolder.java

# Below 1.16 there is no MutableComponent, TextColor or CommonComponents: a template color is set in place through
# Style.setColor as the nearest ChatFormatting, the proceed and cancel labels are TranslatableComponents by key, and
# ConfirmScreen takes its two button labels as Strings.
common/src/main/java/world/thearchive/wdl/client/ResumeConfirm.java

# Below 1.20.3 there is no Crafter menu or block entity, so the crafter capture tests are dropped; below 1.16
# TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/ContainerCaptureTest.java

# Below 1.20.3 there is no Crafter block entity, so this flush-plan test builds its state-bearing block-entity
# fixtures on the brewing stand (BrewTime/Fuel) rather than the crafter; below 1.16 the sink doubles and the
# fixtures take no registries.
common/src/test/java/world/thearchive/wdl/adapter/ChunkFlushPlanTest.java

# Below 1.16 the captured player carries a DimensionType, the RootVehicle Attach UUID is read back through
# CompoundTag.getUUID, the UUIDMost/UUIDLeast pair (no SerializableUUID), buildLevelData takes no registries, the
# write goes through the LevelStorage that LevelStorageSource(Path, Path, DataFixer).selectLevel returns, and
# NbtIo.readCompressed takes only a stream (the File overload is 1.16.5's).
common/src/test/java/world/thearchive/wdl/adapter/LevelDatPlayerRoundTripTest.java

# 1.15.2's NbtIo.readCompressed takes only a stream (the File overload present at 1.16.5 is absent at 1.16 as
# well), so this round trip reads the written files through Files.newInputStream.
common/src/test/java/world/thearchive/wdl/adapter/MapDataWriterRoundTripTest.java

# 1.15.2's NbtIo.readCompressed takes only a stream (the File overload present at 1.16.5 is absent at 1.16 as
# well), so this write-task test reads the file through Files.newInputStream; TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/MapWriteTaskTest.java

# ModMenu 1.10.7, the API 1.15.2 pins, ships no update-checker API (UpdateChecker/UpdateInfo/UpdateChannel arrived
# in a later ModMenu) and its ModMenuApi and ConfigScreenFactory live in io.github.prospector.modmenu.api (the
# com.terraformersmc package is 1.16.23's), so this entrypoint implements only the config-screen factory and does
# not override getUpdateChecker.
fabric/src/main/java/world/thearchive/wdl/fabric/WdlFabricModMenu.java

# ModMenu 1.10.7, the API 1.15.2 pins, ships no update-checker API, so the ModMenu update-badge feeder is deleted,
# not merely divergent; the launch update check still runs, only its ModMenu badge is gone.
fabric/src/main/java/world/thearchive/wdl/fabric/WdlModMenuUpdateChecker.java

# Below 1.20.2 there is no AdvancementHolder/AdvancementNode split (the advancement datapack rewrite is 1.20.2),
# so the harvesting ClientAdvancements.Listener works in Advancement and reads the id via Advancement.getId().
common/src/main/java/world/thearchive/wdl/adapter/AdvancementSnapshot.java

# Below 1.20.2 there is no configuration phase, so the connection tee does not observe
# ClientboundStartConfigurationPacket; the backend-transfer stop machinery (signalTransfer / consumeTransferSignal)
# stays for the controller wiring but is never raised below 1.20.2. The tee declares moveDeltaX/Y/Z for the loader
# plugs to read off the widened protected fields of the move packet and hands the three deltas to the capture, and
# below 1.17 there is no slf4j, so the logger is log4j2's. Java 8 form: no pattern instanceof.
common/src/main/java/world/thearchive/wdl/adapter/ConnectionTee.java

# Below 1.20.2 there is no AdvancementRequirements, so the fixture drives the two-argument
# AdvancementProgress.update(Map<String, Criterion>, String[][]) with a no-argument Criterion (update reads only
# the map keys); below 1.16 TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/PlayerProgressSerializerTest.java

# The Forge island's manifest carries loaderVersion [31,), Forge 31.x for 1.15.2 (31.2.62 pinned), versus the [36,) of
# the 1.16.5 mint parent; dev ships no forge/ tree at all.
forge/src/main/resources/META-INF/mods.toml

# Absent from dev, which ships no forge/ tree: the island's resource pack_format is 5 for 1.15.2, versus the parent's
# 6 for 1.16.5.
forge/src/main/resources/pack.mcmeta

# Below the 1.19 registry split the code-defined registries are static fields on Registry (Registry.ENTITY_TYPE),
# not the net.minecraft.core.registries.BuiltInRegistries added at 1.19.3, so this open-click type check resolves the
# entity id through Registry.ENTITY_TYPE.
common/src/main/java/world/thearchive/wdl/adapter/OpenClickTracker.java

# Below 1.16 there is no ResourceKey: the dimension key is the DimensionType itself, so forType maps the built-in
# NETHER and THE_END values to themselves and everything else to OVERWORLD, forId names them through
# DimensionType.getName, and the blending decision is keyed by DimensionType. At 1.19 and above forType takes the
# ResourceKey the client resolves against the synced registry.
common/src/main/java/world/thearchive/wdl/adapter/VanillaDimensions.java

# Below 1.19 there is no chiseled bookshelf, so the per-chunk bookshelf-occupancy producer is dropped and the
# read-merge composer passes an empty occupancy map (ChunkMerge.occupancyMap); the ChunkMerge per-slot union stays as
# unused-but-compiling code, the way the crafter open-bind stays below 1.20.3.
common/src/main/java/world/thearchive/wdl/adapter/ChunkFlushPlan.java

# Below 1.16 the entity read-merge keys the captured-entity UUID through CompoundTag.getUUID, the UUIDMost/UUIDLeast
# long pair (SerializableUUID's four-int codec is a 1.16 addition, renamed UUIDUtil at 1.19). Below 1.17 (era E3)
# the merge folds into the in-chunk Level.Entities rather than a separate entities/ region (its {Entities:[...]}
# envelope in/out is band-agnostic, so only the Javadoc naming the target diverges; the merge logic is unchanged and
# is reused by foldEntitiesIntoRegion).
common/src/main/java/world/thearchive/wdl/adapter/EntityMerge.java

# XaeroPlus publishes no 1.15.x build for either loader (its builds skip 1.13 through 1.18), so the coverage overlay's
# XaeroPlus binding is dropped as a disclosed limit: Wdl.initialize no longer constructs the XaeroPlus integration, and
# the compat/xaeroplus package is deleted. The JourneyMap overlay covers the same role where its API resolves. Below
# 1.16 there is no LevelResource, so the singleplayer save root is the storage source's base directory resolved
# against the level id. Wdl also carries onClientDisconnect, the entry the MinecraftDisconnectMixin calls, and a
# VersionAdapter fallback that reads the META-INF/services provider file directly when ServiceLoader finds none, the
# Forge 31 island resolving none through it (carried by 1.10.2 through 1.15.2). Java 8 form: no switch expressions
# (Java 14), ServiceLoader.findFirst (Java 9), String.isBlank (Java 11) or List.of.
common/src/main/java/world/thearchive/wdl/Wdl.java

# Deleted with the rest of the XaeroPlus binding (XaeroPlus ships no 1.15.x build for either loader): the plugin
# binding that reads the coverage index into XaeroPlus.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/XaeroPlusBinding.java

# Deleted with the rest of the XaeroPlus binding: the launch-time integration hook Wdl.initialize called.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/XaeroPlusIntegration.java

# Deleted with the rest of the XaeroPlus binding: the package's @NullMarked marker.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/package-info.java

# Deleted: ChiseledBookShelfBlock does not exist below 1.19, so the chiseled-bookshelf slot helper is dropped; the
# MC-free core bookshelf helpers (CapturedContainers, OutlineClassifier.classifyBookshelf) stay byte-identical.
common/src/main/java/world/thearchive/wdl/adapter/BookshelfSlots.java

# The view-layer literal inventory is band-specific: below 1.19 the GUI writes user-facing strings through the
# new TextComponent(...) constructor rather than Component.literal, and the acknowledged-literals map reflects
# 1.15.2's set (two literals in WdlDownloadsScreen and three on the shared bridge, against 1.16.5's four and four).
# The loader-root loop is skip-if-absent for a loader root rather than dev's hard assertTrue, since 1.15.2 ships only
# some of the loader trees; common's own root stays required and scanned. Java 8 form: no List.of/Map.of, Path.of,
# Files.readString or Stream.toList.
common/src/test/java/world/thearchive/wdl/UiLiteralEnrollmentTest.java

# Deleted: the chiseled-bookshelf slot-mask helper it tests (BookshelfSlots) is dropped because ChiseledBookShelfBlock
# does not exist below 1.19.
common/src/test/java/world/thearchive/wdl/adapter/BookshelfSlotsTest.java

# Below 1.19 the ClientboundAddEntityPacket constructor takes ten arguments ending at the packet-visible Vec3 velocity,
# without the trailing double the 1.19 constructor added, so the synthetic add-entity packets are built with the
# ten-argument form. The relative move hands the capture its three short deltas alongside the packet, the seam the
# loader tees feed below 1.17; below 1.16 TestRegistries only runs the bootstrap. Java 8 form: no Set.of.
common/src/test/java/world/thearchive/wdl/adapter/EntityPacketCaptureTest.java

# Below 1.16 TestRegistries only runs the bootstrap (no RegistryAccess). Java 8 form: no Set.of or List.of (Java 9),
# so the expected chunk sets and marker lists are Guava ImmutableSet.of and ImmutableList.of.
common/src/test/java/world/thearchive/wdl/adapter/EntityBufferTest.java

# Two of the nine band-neutral curated game-rule specs have no rule at 1.15.2 (doWardenSpawning arrived at 1.19 and
# doVinesSpread by 1.19.4), so the runtime filter surfaces seven and the layout skips their order slots alongside
# the band-neutral fire-spread row; below 1.16 TestRegistries only runs the bootstrap. Java 8 form: no Set.of.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatCuratedGameRulesTest.java

# Below 1.16 the dimension key is the DimensionType itself, so these assertions feed forType the built-in
# DimensionType values and expect them back (the caves-variant case is 1.16's, there being no second overworld
# type), forId is driven with DimensionType.getName strings, and the blending gate takes DimensionType.OVERWORLD.
common/src/test/java/world/thearchive/wdl/adapter/VanillaDimensionsTest.java

# Below 1.19 the Forge client-registration APIs differ: the config screen is an ExtensionPoint.CONFIGGUIFACTORY
# BiFunction at Forge 31, not the ConfigGuiHandler.ConfigGuiFactory record added at 1.17 nor the ConfigScreenHandler
# added at 1.19; keys register through ClientRegistry.registerKeyBinding (fml.client.registry at Forge 31) during
# FMLClientSetupEvent rather than the RegisterKeyMappingsEvent added at 1.19, deferred through
# DeferredWorkQueue.runLater since the setup event has no enqueueWork at Forge 31 (ParallelDispatchEvent.enqueueWork
# is Forge 1.16.x's); the HUD registers through RenderGameOverlayEvent.Post on the game bus, since OverlayRegistry is
# a 1.17 addition and RegisterGuiOverlaysEvent a 1.19 one, and at Forge 31 that event carries no getMatrixStack (a
# 1.16.x addition, the HUD render taking no PoseStack below 1.16); the login event is
# ClientPlayerNetworkEvent.LoggedInEvent (getNetworkManager) rather than LoggingIn; PlayerInteractEvent exposes
# getPlayer/getWorld rather than getEntity/getLevel, and at Forge 31 RightClickBlock has no getHitVec (Forge
# 1.16.x's), so the block hit is rebuilt from the event's position and face.
forge/src/main/java/world/thearchive/wdl/forge/WdlForge.java

# Below 1.19 keys register through ClientRegistry.registerKeyBinding (fml.client.registry at Forge 31) during
# FMLClientSetupEvent rather than the RegisterKeyMappingsEvent added at 1.19, deferred through
# DeferredWorkQueue.runLater since the setup event has no enqueueWork at Forge 31; the screen-init event is
# GuiScreenEvent.InitGuiEvent.Post (getGui/addWidget) rather than the 1.18.2 ScreenEvent.InitScreenEvent.Post, the
# login and logout events are ClientPlayerNetworkEvent.LoggedInEvent and LoggedOutEvent, and the /wdl client command
# is a no-op because Forge 31 has no client-command event (RegisterClientCommandsEvent is a 1.18.2 addition).
forge/src/main/java/world/thearchive/wdl/forge/ForgePlatformBridge.java

# Below 1.19 the pre-download warm gate warms the void default alongside DEFAULT and FLAT (at 1.19 and above only
# DEFAULT and FLAT warm); at 1.15.2 the warm is inert, the level-data writer's warmWorldgen being empty since a
# pre-1.16 level.dat stores only a generator name and its options and reconstructs no worldgen.
common/src/main/java/world/thearchive/wdl/WorldgenWarmup.java

# Below 1.19 the screen-open warm submits for VOID too, so this test asserts VOID warms, where at 1.19 and above the
# void default does not warm.
common/src/test/java/world/thearchive/wdl/WorldgenWarmupTest.java

# Below 1.18 a chunk's block entities are nested under Level.TileEntities, not the flat root block_entities added at
# 1.18, so the carry-forward read navigates the Level compound first.
common/src/main/java/world/thearchive/wdl/adapter/ChunkMerge.java

# Below 1.18 the recovered-container scan reads block entities from Level.TileEntities, not the flat root
# block_entities. Below 1.17 (era E3) the recovered-entity scan (recordEntities) likewise reads the in-chunk
# Level.Entities, the sibling of that Level.TileEntities, rather than a separate entities/ region chunk's top-level
# Entities. Below 1.16 the scan is keyed by DimensionType (no ResourceKey). Java 8 form: no pattern instanceof.
common/src/main/java/world/thearchive/wdl/adapter/RecoveredScan.java

# com.mojang.logging.LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is
# acquired through log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketAccumulator.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/adapter/PlayerProgressWriter.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/client/WdlOutlineRenderer.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/compat/bobby/BobbyChunkFilter.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/compat/flashback/FlashbackReplayProbe.java

# JsonParser.parseString is a gson 2.8.6+ static, absent from the gson 2.8.0 bundled with 1.15.2, so the release index
# parses through the instance form (new JsonParser().parse).
common/src/main/java/world/thearchive/wdl/update/ReleaseIndex.java

# The Java 8 floor has no java.net.http HTTP client (that is Java 11) nor its Flow-based reactive body subscribers, so
# the update transport is rewritten off HttpClient/HttpResponse.BodySubscriber onto a java.net.HttpURLConnection with a
# manual bounded read.
common/src/main/java/world/thearchive/wdl/update/HttpTransport.java

# Below 1.16 there is no RegistryAccess (RegistryAccess.Frozen and builtinCopy().freeze() are 1.18's), so
# TestRegistries only runs the bootstrap and the fixtures and the encode take no registries.
common/src/test/java/world/thearchive/wdl/adapter/ChunkReencodeFidelityTest.java

# Below 1.16 there is no RegistryAccess (RegistryAccess.Frozen is 1.18's), so the session is built with DimensionType
# keys and no registries, and the writer takes no session-lock argument.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionMapTallyTest.java

# Below 1.16 there is no RegistryAccess (RegistryAccess.Frozen is 1.18's), so the session is built with DimensionType
# keys and no registries and captureItems takes none.
common/src/test/java/world/thearchive/wdl/adapter/RiddenMountContentsFoldTest.java

# Below 1.18 the merge test reads chunk block entities from Level.TileEntities, not the flat root block_entities;
# below 1.16 captureItems takes no registries (TestRegistries only runs the bootstrap).
common/src/test/java/world/thearchive/wdl/adapter/ContainerTypeGateMergeTest.java

# The fastutil bundled with 1.15.2 (8.2.1) has no LongSet.of() factory, so the test builds sets via LongOpenHashSet;
# Java 8 form: no Set.of collection factory (Java 9), so the entity sets are Guava ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/CaptureControllerTest.java

# The fastutil bundled with 1.15.2 (8.2.1) has no LongSet.of() factory, so the test builds sets via LongSets and
# LongOpenHashSet; Java 8 form: no Set.of collection factory (Java 9), so the entity sets are Guava ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/CapturedContainersTest.java

# The fastutil bundled with 1.15.2 (8.2.1) has no LongSet.of() factory, so the test builds sets via LongSets and
# LongOpenHashSet; Java 8 form: no Set.of collection factory (Java 9), so the entity sets are Guava ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/OutlineClassifierTest.java

# The fastutil bundled with 1.15.2 (8.2.1) has no LongSet.of() factory, so the test builds sets via LongSets and
# LongOpenHashSet; Java 8 form: no Set.of collection factory (Java 9), so the entity sets are Guava ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/RecoveredCoverageTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected lists are Guava
# ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/ChatCopyTest.java

# Below 1.17 the Java floor is 8, which has no Set.of collection factory (Java 9), so the moved-keys set is a Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/ConfigSchemaTest.java

# Below 1.17 the Java floor is 8, where Optional has no isEmpty() (Java 11), so the crafter-drop assertions negate
# isPresent() instead.
common/src/test/java/world/thearchive/wdl/core/ContainerAssociationTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected drain order is a
# Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/ReadyLatchTest.java

# Below 1.17 the Java floor is 8, which has no List.of/Set.of collection factories (Java 9), so the derived-prefix
# list and the empty undeclared-key set use Guava ImmutableList.of/ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/SettingsLangTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected tab-id list is a
# Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/SettingsLayoutTest.java

# Below 1.17 the Java floor is 8, which has no List.of/Map.of collection factories (Java 9), so the expected lists use
# Guava ImmutableList.of and the seven-entry refusal map uses ImmutableMap.builder (past ImmutableMap.of's five-pair
# limit).
common/src/test/java/world/thearchive/wdl/core/ToastCopyTest.java

# Below 1.17 the Java floor is 8: no Files.readString/writeString (Java 11), no records (Java 16), no List.of (Java
# 9), so the config reads/writes go through readAllBytes/write with StandardCharsets.UTF_8, DefaultOnBoolean is a
# final class with explicit accessors, and the toggle list is a Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/WdlConfigTest.java

# Below 1.17 the Java floor is 8, which has no no-argument Optional.orElseThrow (Java 10) nor List.of (Java 9), so the
# entry lookup uses Optional.get and the empty SaveChunks lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/browse/DownloadCatalogTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected managed-folder
# list is a Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/browse/DownloadFoldersTest.java

# Below 1.17 the Java floor is 8, where there is no Path.of factory (Java 11), so the save path is built with Paths.get.
common/src/test/java/world/thearchive/wdl/core/browse/SinglePlayerTaintTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected surviving-paths
# lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/export/RestoreOperationTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected
# moved-back/relocated/deferred lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/export/RestoreSweepTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the DownloadCounts/SaveChunks
# and render-input lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/report/DownloadReportFormatterTest.java

# Below 1.17 the Java floor is 8: no List.of (Java 9) nor Files.readString/writeString (Java 11), so the SaveChunks
# lists are Guava ImmutableList.of and the corrupt-timestamp round trip reads/writes via readAllBytes/write with
# StandardCharsets.UTF_8.
common/src/test/java/world/thearchive/wdl/core/report/DownloadReportLogTest.java

# Below 1.17 the Java floor is 8, which has no no-argument Optional.orElseThrow (Java 10) nor List.of (Java 9), so
# SemVer.parse is unwrapped with Optional.get and the release/game-version lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/update/LatestReleaseTest.java

# Below Forge 40 the world-render hook is RenderWorldLastEvent (no render stage, no frustum), not the
# RenderLevelStageEvent added at 1.18.2; the outline draws on it and uses the shared renderer's null-frustum path.
forge/src/main/java/world/thearchive/wdl/forge/ForgeOutlineRegistrar.java

# Absent from dev and carried by 1.10.2 through 1.17.1: a 26.x server reached through a downgrade proxy sends a 1.15.2
# client sections outside the 0..256 column, and this test pins that the codec drops them so the save loads; the
# scenario and the clamp are below-1.18-only. Below 1.17 there is no Tag.TAG_COMPOUND; below 1.16 the encode takes
# no registries. Java 8 form: no records, List.of, Set.of or Map.of.
common/src/test/java/world/thearchive/wdl/adapter/ChunkSectionClampTest.java

# Below 1.18 a chunk's block entities nest under Level.TileEntities, not the flat root block_entities added at 1.18,
# so the in-session stash merge locates a block entity by x/y/z through the Level compound.
common/src/main/java/world/thearchive/wdl/adapter/ContainerMerge.java

# Absent from dev and carried by every band from 1.10.2 to 1.20.4.
# Below 1.20.5 a chested mount has no chest inventory of its own: the saddle slot, the armor slot and the chest are
# one SimpleContainer, AbstractChestedHorse writes each stack under its own index in it walking from two, and its
# read refuses any saved Slot below two or at or past that inventory's size. At 1.20.5 the write starts at one and
# stamps the index less one and the read adds it back, turning the saved number chest-relative, which is why no band
# from there up carries this file. So the lift has to offset its menu-order count into the absolute numbering, and
# this gate reloads through vanilla's own entity reader rather than counting what survives, a count assertion passing
# unchanged on shifted contents. The widths it drives are 1.15.2's: getInventorySize returns a flat seventeen for
# a donkey or mule, which its five columns reproduce as two tack slots and fifteen chest slots, and Llama overrides
# that with two plus three times its own strength, clamped between one and five, so a llama chest runs from three
# slots to fifteen. Below 1.17 there is no SlotAccess either, so the fixture puts the chest on through setSlot at
# vanilla's address 499 and takes its container size from that formula rather than asking the mount where its
# inventory ends; Player.inventory is a public field (no getInventory()) and Tag has no TAG_COMPOUND. Below 1.16 the
# ContainerCapture is built without registries.
common/src/test/java/world/thearchive/wdl/adapter/MountChestSlotsTest.java

# Java 8 form (java_version 8): a type test cannot bind a pattern variable (Java 16), so each instanceof over an NBT
# read is a local, an unbound instanceof and a cast rather than the pattern form the bands from 1.17 up use.
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferScrub.java

# Java 8 form (java_version 8): a type test cannot bind a pattern variable (Java 16), so each instanceof over an NBT
# read is an unbound instanceof and a cast; and below 1.17 (era E3) entities live inside the region chunk, so the
# resume scan hands the bank that whole chunk and the record side reads Level.Entities where the bands from 1.17 up
# read a top-level Entities. The apply side is not rewritten with it, taking the flat envelope the entity fold passes.
common/src/main/java/world/thearchive/wdl/adapter/RecoveredEntityContent.java

# Below 1.17 there is no Tag.TAG_COMPOUND constant, so the list read passes the raw compound type id 10; below 1.16
# TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/NbtMergeTest.java

# Below 1.21.5 the CompoundTag and ListTag accessors getCompound and getList return the value, where from 1.21.5 they
# return Optional and dev reads through the defaulting getCompoundOrEmpty and getListOrEmpty, dev's getIntOr becoming a
# local intOr helper because getInt reads an absent key back as a real zero; the merchant fixture builds its offer with
# a plain ItemStack buy cost and serializes through MerchantOffers.createTag, there being no ItemCost or offers codec
# below 1.20.5; below 1.17 there is no Tag.TAG_COMPOUND, so the list read passes the raw type id 10; below 1.16 the
# entity's presence is asserted on its UUIDMost key, the UUIDMost/UUIDLeast pair being the pre-1.16 UUID form, and
# TestRegistries only runs the bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferScrubTest.java

# Below 1.17 (era E3) entities live inside the region chunk, so the scanned-chunk fixture wraps its entities under
# Level.Entities while the fresh-chunk fixture keeps the flat envelope, the two shapes the bank's two sides take; and
# the merchant fixture uses the pre-1.20.5 plain-ItemStack offer and createTag. The later getIntOr, getListOrEmpty,
# getCompoundOrEmpty and getStringOr accessors are spelled in the pre-1.21.5 forms, getIntOr as the local intOr helper
# because getInt reads an absent key back as a real zero. Below 1.16 captureItems takes no registries.
common/src/test/java/world/thearchive/wdl/adapter/RecoveredEntityContentTest.java

# Below 1.17 (era E3) entities live inside the region chunk, so the on-disk oracle reads the host chunk's
# Level.Entities through the vanilla IOWorker rather than a separate entities/ store (no SimpleRegionStorage below
# 1.20.5), and the fixture seeds a host chunk before each download because a fold with no host on disk is a counted
# loss. The merchant fixture builds its offer with a plain ItemStack buy cost and serializes through
# MerchantOffers.createTag (no ItemCost or offers codec below 1.20.5), and the pre-1.21.5 getCompound, getList and
# getString accessors replace dev's getCompoundOrEmpty, getListOrEmpty and getStringOr. Below 1.16 the dimension keys
# are DimensionType values, captureItems takes no registries, and LevelStorageSource has no createDefault, so the
# source is constructed with its saves and backups paths and DataFixers.getDataFixer().
common/src/test/java/world/thearchive/wdl/adapter/EntityResumeContentCarryTest.java

# Below 1.21.9 the keybind category key is key.categories.wdl.downloader, vanilla's key.categories.* shape at
# 1.15.2, so the external-consumer set names that key. The loader-root loop is skip-if-absent for a loader root rather
# than dev's hard assertTrue, since 1.15.2 ships only some of the loader trees; common's own root stays required
# and scanned. Two button tooltips 1.15.2 does not draw (Button having no OnTooltip constructor below 1.16) sit in
# BAND_DROPPED_KEYS, their en_us strings kept so the catalog stays identical across bands. Java 8 form: no
# List.of/Set.of/Map.of (Java 9), Path.of (Java 11) or Stream.toList (Java 16), so the collections are Guava
# (ImmutableMap.builder past the five-pair ImmutableMap.of), the paths Paths.get.
common/src/test/java/world/thearchive/wdl/LangKeyCoverageTest.java

# Below Java 21 the pinned Error Prone release, a Java 21 build, cannot load inside the compile toolchain, so 1.15.2
# gates NullAway behind errorProneUsable and runs annotations-only, where dev runs the checker unconditionally and
# carries no such gate; the floor comment drops the clause comparing 1.15.2 to the deep Java 8 bands, 1.15.2 being
# one of them.
build-logic/src/main/kotlin/wdl.nullness-conventions.gradle.kts

# Below Java 11 Checkstyle cannot run on the java_version toolchain, so this file points the Checkstyle tasks at a
# modern launcher, and PITest at the Java 8 toolchain where mcp_mappings_version marks a classic-MCP band (legacy FML
# cannot static-initialize above Java 8) or at the modern launcher otherwise; dev, a Java 25 band, carries no
# launcher block.
build-logic/src/main/kotlin/wdl.java-conventions.gradle.kts

# The consumer comment names :fabric alone as the remapping loader, 1.15.2 including no :neoforge; the code is dev's.
build-logic/src/main/kotlin/wdl.common-merge.gradle.kts

# This root includes common and fabric: the second loader is Forge, built by the island under forge/, and the
# paragraph above include(...) names why NeoForge is absent for 1.15.x; dev includes common, fabric and neoforge. The
# pattern also credits forge/settings.gradle.kts, the island root that declares the Architectury Loom plugin
# repositories.
settings.gradle.kts

# The apply-false plugin block declares only the catalog-pinned plugins a subproject on 1.15.2 applies, and its
# notes name 1.15.2's appliers, where dev declares the set for common, fabric and neoforge. The pattern also credits
# the subproject scripts: common, fabric and forge/build.gradle.kts pin the 1.x JourneyMap API (journeymap-api
# 1.14.4-1.5-SNAPSHOT from jm.gserv.me) where 1.16.5 pins the 2.0 API from blamejared, fabric/build.gradle.kts
# enables Loom's mixin annotation processor for the wdl.refmap.json the mixins need, and forge/build.gradle.kts is the
# Architectury Loom island build.
build.gradle.kts

# The catalog carries only the plugins some build on 1.15.2 applies, so it has no moddev
# entry (dev's carries loom, moddev and mod-publish-plugin for its fabric and neoforge subprojects); the
# tool-library entries match dev's.
gradle/libs.versions.toml
