# Band-divergence allowlist.
#
# checkBandDivergence diffs the working tree against the branch this band was minted from (mint_parent in
# gradle.properties) and against dev, over every tracked path except gradle.properties, this file and the
# per-band plug (common/src/main/java/world/thearchive/wdl/adapter/impl/). A path that differs from both must
# match a pattern below; a pattern that matches no path differing from dev is stale; core/ main sources may not
# differ from the parent and are never listed here, which along the mint chain keeps them one blob on every band.
# A path equal to the parent's is inherited and a path equal to dev's is not this band's divergence; an inherited
# entry stays live while its file differs from dev and is kept. Each entry records why the file legitimately
# carries band-local code, so neither the gate nor a future reader mistakes its difference for a lost fix or a
# bug. Anchor a reason to a version rather than to "this band", say "deleted" for a file dev has and this band
# does not, and re-read every inherited reason at a mint: the gate checks that a pattern is live, never that
# its reason is true. Patterns match as gitignore(5) matches: the last matching pattern is the one credited and a
# directory pattern covers its subtree, so a pattern nested under a registered directory is never credited and
# reads as stale.
#
# Format: gitignore-style per gitignore(5), one path pattern per line, blank lines and # comments ignored.
# Every pattern carries a # reason directly above it (checkBandDivergence fails on a pattern without one), so
# a shared reason across several files is repeated per line rather than written once over a group.

# 26.1.2 keeps the current screen on Minecraft.screen and opens one through Minecraft.setScreen, and reads the camera
# through GameRenderer.getMainCamera; 26.2 moved the screen into Minecraft.gui (Gui.screen and Gui.setScreen) and
# renamed the camera accessor mainCamera.
common/src/main/java/world/thearchive/wdl/Wdl.java
# 26.1.2 opens and closes its confirm screens through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under
# Minecraft.gui.
common/src/main/java/world/thearchive/wdl/ResumeFlow.java
# 26.1.2 opens the resume confirms and returns to the parent through Minecraft.setScreen; 26.2 moved it to
# Gui.setScreen under Minecraft.gui.
common/src/main/java/world/thearchive/wdl/client/WdlDownloadsScreen.java
# 26.1.2 opens its confirm screens through Minecraft.setScreen and tests a tooltip key with I18n.exists; 26.2 moved
# setScreen into Minecraft.gui and dropped I18n.exists, so it asks Language.getInstance().has.
common/src/main/java/world/thearchive/wdl/client/WdlSettingsScreen.java

# 26.1.2 reads the current screen from Minecraft.screen, the hidden-HUD flag from Options.hideGui, the chat from
# Gui.getChat and the toast manager from Minecraft.getToastManager, and builds the toast with SystemToast.multiline;
# 26.2 moved the screen and the manager into Gui, the flag and the chat onto Gui.hud, and dropped multiline.
common/src/main/java/world/thearchive/wdl/platform/AbstractPlatformBridge.java

# 26.1.2 keeps the block entity type constants on BlockEntityType; 26.2 moved them to BlockEntityTypes.
common/src/main/java/world/thearchive/wdl/adapter/InteractionCapture.java
# 26.1.2 keeps the entity type constants on EntityType; 26.2 moved them to EntityTypes.
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketCapture.java

# 26.1.2 draws the outline immediately, so the context carries a PoseStack and a MultiBufferSource; 26.2 removed
# MultiBufferSource and records the draw into the submit-node collector, which hands a Pose and a VertexConsumer.
common/src/main/java/world/thearchive/wdl/adapter/OutlineRenderContext.java
# 26.1.2 renders from a PoseStack and a MultiBufferSource and reads the camera through GameRenderer.getMainCamera;
# 26.2 removed MultiBufferSource (the draw takes the collector's Pose and VertexConsumer) and renamed the accessor
# mainCamera.
common/src/main/java/world/thearchive/wdl/client/WdlOutlineRenderer.java
# 26.1.2 draws on fabric-api's BEFORE_GIZMOS level render event from the context's bufferSource; fabric-api 0.156.0
# for 26.2 dropped bufferSource with MultiBufferSource, so 26.2 submits the outline on COLLECT_SUBMITS through the
# submit-node collector.
fabric/src/main/java/world/thearchive/wdl/fabric/FabricOutlineRegistrar.java
# 26.1.2 draws on RenderLevelStageEvent.AfterTranslucentBlocks into the shared render buffers and flushes the lines
# batch itself; 26.2 removed MultiBufferSource and Minecraft.renderBuffers, so it records the outline on
# SubmitCustomGeometryEvent through the submit-node collector.
neoforge/src/main/java/world/thearchive/wdl/neoforge/NeoForgeOutlineRegistrar.java

# 26.1.2 keeps the block entity type constants on BlockEntityType; 26.2 moved them to BlockEntityTypes.
common/src/test/java/world/thearchive/wdl/adapter/ChunkMergeTest.java
# 26.1.2 keeps the entity type constants on EntityType; 26.2 moved them to EntityTypes.
common/src/test/java/world/thearchive/wdl/adapter/EntityPacketCaptureTest.java
# 26.1.2 keeps the entity type constants on EntityType; 26.2 moved them to EntityTypes.
common/src/test/java/world/thearchive/wdl/adapter/ItemLocationScrubTest.java
# 26.1.2 keeps the entity type constants on EntityType and its Entity draws its id from a static counter, so the
# entity double takes a null level; 26.2 moved the constants to EntityTypes and the id onto Level.getNextEntityId,
# so the double takes HeadlessLevel.
common/src/test/java/world/thearchive/wdl/adapter/CaptureAnchorTest.java
# 26.1.2 keeps the entity type constants on EntityType and its Entity draws its id from a static counter, so the
# entity double takes a null level; 26.2 moved the constants to EntityTypes and the id onto Level.getNextEntityId,
# so the double takes HeadlessLevel.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossTallyTest.java
# 26.1.2 keeps the entity type constants on EntityType and its Entity draws its id from a static counter, so the
# entity double takes a null level; 26.2 moved the constants to EntityTypes and the id onto Level.getNextEntityId,
# so the double takes HeadlessLevel.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkLeashDegradationTest.java
# 26.1.2 keeps the entity type constants on EntityType and its Entity draws its id from a static counter, so the
# entity doubles take a null level; 26.2 moved the constants to EntityTypes and the id onto Level.getNextEntityId,
# so the doubles take HeadlessLevel.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkRootVehicleTest.java
# 26.1.2 keeps the entity type constants on EntityType and its Entity draws its id from a static counter, so the
# doubles take a null level and serve registry access themselves; 26.2 moved the constants to EntityTypes and the
# id onto Level.getNextEntityId, so the doubles take HeadlessLevel.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkItemDegradationTest.java
# 26.1.2 keeps the entity type constants on EntityType, its Entity draws its id from a static counter (the mob
# double takes a null level), and the banner items are per-color constants such as Items.WHITE_BANNER; 26.2 moved
# the constants to EntityTypes, the id onto Level.getNextEntityId, and the banners into the Items.BANNER collection.
common/src/test/java/world/thearchive/wdl/adapter/impl/NaturalEquipmentTest.java

# 26.1.2's fabric-api 0.155.2 client gametest API waits for chunks through TestServerConnection.getClientLevel, a
# TestClientLevelContext the fixture hands out as clientWorld; fabric-api 0.156.0 for 26.2 put waitForChunksDownload
# on the connection itself and types the dedicated one TestDedicatedServerConnection.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/MultiplayerFixture.java
# 26.1.2 waits for chunks through the fixture's clientWorld, the fabric-api 0.155.2 TestClientLevelContext; at 26.2
# fabric-api 0.156.0 put the wait on the connection and the fixture exposes it directly.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlCoverageOverlayResumeTest.java
# 26.1.2 waits for chunks through the fixture's clientWorld, the fabric-api 0.155.2 TestClientLevelContext; at 26.2
# fabric-api 0.156.0 put the wait on the connection and the fixture exposes it directly.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlDisconnectFlushTest.java
# 26.1.2 waits for chunks through the fixture's clientWorld, the fabric-api 0.155.2 TestClientLevelContext; at 26.2
# fabric-api 0.156.0 put the wait on the connection and the fixture exposes it directly.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlFramedMapRevisitUnionTest.java
# 26.1.2 waits for chunks through the fixture's clientWorld, the fabric-api 0.155.2 TestClientLevelContext; at 26.2
# fabric-api 0.156.0 put the wait on the connection and the fixture exposes it directly.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlMapSchemeSignalBeginTest.java
# 26.1.2 waits for chunks through the fixture's clientWorld, the fabric-api 0.155.2 TestClientLevelContext; at 26.2
# fabric-api 0.156.0 put the wait on the connection and the fixture exposes it directly.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlMapStreamCrashStateTest.java
# 26.1.2 waits for chunks through the fixture's clientWorld, the fabric-api 0.155.2 TestClientLevelContext; at 26.2
# fabric-api 0.156.0 put the wait on the connection and the fixture exposes it directly.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlResumeCaptureTest.java

# 26.1.2 reads the current screen from Minecraft.screen; 26.2 moved it to Gui.screen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/ScreenDriver.java
# 26.1.2 reads the current screen from Minecraft.screen; 26.2 moved it to Gui.screen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlPausedSaveCompletionTest.java
# 26.1.2 reads the current screen from Minecraft.screen and dismisses it through Minecraft.setScreen; 26.2 moved both
# into Minecraft.gui (Gui.screen and Gui.setScreen).
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/RestoreFlowTest.java
# 26.1.2 reads the current screen from Minecraft.screen and dismisses it through Minecraft.setScreen; 26.2 moved both
# into Minecraft.gui (Gui.screen and Gui.setScreen).
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlCoverageOverlayRespawnFloodTest.java
# 26.1.2 dismisses the screen through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlCoverageOverlayRemovalArrivalOriginTest.java
# 26.1.2 dismisses the screen through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlCoverageOverlayRemovalSeedOriginTest.java
# 26.1.2 dismisses the screen through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlSneakPlaceClickCascadeTest.java
# 26.1.2 dismisses the screen through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlChestedAnimalCaptureTest.java
# 26.1.2 dismisses the screen through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlEnderChestCaptureTest.java
# 26.1.2 dismisses the screen through Minecraft.setScreen; 26.2 moved it to Gui.setScreen under Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlSpectatorContainerCaptureTest.java

# 26.1.2 keeps the entity type constants on EntityType; 26.2 moved them to EntityTypes.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlHalfFailedCaptureTest.java
# 26.1.2 keeps the entity type constants on EntityType; 26.2 moved them to EntityTypes.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlPreexistingEntityCaptureTest.java
# 26.1.2 keeps the entity type constants on EntityType and dismisses the screen through Minecraft.setScreen; 26.2
# moved the constants to EntityTypes and setScreen into Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlEntityContainerCaptureTest.java
# 26.1.2 keeps the entity type constants on EntityType and dismisses the screen through Minecraft.setScreen; 26.2
# moved the constants to EntityTypes and setScreen into Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlIncapableEntityClickCascadeTest.java
# 26.1.2 keeps the entity type constants on EntityType and dismisses the screen through Minecraft.setScreen; 26.2
# moved the constants to EntityTypes and setScreen into Minecraft.gui.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlMerchantCaptureTest.java
# 26.1.2 reads the camera through GameRenderer.getMainCamera; 26.2 renamed the accessor mainCamera.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/CaptureDriver.java
# 26.1.2 draws the rim on BEFORE_GIZMOS from the context's PoseStack and bufferSource and reads the camera through
# GameRenderer.getMainCamera; 26.2 removed MultiBufferSource, so it submits the rim on COLLECT_SUBMITS through the
# submit-node collector, and renamed the accessor mainCamera.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/WdlOutlineRenderSmokeTest.java

# 26.1.2 reaches the toast manager through Minecraft.getToastManager and a SystemToast's title is one Component; 26.2
# moved the manager to Gui.toastManager and splits the title into titleLines.
fabric/src/gametest/java/world/thearchive/wdl/fabric/gametest/ToastReadback.java

# 26.1.2 carries an extra comment paragraph on the checkCoreJava8 --release 8 floor above the paragraph dev has; dev
# has only that one.
common/build.gradle.kts
