# Band-divergence allowlist.
#
# checkBandDivergence diffs the working tree against the branch this band was minted from (mint_parent in
# gradle.properties) and against dev, over every tracked path except gradle.properties, this file and the
# per-band plug (common/src/main/java/world/thearchive/wdl/adapter/impl/). A path that differs from both must
# match a pattern below; a pattern that matches no path differing from dev is stale; core/ main sources may not
# differ from the parent and are never listed here, which along the mint chain keeps them one blob on every band.
# A path equal to the parent's is inherited and a path equal to dev's is not this band's divergence; an inherited
# entry stays live while its file differs from dev and is kept. Each entry records why the file legitimately
# carries band-local code, so neither the gate nor a future reader mistakes its difference for a lost fix or a
# bug. Anchor a reason to a version rather than to "this band", say "deleted" for a file dev has and this band
# does not, and re-read every inherited reason at a mint: the gate checks that a pattern is live, never that
# its reason is true. Patterns match as gitignore(5) matches: the last matching pattern is the one credited and a
# directory pattern covers its subtree, so a pattern nested under a registered directory is never credited and
# reads as stale.
#
# Format: gitignore-style per gitignore(5), one path pattern per line, blank lines and # comments ignored.
# Every pattern carries a # reason directly above it (checkBandDivergence fails on a pattern without one), so
# a shared reason across several files is repeated per line rather than written once over a group.

# Below 1.16 there is no RegistryAccess, so the test registries are the static Registry tables the vanilla
# Bootstrap.bootStrap populates, run once per JVM and read directly. 1.13.2's SharedConstants has no getCurrentVersion
# (the 1.14.4 accessor the 1.14.4 bootstrap sequence calls first), so the bootstrap runs alone.
common/src/test/java/world/thearchive/wdl/testsupport/TestRegistries.java

# Below the 1.21.2 chunk-serialization cut vanilla has no SerializableChunkData.SectionData record, so this
# chunk-snapshot seam defines its own band-stable SectionData carrier for the static-write codec. Below 1.20.5
# ChunkStatus is in net.minecraft.world.level.chunk, not the .status subpackage added at that update.
common/src/main/java/world/thearchive/wdl/adapter/ChunkSnapshotSource.java

# Reads the captured section through ChunkSnapshotSource.SectionData (no SerializableChunkData.SectionData below
# 1.21.2). Below the 1.21 update a playable disc is a RecordItem instance (no jukebox_playable component) and the
# captured jukebox carries IsPlaying, RecordStartTick and TickCount; below 1.20.5 there are no item components, so the
# disc is written with ItemStack.save and a placed shulker's contents are read from the stack's BlockEntityTag element
# with ContainerHelper.loadAllItems. 1.13.2 has no BlockHitResult (a 1.14.4 class): the use-block hook takes HitResult,
# reading the block through method_9344 and the face through field_10268, and its UseOnContext is the seven-argument
# (player, stack, pos, direction, fractional hit) form. Java 8 has no sealed interfaces (Java 17) or pattern switch
# (Java 21), so Candidate is a plain interface and confirm is an instanceof chain.
common/src/main/java/world/thearchive/wdl/adapter/InteractionCapture.java

# Below the 1.21 update a captured jukebox's play state is the IsPlaying note-particle flag rather than the
# ticks_since_song_started sidecar, so the RecordItem carry-forward field carries IsPlaying. Below 1.20.5 the
# beehive block entity's occupants key is still Bees (capitalized), the pre-lowercasing form, so this field
# carries that key rather than bees.
common/src/main/java/world/thearchive/wdl/adapter/CapturedBlockField.java

# Below 1.21.2 the entity position wire is the absolute-only ClientboundTeleportEntityPacket (no
# ClientboundEntityPositionSyncPacket) with byte-encoded rotations, decoded by the capture; below 1.20.2 the respawn
# and login
# packets carry no CommonPlayerSpawnInfo, so the entered dimension is read through their getDimension. Below 1.19 the
# spawn is split across the AddEntity, AddMob, AddPainting and AddExperienceOrb packets (folded into AddEntity at 1.19
# and 1.21.5), so the reconstruct dispatches on the concrete packet, and there is no Entity.recreateFromPacket (1.17),
# so a hanging entity is built through its positioned constructor. 1.13.2's ClientboundAddEntityPacket carries an int
# object-type id (method_7626) where 1.14.4's carries the EntityType, so the ids of the client's handleAddEntity switch
# are mapped to types; its EntityType has no chunkRange, so only the decorations feed the range estimator and the
# range-10 exclusion list is dropped; Entity has no position(), so the base is read off the x, y and z fields; and the
# Pos and PosRot move packets are unnamed in the bridge mapping (class_2028, class_2029).
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketCapture.java

# The Java 8 floor has no records, so this MC-free packet-position carrier, a record from 1.17.1 up, is a
# final class with explicit fields and accessors.
common/src/main/java/world/thearchive/wdl/adapter/EntityPos.java

# Reads ChunkSnapshotSource.SectionData (no SerializableChunkData.SectionData below 1.21.2). Below the 1.21 update
# leash state lives on Mob rather than Leashable, so a saved leash is rebound via Mob.setLeashedTo. Below 1.20.5 there
# is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer is opened through WorldPaths.openRegionStorage,
# the mod's own region store; below 1.20.3 there is no Crafter menu, so the crafter legs are dropped, and
# NbtIo.readCompressed takes no NbtAccounter; below 1.20.2 the server brand is LocalPlayer.getServerBrand; below 1.20
# Entity.level is a public field, so the camera-anchor guard reads camera.level. Below 1.17 entities live inside the
# region chunk, so the writer is built with the region opener alone. Below 1.16 ServerData.motd is a plain String,
# null-guarded inline. 1.13.2 has no lectern (LecternMenu is 1.14), no AbstractVillager (the merchant is a Villager),
# no MerchantMenu.getOffers (the offers are read from the MerchantScreen's merchant, class_1145 via method_1160 and
# method_3545), no LevelChunk.isUnsaved (the dirty poll is the should-save query method_3893(false) and the re-arm
# stamps setLastSaveTime, bounding the residual to one re-encode per entity-bearing chunk per 600 ticks), no
# SectionPos, BlockPos.of (method_10488), MapItem.makeKey (the map_ prefix and MapItem.method_16115),
# SharedConstants.getCurrentVersion (the DataVersion is the literal 1631), LevelStorageSource.getBaseDir (the saves
# directory is passed in) or Executor on Minecraft (method_6635); the chunk lookup is ChunkSource.method_17044 with
# ChunkStatus.field_18865 set, and entitiesForRendering is the field_4541 list. encodeQuiesced is not volatile: with
# no Fabric tree, every path that takes or reads the hold is the client thread.
common/src/main/java/world/thearchive/wdl/adapter/LiveCaptureSession.java

# Absent from dev: 1.13.2's LevelChunk has no isUnsaved reader (1.14.4 has one, which 1.14.4's test reads alongside
# setLastSaveHadEntities), so the dirty poll uses the should-save query method_3893(false), whose entity arm compares
# the game time against a last save time nothing writes on a client. This pins that LiveCaptureSession.rearmUnsaved's
# last-save-time stamp is what quiets that arm, that the arm returns once at 600 ticks, and that the chunk is built
# through 1.13.2's LevelChunk(Level, int, int, Biome[]).
common/src/test/java/world/thearchive/wdl/adapter/RecaptureRearmTest.java

# Below 1.21.2 the toast host is Minecraft.getToasts (ToastComponent), not getToastManager; below 1.20.3 SystemToast
# has no custom-id class, so the two toast streams reuse two vanilla SystemToastIds. 1.13.2's Button and AbstractWidget
# are the unnamed class_356 (x, y, active and hovered as field_1051, field_1052, field_1055 and field_5056, the click
# as method_18374), so the pause row is built from it and the settings button draws its hover label from
# renderButton, Button gaining a tooltip parameter only at 1.16. isConnectionClosed reads the play connection's
# Connection.isConnected the way MultiPlayerGameMode does, beside isRemoteWorld; the saves directory is
# gameDirectory/saves, 1.13.2's LevelStorageSource having no getBaseDir (1.14.4).
common/src/main/java/world/thearchive/wdl/platform/AbstractPlatformBridge.java

# The 1.13.2 loader detects a disconnect on a client-tick edge, one tick past the player that the level-install method
# (Minecraft.method_18206) nulls, so the bridge SPI carries a second edge and a predicate dev's has no equivalent of:
# onLevelTeardown, fired from the loader's own world-unload signal while the local player is still live, and
# isConnectionClosed, which tells a subscriber which kind of teardown it is being called for.
common/src/main/java/world/thearchive/wdl/platform/PlatformBridge.java

# Pins the pause-menu row placement against 1.13.2's unnamed class_356 buttons over the 200-wide column of the 1.13.2
# pause screen (1.14.4's Button column is 204 wide).
common/src/test/java/world/thearchive/wdl/platform/AbstractPlatformBridgeTest.java

# Absent from dev, whose SystemToast wraps the body itself (multiline from 1.16, the constructor at 26.2), so this
# toast wraps its own body and stitches the frame taller; below 1.15 there is no RenderSystem, so the tint is
# GlStateManager.method_9825, and the blit inherited from class_372 is method_992, both unnamed in the bridge.
common/src/main/java/world/thearchive/wdl/platform/WdlMultilineToast.java

# The chunk fixtures build ChunkSnapshotSource.SectionData (no SerializableChunkData.SectionData below 1.21.2) and
# below 1.20.5 name ChunkStatus in net.minecraft.world.level.chunk. At 1.13.2 the full status is the unnamed
# ChunkStatus.field_18865, LevelChunkSection takes (int, boolean), and the biomes are the pre-1.15 flat 16 by 16
# column array.
common/src/test/java/world/thearchive/wdl/testsupport/SyntheticChunks.java

# A headless overworld Level for the entity fixtures (the Mob constructor binds a level). At 1.13.2 it goes through
# the six-argument Level constructor (a null server and dimension store, the overworld DimensionType.field_18954's
# dimension, an ActiveProfiler) and answers the abstract chunk-source factory method_3712 with none; the map-data,
# game-event, tick-rate and potion-brewing overrides dev's fixture carries name members 1.13.2's Level lacks.
common/src/test/java/world/thearchive/wdl/testsupport/HeadlessLevel.java

# Below 1.16 there is no RegistryAccess to smoke-test, so this proves the vanilla bootstrap ran by resolving stone and
# granite through the static Registry.BLOCK table and round-tripping a CompoundTag and a BlockPos.
common/src/test/java/world/thearchive/wdl/testsupport/RegistriesSmokeTest.java

# At 1.13.2 Registry.ITEM is a plain MappedRegistry with no default, so an item resolves through getOptional (null for
# an unknown id) rather than Registry.get. Below the 1.21 update ResourceLocation has no parse factory, so an id
# string is built with the public ResourceLocation(String) constructor. Below 1.20.5 there are no item components: a
# custom name is set via ItemStack.setHoverName, a stored stack is written with ItemStack.save (a {id, Count, tag}
# compound with Count as a byte), a written book's title, author and pages live on the item's tag, and
# ContainerHelper.saveAllItems takes no registries argument.
common/src/test/java/world/thearchive/wdl/testsupport/ItemFixtures.java

# Below 1.14 there is no ChunkSerializer, and 1.14.4's read needs a ServerLevel the headless test lacks, so the round
# trip proves self-consistency by decoding each section's block states directly through 1.13.2's
# PalettedContainer.method_17103 and pins that light is section-resident: isLightOn is a 1.14 chunk key.
common/src/test/java/world/thearchive/wdl/adapter/ChunkRoundTripTest.java

# Deleted: the plug's chunk-codec test dev carries exists from 1.18.2 up and is absent at 1.14.4 and 1.13.2 alike.
common/src/test/java/world/thearchive/wdl/adapter/impl/ChunkCodecImplTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the round trip opens the mod's own
# WdlRegionStorage through WorldPaths. Below 1.17 there is no separate entities/ region: entities live inside the
# region chunk, so the round trip seeds a host region chunk, folds the entity carrier into its Level.Entities through
# RegionChunkWriter.foldEntitiesIntoRegion and reads the entity back from that chunk; the sink's carrier is a bare
# {Entities:[...]} with no DataVersion or Position.
common/src/test/java/world/thearchive/wdl/adapter/EntityRoundTripTest.java

# Below 1.16 there is no RegistryAccess, so the level data is built after TestRegistries.bootstrap with no registries
# argument and read back through worldData().createTag; the rules are loaded into a new GameRules through loadFromTag,
# and 1.13.2's GameRules has no RULE_ keys (1.14.4 has them), so a rule is read by its name string through the unnamed
# method_4671. Dev's save-and-read-back cases through a LevelStorageAccess are absent.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatWorldOutputTest.java

# The headless entity double overrides hurt(DamageSource, float), the pre-1.21.2 damage entry point (the
# ServerLevel-taking hurtServer is 1.21.2 up). Below 1.20.5 Entity.defineSynchedData is no-arg, so the double
# overrides that form too; below 1.14 Entity.getAddEntityPacket does not exist (1.14.4 declares it abstract), so the
# double carries no override of it.
common/src/test/java/world/thearchive/wdl/adapter/CaptureAnchorTest.java

# The headless entity double overrides hurt(DamageSource, float) (hurtServer is 1.21.2 up) and the no-arg
# defineSynchedData (below 1.20.5), and below 1.14 carries no getAddEntityPacket override; the region store is the
# mod's own WdlRegionStorage (no IOWorker below 1.15, no SimpleRegionStorage below 1.20.5); below 1.20.5 there is no
# ItemCost, so a merchant offer's buy cost is a plain ItemStack, and at 1.13.2 the offers are the unnamed class_1145
# and class_1144. The overworld DimensionType is the unnamed field_18954, the lectern fixture (a 1.14 block entity) is
# an ender chest, and a spawned minecart is the object-type id 10 of 1.13.2's AddEntity packet.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossTallyTest.java

# The overworld DimensionType is unnamed in the bridge mapping at 1.13.2 (field_18954), so the session fixture names
# that constant.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionCompletionRecordTest.java

# The overworld DimensionType is unnamed in the bridge mapping at 1.13.2 (field_18954), so the session fixture and
# the captured-player fixture name that constant.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionFailSoftTest.java

# The overworld DimensionType is unnamed in the bridge mapping at 1.13.2 (field_18954), so the session fixture names
# that constant.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionFinishLossLineTest.java

# The overworld DimensionType is unnamed in the bridge mapping at 1.13.2 (field_18954), so the session fixture names
# that constant.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossLogWiringTest.java

# The overworld DimensionType is unnamed in the bridge mapping at 1.13.2 (field_18954), so the session fixture names
# that constant; LevelStorageSource is an interface at 1.13.2, its class the unnamed class_99, and the writer is
# opened with the saves directory passed in (1.13.2's LevelStorageSource has no getBaseDir).
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionReportStampFailSoftTest.java

# The headless mob double overrides hurt(DamageSource, float) (hurtServer with a ServerLevel is 1.21.2 up) and
# below 1.14 carries no getAddEntityPacket override; it is built against the HeadlessLevel, the Mob constructor
# binding a level.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkRootVehicleTest.java

# The geared mob double is built against the HeadlessLevel, the Mob constructor binding a level; the block-item
# expectations go through Blocks.X.asItem(), 1.13.2's Items having no constants for block items.
common/src/test/java/world/thearchive/wdl/adapter/impl/NaturalEquipmentTest.java

# Absent from dev: pins the plug's mount-armor and llama-carpet capture. At 1.13.2 the horse and llama constructors
# take the Level alone (the EntityType parameter is 1.14.4), the horse armor is the unnamed class_2976 tier read by
# getArmor and set by method_8390 (one armor item per tier, so the write names its item), and there is no TraderLlama.
common/src/test/java/world/thearchive/wdl/adapter/impl/MountArmorCaptureTest.java

# Absent from dev: pins the plug's saddle capture on a horse and a minecart, both built through 1.13.2's
# Level-only constructors (the EntityType parameter is 1.14.4).
common/src/test/java/world/thearchive/wdl/adapter/impl/SaddleCaptureTest.java

# Below the 1.21 update the HUD render callback carries a float partial-tick rather than a DeltaTracker, so the
# shared overlay's render entry point takes a float.
common/src/main/java/world/thearchive/wdl/client/WdlHudOverlay.java

# Absent from dev: below 1.17 there is no CycleButton widget (it arrived at 1.17 with the narration API), so this
# cycle button reproduces the value-cycling toggle. At 1.13.2 it extends the unnamed class_356, its label is the
# field_1053 string and its click is method_18374.
common/src/main/java/world/thearchive/wdl/client/WdlCycleButton.java

# Below 1.20 there is no FaviconTexture (added by the 1.20 GUI overhaul), so the client draw seam's blitFavicon names
# the world icon's registered ResourceLocation instead of the FaviconTexture handle. The draw surface itself
# (GuiGraphics, likewise 1.20 up) is reimplemented in the plug, which the diff excludes.
common/src/main/java/world/thearchive/wdl/adapter/RenderSurface.java

# Below the 1.21 update Util.OS has no openPath(Path), so the open-folder affordances call openFile(File). 1.13.2's GUI
# predates the 1.14 widget rework: the button is the unnamed class_356, the entry list class_1802, and the Screen
# members are unnamed too (width field_1230, height field_1231, font field_1234, addButton method_13411), so the
# screen and its download rows are built from those forms; the list overrides the scrollbar position method_1069,
# whose default fixed width/2 + 124 lands inside a wider row, to derive it from the row right edge, and blanks the
# top and bottom fade method_1065; a row narrates through NarratorChatListener.INSTANCE.handle, the shared lang key
# wdl.screen.downloads.narration staying identical across bands.
common/src/main/java/world/thearchive/wdl/client/WdlDownloadsScreen.java

# The settings list is 1.13.2's unnamed class_1802 and its controls class_356, built from the unnamed Screen members
# (field_1230, field_1231, field_1234, method_13411); its scrollbar position method_1069, a fixed width/2 + 124 by
# default, is derived from the row right edge so it clears the wider settings row. 1.13.2's Screen.render paints only
# its buttons and labels, so the list, added as a child (field_20307) rather than a renderable, is drawn by hand
# (method_1053) in the render override before super.render, ahead of the tab strip and footer buttons, and the
# recorded hover tooltips are drawn after it; below 1.20.2 the list fills tiled dirt over and under itself, which
# hides its unclipped row overflow (the list does not scissor).
common/src/main/java/world/thearchive/wdl/client/WdlSettingsScreen.java

# The JourneyMap API pin is the journeymap.client.api generation (journeymap_api_coordinate 1.14.4-1.5), not the
# journeymap.api.v2 dev binds, so the entry point implements journeymap.client.api.IClientPlugin under the
# journeymap.client.api.ClientPlugin annotation JourneyMap's annotation scan discovers on Forge.
common/src/main/java/world/thearchive/wdl/compat/journeymap/WdlJourneyMapPlugin.java

# Binds the journeymap.client.api generation: a PolygonOverlay takes the int dimension id DimensionType.getId returns
# (the pre-1.16 dimension identity) and the tick keys its rebuild on the level's DimensionType.
common/src/main/java/world/thearchive/wdl/compat/journeymap/JourneyMapOverlayDriver.java

# The journeymap.client.api generation has no MapPolygonWithHoles, so the overlay takes the coverage rectangles rather
# than the tracer's holed hulls, and MapPolygon(List) is built reflectively because the pinned API jar names its
# BlockPos parameter in mappings the compile does not bridge.
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygons.java

# The shared coverage-polygon tracer is public rather than dev's package-private, and its empty list is a Guava
# ImmutableList.of for the Java 8 floor (no List.of below Java 9).
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygonTracer.java

# Below 1.16 a dimension is a DimensionType rather than a ResourceKey, so the rebind is driven with
# DimensionType.NETHER and the overworld is the unnamed field_18954; below 1.16 there is no RegistryAccess, so the
# test bootstraps through TestRegistries.bootstrap, and a chunk key is ChunkPos.toLong (pack on dev).
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionDimensionRebindTest.java

# Below 1.16 the player's dimension is a DimensionType written as its int getId, so the dimension cases feed
# DimensionType.NETHER and read the int back, and dev's datapack-dimension case is absent; below 1.16 there is no
# RegistryAccess, so the test bootstraps through TestRegistries.bootstrap. Carries a setPosition shape case added at
# 1.13.2 that dev does not have.
common/src/test/java/world/thearchive/wdl/adapter/PlayerTagTest.java

# Below the 1.21 update ResourceLocation has no parse factory, so a block-entity id string is built with the public
# ResourceLocation(String) constructor. Below 1.20.5 a block entity is written with BlockEntity.save(CompoundTag) and
# read back with the one-argument BlockEntity.loadStatic (no Provider), and ContainerHelper.loadAllItems/saveAllItems
# take no registries argument. At 1.13.2 EntityBlock is the unnamed class_1451 and Registry.BLOCK iterates as Object.
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelity.java

# 1.13.2's JukeboxBlockEntity writes only RecordItem, no IsPlaying or tick sidecar (present at 1.19.4), so the
# jukebox merge fixtures set only that key. Below 1.20.5 a beehive block entity's occupants key is
# still Bees (capitalized), so these merge fixtures use that key and the beehive fixture goes through the unchecked
# malformedChunkTagWith instead of the fidelity-checked chunkTagWith. Below 1.18 the merge reads the chunk block
# entities under Level.TileEntities, and fastutil 8.2.1 has no LongSet.of(), so the empty set is LongSets.EMPTY_SET.
# Below 1.14 there is no barrel, so the second container fixture is a trapped chest.
common/src/test/java/world/thearchive/wdl/adapter/ChunkMergeTest.java

# Below the 1.21 update a captured jukebox is marked playing via IsPlaying rather than the ticks_since_song_started
# sidecar, so this round-trip asserts the playing flag. Below 1.20.5 there are no item components: the RecordItem
# write is ItemStack.save, read back via vanilla's own ItemStack.of. No beehive exists at 1.13.2, so dev's beehive
# round trip is absent.
common/src/test/java/world/thearchive/wdl/adapter/InteractionMergeRoundTripTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer drains against the mod's own
# WdlRegionStorage, opened per dimension through a StorageOpener. Below 1.17 entities live inside the region chunk
# under Level.Entities, so the ENTITIES write task folds into the host region chunk through
# RegionChunkWriter.foldEntitiesIntoRegion rather than draining a second entities/ storage: one Storages of region
# files, the resume scan over the same chunk, and a fold with no host chunk a counted loss. The pauseEncoding Javadoc
# names the login handshake's registry rebuild alone: Forge 25 reverts nothing when a level closes, its client-side
# revert (ClientHooks.handleClientWorldClosing) having no caller.
common/src/main/java/world/thearchive/wdl/adapter/AsyncSaveWriter.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the merge-write reads and writes through
# the mod's own WdlRegionStorage. Below 1.17 entities live inside the region chunk, so this carries
# foldEntitiesIntoRegion, the entity fold that unions a captured entity carrier into the host chunk's Level.Entities
# through EntityMerge and writes the chunk back, and the terrain merge-write preserves the on-disk Level.Entities the
# fresh terrain tag lacks, so a routine terrain re-flush of a revisited chunk does not drop the entities a prior fold
# saved there.
common/src/main/java/world/thearchive/wdl/adapter/RegionChunkWriter.java

# Below 1.20.5 there is no RegionStorageInfo and below 1.15 no IOWorker, so this seam opens the mod's own
# WdlRegionStorage (openRegionStorage). Below 1.17 there is no separate entities/ region: entities live inside the
# region chunk, so the entitiesDirectory/openEntitiesStorage members the 1.17.1 seam carries are dropped and this
# maps and opens only region/.
common/src/main/java/world/thearchive/wdl/adapter/WorldPaths.java

# Absent from dev, added at 1.14.4 as the synchronous region store below 1.15's IOWorker; 1.13.2 also lacks
# RegionFileStorage (1.14.4's base, which its subclass extends), so the store owns a RegionFile cache keyed by region
# and reads and writes through RegionFile's chunk streams, unnamed in the bridge (method_3957, method_3961).
common/src/main/java/world/thearchive/wdl/adapter/WdlRegionStorage.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the drain is proven against the mod's
# own WdlRegionStorage rather than constructing one via RegionStorageInfo/DataFixers. Below 1.20.5
# ContainerHelper.loadAllItems takes no registries argument. The overworld DimensionType is the unnamed field_18954.
# Its encode-pause Javadoc names the login handshake's registry rebuild alone, Forge 25 reverting nothing when a level
# closes (its client-side revert, ClientHooks.handleClientWorldClosing, has no caller).
common/src/test/java/world/thearchive/wdl/adapter/AsyncSaveWriterTest.java

# Deleted below 1.19: the whole test exercises chiseled-bookshelf occupancy flush, and ChiseledBookShelfBlock does
# not exist below 1.19.
common/src/test/java/world/thearchive/wdl/adapter/BookshelfOccupancyFlushTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the prior-write and read-back open the
# mod's own WdlRegionStorage through WorldPaths; the placed chest item is Blocks.CHEST, 1.13.2's Items having no
# constants for block items.
common/src/test/java/world/thearchive/wdl/adapter/CarryForwardWiringTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer and the entity read-back open
# the mod's own WdlRegionStorage through WorldPaths, and each entity chunk is seeded with a host region chunk because
# below 1.17 entities fold into it. Below 1.20.5 a filled map's id is the raw int tag."map", not a components entry.
# Below 1.16 there is no RegistryAccess, so the fixtures take none after TestRegistries.bootstrap, and the item ids
# are read with getString (no getStringOr below 1.21.5). The overworld DimensionType is the unnamed field_18954.
common/src/test/java/world/thearchive/wdl/adapter/EntityVehicleRelocationTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the writer and the entity read-back open
# the mod's own WdlRegionStorage through WorldPaths. Below 1.16 there is no RegistryAccess, so the session fixtures
# take none after TestRegistries.bootstrap, and a dimension is a DimensionType (NETHER, the unnamed overworld
# field_18954) rather than dev's ResourceKey. The scan hands the bank a region chunk, whose entities sit under
# Level.Entities below 1.17, so the fixture that stands in for a prior download's record builds that shape; the prior
# level.dat is written through a LevelStorage constructed directly over the saves directory.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionResumedMountReleaseTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so the sweep is proven against the mod's
# own WdlRegionStorage. Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument. The lectern fixture
# (a 1.14 block entity) is an ender chest, and the overworld DimensionType is the unnamed field_18954.
common/src/test/java/world/thearchive/wdl/adapter/OrphanedContainerSweepTest.java

# Below 1.20.5 there is no SimpleRegionStorage or RegionStorageInfo and below 1.15 no IOWorker, so this integration
# test drives the mod's own WdlRegionStorage opened through WorldPaths; the overworld DimensionType is the unnamed
# field_18954.
common/src/test/java/world/thearchive/wdl/adapter/RegionIntegrationTest.java

# Below 1.20.5 there is no SimpleRegionStorage and below 1.15 no IOWorker, so this drives the mod's own
# WdlRegionStorage directly rather than the newer wrapper.
common/src/test/java/world/thearchive/wdl/testsupport/RegionRoundTrip.java

# Below 1.20.5 ChunkStatus is in net.minecraft.world.level.chunk, not the .status subpackage added at that update.
# 1.13.2 has no SectionPos (1.14.4), so the section keys go through the mod's own SectionKey and the camera chunk is
# a shift by four; no lectern (1.14), so the lectern candidate arm is dropped; no Entity.position(), so a center is
# built from the x, y and z fields; and the chunk lookup is ChunkSource.method_17044.
common/src/main/java/world/thearchive/wdl/adapter/OutlineTracker.java

# 1.13.2's BlockPos has no static getX/getY/getZ(long) unpackers (1.14.4) and its long-to-BlockPos factory is unnamed
# in the bridge mapping, so each packed cell is unpacked through BlockPos.method_10488 and read off the instance.
common/src/main/java/world/thearchive/wdl/adapter/OutlineRim.java

# Absent from dev, added at 1.13.2: the section-coordinate long packing the outline draw-set keys its buckets by,
# replacing net.minecraft.core.SectionPos (a 1.14.4 class), shared by the producer and the per-band renderer so the
# encode and decode cannot drift; the block-to-section step unpacks through BlockPos.method_10488.
common/src/main/java/world/thearchive/wdl/adapter/SectionKey.java

# Absent from dev, added at 1.13.2 with SectionKey: pins the packing's round trip across the sign range.
common/src/test/java/world/thearchive/wdl/adapter/SectionKeyTest.java

# Below 1.16 the player's dimension is an int id, so setDimension writes DimensionType.getId and dimensionOf reads
# DimensionType.getById; the Attach UUID is written with CompoundTag.putUUID (the AttachMost/AttachLeast long pair),
# and below 1.15 DoubleTag and FloatTag have no valueOf factories, so the Pos and Rotation entries use the
# constructors. The Java 8 floor cannot bind a pattern variable, so each instanceof over a tag is a local, a test and
# a cast.
common/src/main/java/world/thearchive/wdl/adapter/PlayerTag.java

# Below 1.20.2 there is no AdvancementProgress.CODEC, so the advancement value is serialized through vanilla's own
# AdvancementProgress Gson adapter rather than encoded through a codec; at 1.13.2 that adapter is the unnamed
# AdvancementProgress.class_3335 and Registry is walked through its explicit iterator.
common/src/main/java/world/thearchive/wdl/adapter/PlayerProgressSerializer.java

# The entity UUID is written with CompoundTag.putUUID (the UUIDMost/UUIDLeast long pair below 1.16) and the Pos list
# is built by hand from DoubleTag constructors, DoubleTag having no valueOf factory below 1.15 and no Vec3 codec at
# 1.13.2.
common/src/test/java/world/thearchive/wdl/testsupport/EntityFixtures.java

# Below 1.16 there is no RegistryAccess, so the level data is built after TestRegistries.bootstrap with no registries
# argument, the player UUID is written with CompoundTag.putUUID (the UUIDMost/UUIDLeast long pair), the dimension is a
# DimensionType rather than dev's ResourceKey, and the item ids are read with getString (no getStringOr below 1.21.5);
# LevelStorageSource is an interface at 1.13.2, its class the unnamed class_99 and its selectLevel result cast to
# LevelStorage.
common/src/test/java/world/thearchive/wdl/adapter/PriorPlayerReadRoundTripTest.java

# Below 1.16 there is no RegistryAccess, so the level data is built after TestRegistries.bootstrap with no registries
# argument and its Data tag written and read back through NbtIo.writeCompressed(OutputStream) and
# readCompressed(InputStream) (no File overloads of the compressed forms at 1.13.2); the void world is the flat
# generatorName the Data tag carries, not dev's world_gen_settings.dat; LevelStorageSource is an interface at 1.13.2,
# its class the unnamed class_99 and its selectLevel result cast to LevelStorage.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatRoundTripTest.java

# The Java 8 floor cannot bind a pattern variable in a type test, so the carried Xp tag is read with a null check
# rather than dev's instanceof-Tag pattern. 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag
# rather than throwing, so the tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/EntityContainerMerge.java

# Below 1.20.5 an item is {id, Count, tag}; the walk recurses into a shulker's nested items via
# tag.BlockEntityTag.Items and a bundle's via tag.Items, rather than the minecraft:container/minecraft:bundle_contents
# components. 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than throwing, so the
# tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/ItemTreeWalk.java

# Below 1.20.5 a filled map's id is the raw int tag."map", not a minecraft:map_id component, so the remap reads
# and writes that key directly rather than through MapId.CODEC.
common/src/main/java/world/thearchive/wdl/adapter/MapIdRemap.java

# Below 1.20.5 a filled map's id is the raw int tag."map", not a minecraft:map_id component, so the collector
# reads that key directly rather than through MapId.CODEC.
common/src/main/java/world/thearchive/wdl/adapter/MapIdCollector.java

# Below 1.20.5 there is no BundleContents component, so the change signal compares the slot's stack identity and count
# alone; dev's bundle-contents leg (the DataComponents.BUNDLE_CONTENTS compare and its lastBundles snapshot) is
# absent.
common/src/main/java/world/thearchive/wdl/adapter/MenuChangeTracker.java

# Below 1.14 there is no MerchantOffers type or offers codec: the offers are the unnamed class_1145, written through
# its method_3557 (a lenient NBT write that never rejects an offer), and a villager's xp is not carried (no
# trader xp below 1.14). 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than
# throwing, so the tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferCapture.java

# Below 1.20.5 an item's data lives in its tag compound rather than a components map: the lodestone target is the
# raw LodestonePos/LodestoneDimension keys, and a beehive's occupants and the hive's own flower position (carried
# by a pre-component item's whole block-entity copy) are tag.BlockEntityTag.Bees[].EntityData.FlowerPos and
# tag.BlockEntityTag.FlowerPos, both scrubbed. 1.13.2's ListTag.get answers an out-of-range index with a sentinel
# EndTag rather than throwing, so the tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/ItemLocationScrub.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument; the planks stack is Blocks.OAK_PLANKS,
# 1.13.2's Items having no constants for block items.
common/src/test/java/world/thearchive/wdl/adapter/ContainerMergeRoundTripTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument.
common/src/test/java/world/thearchive/wdl/adapter/ContainerRecaptureSynergyTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument.
common/src/test/java/world/thearchive/wdl/adapter/ContainerStashMergeTest.java

# Below 1.20.5 ContainerHelper.loadAllItems takes no registries argument.
common/src/test/java/world/thearchive/wdl/adapter/EntityContainerStashMergeTest.java

# Below 1.20.5 there is no ItemCost, so a merchant offer's buy cost is a plain ItemStack, and below 1.14 there is no
# MerchantOffers type: the offers holder is built from the unnamed class_1145 and class_1144 through method_3557.
common/src/test/java/world/thearchive/wdl/adapter/EntityMergeTest.java

# Below 1.20.5 there are no item components: a placed shulker's contents live under the item's
# tag.BlockEntityTag.Items rather than DataComponents.CONTAINER, and InteractionCapture.captureRecordItem takes no
# registries argument. Below 1.19 there is no chiseled bookshelf and at 1.13.2 no beehive, so dev's bookshelf and
# beehive cases are absent. The placed block items are Blocks.SHULKER_BOX, Blocks.CHEST, Blocks.DIRT and Blocks.STONE,
# 1.13.2's Items having no constants for block items, and below 1.16 there is no RegistryAccess, so the test
# bootstraps through TestRegistries.bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/InteractionStashMergeTest.java

# Below 1.20.5 there are no item components: the lodestone target is the raw LodestonePos/LodestoneDimension/
# LodestoneTracked keys, a hive item's occupants and flower position are
# tag.BlockEntityTag.Bees[].EntityData.FlowerPos and tag.BlockEntityTag.FlowerPos, and shulker contents live under
# tag.BlockEntityTag.Items. Below 1.14 there is no GlobalPos, so the target is a BlockPos plus the overworld
# DimensionType (the unnamed field_18954); no bell, campfire or beehive exists at 1.13.2, so a jukebox, a chest and a
# sign stand in as the block-entity fixtures and a chest item carries the hand-built Bees shape; the block items are
# Blocks.SHULKER_BOX and Blocks.CHEST, 1.13.2's Items having no constants for block items.
common/src/test/java/world/thearchive/wdl/adapter/ItemLocationScrubTest.java

# Below 1.20.5 an item is {id, Count, tag}; the walk drives hand-built NBT keyed by tag.BlockEntityTag.Items (a
# shulker) and tag.Items (a bundle) rather than the component keys, and there is no sulfur cube to recurse into.
common/src/test/java/world/thearchive/wdl/adapter/ItemTreeWalkTest.java

# Below 1.20.5 there is no WrittenBookContent component; a written book's title/author/generation/resolved and its
# pages live directly on the item's tag, so the fixture and the read-back build/read that shape. Below 1.14 there is
# no lectern block entity, so the re-captured lectern cases are disabled as a documented limit.
common/src/test/java/world/thearchive/wdl/adapter/LecternRecaptureSynergyTest.java

# Below 1.20.5 there are no WrittenBookContent/WritableBookContent components; a written book's
# title/author/generation/resolved and its pages (JSON-component strings) live on the item's tag, an unsigned
# book's pages are plain strings, and the read-back is vanilla's own ItemStack.of rather than ItemStack.CODEC. Below
# 1.14 there is no lectern block entity, so the lectern cases are disabled as a documented limit.
common/src/test/java/world/thearchive/wdl/adapter/LecternSinkRoundTripTest.java

# Below 1.20.5 there is no WrittenBookContent component; a written book's title/author/generation/resolved and its
# pages live directly on the item's tag, so the fixture and the read-back build/read that shape. Below 1.14 there is
# no lectern block entity, so the class is disabled as a documented limit.
common/src/test/java/world/thearchive/wdl/adapter/LecternStashMergeTest.java

# Below 1.20.5 MapItemSavedData.load takes only the tag, not a registries argument. Below 1.14 a map has no locked
# flag, so the locked round trip asserts that no locked key is written; the overworld DimensionType is the unnamed
# field_18954.
common/src/test/java/world/thearchive/wdl/adapter/MapSinkRoundTripTest.java

# Below 1.15 vanilla ItemStack.save shares the live stack's tag compound instead of copying it, so a guard pins that
# the remap leaves the live stack's map id alone; below 1.16 there is no RegistryAccess, so the test bootstraps vanilla
# and a map's dimension is the int DimensionType id (the overworld the unnamed field_18954); the id sets are Guava.
common/src/test/java/world/thearchive/wdl/adapter/MapArchiveTest.java

# Below 1.20.5 there is no BundleContents component, so dev's in-place bundle-mutation case is absent; 1.13.2's
# SimpleContainer takes a leading Component name (1.14.4's takes the size alone), and below 1.16 there is no
# RegistryAccess, so the test bootstraps through TestRegistries.bootstrap.
common/src/test/java/world/thearchive/wdl/adapter/MenuChangeTrackerTest.java

# Below 1.20.5 there are no item components: the lodestone target is the raw LodestonePos/LodestoneDimension/
# LodestoneTracked keys, a filled map's id is the raw int tag."map", and there is no ItemCost, so a merchant
# offer's buy cost is a plain ItemStack. The sell item's fields are read off its tag rather than its components
# map. Below 1.14 there is no MerchantOffers type or GlobalPos: the offers are the unnamed class_1145 and class_1144,
# the target a BlockPos with the overworld DimensionType (field_18954), and a villager's xp is not carried.
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferCaptureTest.java

# Below 1.20.5 there is no ItemCost, so a merchant offer's buy cost is a plain ItemStack, and below 1.14 there is no
# MerchantOffers type: the offers holder is built from the unnamed class_1145 and class_1144 through method_3557.
common/src/test/java/world/thearchive/wdl/adapter/MerchantStashMergeTest.java

# Below 1.20.5 a beehive block entity's occupants key is still Bees (capitalized), the pre-lowercasing form, and the
# jukebox fixtures carry IsPlaying, RecordStartTick and TickCount alongside RecordItem, the shape the capture writes
# (1.13.2's JukeboxBlockEntity writes none of the three), so the beehive fixture goes through the unchecked
# malformedChunkTagWith instead of the fidelity-checked chunkTagWith. The overworld
# DimensionType is the unnamed field_18954, and a block key is packed through the BlockPos instance, 1.13.2 having no
# static BlockPos.asLong(int, int, int).
common/src/test/java/world/thearchive/wdl/adapter/RecoveredScanTest.java

# Below 1.20.5 only a BaseContainerBlockEntity carries an arbitrary custom name, written as the raw CustomName
# JSON key rather than the minecraft:custom_name component, and a beehive's occupants are the pre-component
# {EntityData, TicksInHive, MinOccupationTicks} entries under Bees rather than an Occupant.LIST_CODEC encode. Below
# 1.18 the chunk tag wraps its block entities under Level.TileEntities rather than the flat root block_entities.
common/src/test/java/world/thearchive/wdl/testsupport/BlockEntityFixtures.java

# Below 1.20.5 an item's count is the raw Count byte (not a components-map int), so a hand-built entry without it
# is silently dropped by ContainerHelper.saveAllItems rather than round-tripping with a single named key missing.
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelityTest.java

# Below 1.20.5 there are no item components: a filled map's id is the raw int tag."map", and a shulker's nested
# contents live under tag.BlockEntityTag.Items; the shulker item is Blocks.SHULKER_BOX, 1.13.2's Items having no
# constant for it, and dev's bundle fixture is absent, there being no bundle at 1.13.2.
common/src/test/java/world/thearchive/wdl/testsupport/MapHolderFixtures.java

# Below 1.20.3 there is no Crafter block, block entity or menu, so the crafter open-bind leg, the crafter stash and
# the crafter menu-only ContainerData are dropped. Below 1.20 Entity.level is a public field rather than the level()
# accessor, so the spectator-reach guard reads player.level directly. 1.13.2 has no BlockHitResult or EntityHitResult
# (1.14.4): the crosshair HitResult's kind is its field_595 and its entity field_601, and ChestMenu has no
# getRowCount, so a large chest is told by its container size of 54; BlockPos.of is the unnamed method_10488.
common/src/main/java/world/thearchive/wdl/adapter/ContainerCapture.java

# Absent from dev, carried on every band up to 1.20.4: below 1.20.5 a mount's saddle, armor and chest share one
# container that vanilla addresses absolutely, writing Slot from index two and refusing anything below two or past
# the container size on read, so a zero-based holder loses its two lowest stacks with no log line; from 1.20.5 a
# saved chest stack is numbered chest-relative. This pins the lift's offset by reloading through vanilla's entity
# reader, a count assertion passing on shifted contents; a chested donkey or mule sizes the container to a flat
# seventeen and a llama to two plus three times its strength (the AbstractChestedHorse and Llama inventory sizes). At
# 1.13.2 the donkey class is unnamed in the bridge mapping, so it comes from EntityType.DONKEY.create; the mount
# inventory is reached by reflection (no Entity.getSlot, the field protected); and the player under the lift is a
# headless LocalPlayer through 1.13.2's five-argument constructor over a ClientPacketListener, the
# HorseInventoryMenu constructor taking (inventory, chest, mount, player).
common/src/test/java/world/thearchive/wdl/adapter/MountChestSlotsTest.java

# Below 1.20.3 NbtIo has no Path or NbtAccounter overloads, and 1.13.2's NbtIo has no File overloads of the
# compressed forms either, so the compressed write and read go through the OutputStream and InputStream forms; the
# DataVersion is the literal 1631, 1.13.2's SharedConstants having no getCurrentVersion.
common/src/main/java/world/thearchive/wdl/adapter/MapDataWriter.java

# Below 1.20.3 NbtIo.readCompressed takes an InputStream with no NbtAccounter argument.
common/src/main/java/world/thearchive/wdl/adapter/StashHolder.java

# 1.13.2's ConfirmScreen takes a two-argument confirm callback, plain-string title, message and button labels and a
# trailing dialog id (1.14.4's takes a BooleanConsumer and Components), and draws the title in a fixed white, so the
# amber option name rides the legacy section codes getColoredString emits.
common/src/main/java/world/thearchive/wdl/client/ResumeConfirm.java

# 1.13.2's ConfirmScreen takes a two-argument confirm callback, plain-string title, message and button labels and a
# trailing dialog id (1.14.4's takes a BooleanConsumer, two Components and two label strings), and draws the title in
# a fixed white, so the amber option name rides the legacy section codes getColoredString emits.
common/src/main/java/world/thearchive/wdl/client/WdlCaptureDisableConfirmScreen.java

# Below 1.20.3 there is no Crafter menu or block entity, so the crafter capture tests are dropped.
common/src/test/java/world/thearchive/wdl/adapter/ContainerCaptureTest.java

# Below 1.20.3 there is no Crafter block entity, so this flush-plan test builds its state-bearing block-entity
# fixtures on the brewing stand (BrewTime/Fuel) rather than the crafter. Below 1.14 there is no lectern or barrel,
# so the lectern fixture is an ender chest and the barrel a shulker box.
common/src/test/java/world/thearchive/wdl/adapter/ChunkFlushPlanTest.java

# Below 1.20.3 NbtIo.readCompressed takes an InputStream with no NbtAccounter argument; LevelStorageSource is an
# interface at 1.13.2, its class the unnamed class_99 and its selectLevel result cast to LevelStorage.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatPlayerRoundTripTest.java

# Below 1.20.3 NbtIo has no Path or NbtAccounter overloads, so this round trip reads and writes through the stream
# forms.
common/src/test/java/world/thearchive/wdl/adapter/MapDataWriterRoundTripTest.java

# Below 1.20.3 NbtIo has no Path or NbtAccounter overloads, so this write-task test uses the stream forms.
common/src/test/java/world/thearchive/wdl/adapter/MapWriteTaskTest.java

# Below 1.20.2 there is no AdvancementHolder/AdvancementNode split (the advancement datapack rewrite is 1.20.2),
# so the harvesting ClientAdvancements.Listener works in Advancement and reads the id via Advancement.getId().
common/src/main/java/world/thearchive/wdl/adapter/AdvancementSnapshot.java

# Below 1.20.2 there is no configuration phase, so the connection tee does not observe
# ClientboundStartConfigurationPacket; the backend-transfer stop machinery (signalTransfer / consumeTransferSignal)
# stays for the controller wiring but is never raised below 1.20.2.
common/src/main/java/world/thearchive/wdl/adapter/ConnectionTee.java

# Below 1.20.2 there is no AdvancementRequirements, so the fixture drives the two-argument
# AdvancementProgress.update(Map<String, Criterion>, String[][]) with a no-argument Criterion (update reads only
# the map keys).
common/src/test/java/world/thearchive/wdl/adapter/PlayerProgressSerializerTest.java

# The Forge island's manifest carries loaderVersion [25,) and pins minecraft versionRange [1.13.2] (Forge
# 1.13.2-25.0.223, the 25.x line for 1.13.2), with ordering NONE on both dependencies because 25.0.223's ModSorter
# resolves an AFTER edge's target before forge and minecraft are ordering nodes. Absent from dev.
forge/src/main/resources/META-INF/mods.toml

# Absent from dev: the island's own gradle.properties. At 1.13.2 it carries the JVM args alone, the hand-rolled island
# having no Architectury Loom and so no loom.platform switch (1.14.4's island sets loom.platform=forge).
forge/gradle.properties

# Absent from dev: the island's daemon toolchain pin. At 1.13.2 it is Java 17, the floor mod-publish-plugin declares,
# where 1.14.4's island pins 21 for Architectury Loom; the compile toolchain stays java_version 8 on both.
forge/gradle/gradle-daemon-jvm.properties

# Absent from dev: the island's access transformer in the classic SRG member scheme with 1.13.2's SRG class names
# (SPacketEntity, ContainerHorseInventory), widening the Connection channel, the move packet's entity id and
# HorseInventoryMenu.horse; the move deltas are int fields with public getXa/getYa/getZa (1.14.4 widens its shorts).
forge/src/main/resources/META-INF/accesstransformer.cfg

# The Forge connection tee names two members the Forge access transformer widens (net.minecraft.network.Connection
# channel and the ClientboundMoveEntityPacket entity id, both non-public with no getter at 1.13.2) and narrows the
# packet's int move-deltas to the shared ConnectionTee contract's short: at 1.13.2 the deltas are int with public
# getters and are read as a sign-extended short off the wire, so the narrowing is exact. Absent from dev.
forge/src/main/java/world/thearchive/wdl/forge/ForgeConnectionTee.java

# The Forge mount-menu read names the non-public HorseInventoryMenu.horse the Forge access transformer widens, the one
# member MountMenuReader needs that 1.13.2 exposes no getter for. Absent from dev.
forge/src/main/java/world/thearchive/wdl/forge/ForgeMountMenuReader.java

# Absent from dev: the hand-rolled Forge island build (no ForgeGradle, no Architectury Loom, which 1.14.4's island
# applies) compiling common's source against the classpath the root's writeIslandClasspath publishes, widening the
# access-transformer members (widenForCompile), reobfuscating to 1.13.2 SRG (reobfJar, checkReobf), packaging modJar.
forge/build.gradle.kts

# Absent from dev: the island's settings. At 1.13.2 the island is a plain-java build with no Loom, so it declares no
# plugin repositories, where 1.14.4's island resolves Architectury Loom from the Architectury, Fabric and Forge
# Mavens.
forge/settings.gradle.kts

# The resource pack_format tracks the Minecraft version: 4 for 1.13.2 (PackCompatibility.forFormat accepts only 4
# at 1.13.2, treating 3 and under as too old and 5 and up as too new).
forge/src/main/resources/pack.mcmeta

# The reobf pass/negative fixtures: a build-support source set of the 1.13.2 island that compiles a small Mojmap-named
# fixture, kept separate from the real merged-main reobf path. checkReobfNegative reobfuscates and scans this fixture
# to prove checkReobf fires on a known-dirty input; reobfJar itself reobfuscates the real merged-main island output
# (Mojmap to 1.13.2 SRG). Absent from dev, which carries no hand-rolled Forge reobf island.
forge/src/reobftest/**

# Deleted: the whole Fabric tree (the loader entry points, the platform bridge, the loader metadata and the
# gametest suite) that dev and 1.14.4 carry. 1.13.2 is Forge-only below the Mojmap floor and its root build
# includes no fabric subproject.
fabric/

# Below the 1.19 registry split the code-defined registries are static fields on Registry (Registry.ENTITY_TYPE),
# not the net.minecraft.core.registries.BuiltInRegistries added at 1.19.3, so this open-click type check resolves the
# entity id through Registry.ENTITY_TYPE. 1.13.2 has no BlockHitResult (the hook takes HitResult and reads the block
# through method_9344), no BlockState.getMenuProvider (a menu-opening block is told by its block entity implementing
# the unnamed class_2610), no AbstractVillager (the merchant is a Villager whose profession is the int method_3115,
# 5 being the nitwit), and isRedstoneConductor is the unnamed BlockState.method_16907.
common/src/main/java/world/thearchive/wdl/adapter/OpenClickTracker.java

# Below 1.16 a dimension is a DimensionType rather than a ResourceKey, so forType takes and returns DimensionType,
# comparing the type against NETHER and the end, and forId compares DimensionType.getName strings; the overworld and
# end are unnamed in the bridge mapping at 1.13.2 (field_18954, field_18956).
common/src/main/java/world/thearchive/wdl/adapter/VanillaDimensions.java

# Below 1.19 there is no chiseled bookshelf, so the per-chunk bookshelf-occupancy producer is dropped and the
# read-merge composer passes an empty occupancy map (ChunkMerge.occupancyMap); the ChunkMerge per-slot union stays as
# unused-but-compiling code, the way the crafter open-bind stays below 1.20.3. BlockPos.of is the unnamed
# method_10488 at 1.13.2.
common/src/main/java/world/thearchive/wdl/adapter/ChunkFlushPlan.java

# Below 1.16 an entity's UUID is the UUIDMost/UUIDLeast long pair, so the read-merge keys the captured entity through
# CompoundTag.getUUID rather than the four-int codec (SerializableUUID.CODEC below 1.19, UUIDUtil.CODEC from 1.19).
# Below 1.17 the merge folds into the in-chunk Level.Entities rather than a separate entities/ region (its
# {Entities:[...]} envelope in/out is band-agnostic, so only the Javadoc naming the target diverges; the merge logic
# is unchanged). 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than throwing, so
# the tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/EntityMerge.java

# Dev's Wdl.initialize constructs the XaeroPlus integration; 1.13.2's does not (XaeroPlus publishes no 1.13.x build).
# It wires the 1.13.2 loader's level-teardown edge: onLevelTeardown stops the controller when the bridge's
# isConnectionClosed is true at fire time and does nothing on a dimension change, and the loader's disconnect callback
# runs controller.stop rather than onDisconnect. 1.13.2's SharedConstants has no getCurrentVersion, so the game
# version is RealmsSharedConstants.VERSION_STRING; its LevelStorageSource has no getBaseDir, so the saves directory is
# getLevelPath(""); and Minecraft is no Executor, so the completion poke is scheduled through method_6635.
common/src/main/java/world/thearchive/wdl/Wdl.java

# 1.13.2's LevelStorageSource has no getBaseDir (1.14.4), so the saves directory is read as getLevelPath(""), the base
# directory resolved against an empty name.
common/src/main/java/world/thearchive/wdl/ResumeFlow.java

# Deleted with the rest of the XaeroPlus binding (XaeroPlus ships no 1.13.x build for either loader): the plugin
# binding that reads the coverage index into XaeroPlus.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/XaeroPlusBinding.java

# Deleted with the rest of the XaeroPlus binding: the launch-time integration hook Wdl.initialize called.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/XaeroPlusIntegration.java

# Deleted with the rest of the XaeroPlus binding: the package's @NullMarked marker.
common/src/main/java/world/thearchive/wdl/compat/xaeroplus/package-info.java

# Deleted: ChiseledBookShelfBlock does not exist below 1.19, so the chiseled-bookshelf slot helper is dropped; the
# MC-free core bookshelf helpers (CapturedContainers, OutlineClassifier.classifyBookshelf) stay byte-identical.
common/src/main/java/world/thearchive/wdl/adapter/BookshelfSlots.java

# The view-layer literal inventory is band-specific: below 1.19 the GUI writes user-facing strings through the
# new TextComponent(...) constructor rather than Component.literal, and the acknowledged-literals map (including the
# empty no-phase HUD placeholder in WdlHudOverlay) reflects the 1.13.2 set. The loader-root loop is skip-if-absent
# for a loader root rather than dev's hard assertTrue, since 1.13.2 ships neither root loader tree; common's own
# root stays required and scanned.
common/src/test/java/world/thearchive/wdl/UiLiteralEnrollmentTest.java

# Deleted: the chiseled-bookshelf slot-mask helper it tests (BookshelfSlots) is dropped because
# ChiseledBookShelfBlock does not exist below 1.19.
common/src/test/java/world/thearchive/wdl/adapter/BookshelfSlotsTest.java

# Below 1.19 the spawn is split by packet, so the synthetic add-entity packets are 1.13.2's: ClientboundAddEntityPacket
# is built from a placeholder Entity and the object-type id (10, a minecart), and the Rot and PosRot move packets
# are the unnamed class_2030 and class_2029 with int deltas.
common/src/test/java/world/thearchive/wdl/adapter/EntityPacketCaptureTest.java

# Four of the nine band-neutral curated game-rule specs have no rule at 1.13.2 (doTraderSpawning and doPatrolSpawning
# are present by 1.15.2, doWardenSpawning from 1.19 and doVinesSpread by 1.19.4), so the runtime filter surfaces five
# and the layout skips their order slots alongside the fire-spread row, whose rule fire_spread_radius_around_player
# exists only from 1.21.11.
common/src/test/java/world/thearchive/wdl/adapter/LevelDatCuratedGameRulesTest.java

# Below 1.16 a dimension is a DimensionType rather than a ResourceKey, so the assertions feed forType the three
# DimensionType constants and read the ids back through DimensionType.getName; the overworld and end are the unnamed
# field_18954 and field_18956 at 1.13.2.
common/src/test/java/world/thearchive/wdl/adapter/VanillaDimensionsTest.java

# Below 1.19 the Forge client-registration APIs differ: the config screen registers through
# ExtensionPoint.CONFIGGUIFACTORY as a BiFunction rather than the ConfigGuiHandler.ConfigGuiFactory record added at
# 1.17 or the ConfigScreenHandler added at 1.19, keys register through ClientRegistry.registerKeyBinding
# (fml.client.registry at Forge 25) during FMLClientSetupEvent rather than the RegisterKeyMappingsEvent added at
# 1.19, the HUD host is RenderGameOverlayEvent.Post on the game bus rather than the OverlayRegistry added at 1.17 or
# the RegisterGuiOverlaysEvent added at 1.19, the connection tee installs on the client-tick edge where
# Minecraft.getConnection() turns non-null rather than a login event (ClientPlayerNetworkEvent does not exist at
# Forge 25), and PlayerInteractEvent exposes getEntityPlayer/getWorld rather than getEntity/getLevel.
forge/src/main/java/world/thearchive/wdl/forge/WdlForge.java

# Below 1.19 keys register through ClientRegistry.registerKeyBinding (fml.client.registry at Forge 25) during
# FMLClientSetupEvent rather than the RegisterKeyMappingsEvent added at 1.19, the screen-init event is
# GuiScreenEvent.InitGuiEvent.Post (getGui/addButton) rather than the 1.18.2 ScreenEvent.InitScreenEvent.Post,
# ClientPlayerNetworkEvent (the login/logout event the Forge bands from 1.14.4 up listen on) does not exist at Forge
# 25, so the connection edge is read off Minecraft.getConnection() turning non-null or null on the client tick
# instead, and the /wdl client command is a no-op because Forge 25 has no client-command event
# (RegisterClientCommandsEvent is a 1.18.2 addition). It also implements the 1.13.2 PlatformBridge.onLevelTeardown,
# from Forge's WorldEvent.Unload, which Forge 25 posts at the head of the level-install method
# (Minecraft.method_18206) and so ahead of the player it nulls there; gated on the client's own level, an integrated
# server posting the same event for its worlds off the client thread.
forge/src/main/java/world/thearchive/wdl/forge/ForgePlatformBridge.java

# The screen-open warm gate is WorldType.VOID or needsWorldgenReconstruction, where dev's is
# needsWorldgenReconstruction alone: below 1.19 the void generator reconstructs the vanilla worldgen registries too,
# its biome needing a Forge registry name the client's synced biome lacks, so the gate matches the level-data build's
# own reconstruction branch.
common/src/main/java/world/thearchive/wdl/WorldgenWarmup.java

# Below 1.19 the void generator reconstructs the vanilla worldgen registries too (its biome needs a Forge registry
# name the client's synced biome lacks), so the screen-open warm submits for VOID; this test asserts VOID warms, where
# dev's asserts the void default uses the synced registries and does not warm.
common/src/test/java/world/thearchive/wdl/WorldgenWarmupTest.java

# Below 1.18 a chunk's block entities are nested under Level.TileEntities, not the flat root block_entities added at
# 1.18, so the carry-forward read navigates the Level compound first. 1.13.2's ListTag.get answers an out-of-range
# index with a sentinel EndTag rather than throwing, so the tag-list walks iterate the list instead of indexing it,
# and a block key is packed through the BlockPos instance, 1.13.2 having no static BlockPos.asLong(int, int, int).
common/src/main/java/world/thearchive/wdl/adapter/ChunkMerge.java

# Below 1.18 the recovered-container scan reads block entities from Level.TileEntities, not the flat root
# block_entities. Below 1.17 the recovered-entity scan (recordEntities) likewise reads the in-chunk Level.Entities,
# the sibling of that Level.TileEntities, rather than a separate entities/ region chunk's top-level Entities.
# 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than throwing, so the tag-list
# walks iterate the list instead of indexing it, and a block key is packed through the BlockPos instance, 1.13.2
# having no static BlockPos.asLong(int, int, int).
common/src/main/java/world/thearchive/wdl/adapter/RecoveredScan.java

# com.mojang.logging.LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is
# acquired through log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketAccumulator.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/adapter/PlayerProgressWriter.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly. 1.13.2 has no SectionPos (1.14.4), so the draw-set buckets are unpacked through the
# mod's own SectionKey, and no Camera position accessor, so the camera position is interpolated from the camera
# entity's x, y, z and their xOld, yOld, zOld by the frame time.
common/src/main/java/world/thearchive/wdl/client/WdlOutlineRenderer.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/compat/bobby/BobbyChunkFilter.java

# LogUtils is a 1.18 addition, and below 1.17 there is no slf4j either, so the per-class logger is acquired through
# log4j2 LogManager directly.
common/src/main/java/world/thearchive/wdl/compat/flashback/FlashbackReplayProbe.java

# JsonParser.parseString is a gson 2.8.6 static, absent from the gson 2.8.0 Minecraft 1.13.2 bundles, so the release
# index parses through the instance form (new JsonParser().parse).
common/src/main/java/world/thearchive/wdl/update/ReleaseIndex.java

# The Java 8 floor has no java.net.http HTTP client (that is Java 11) nor its Flow-based reactive body subscribers, so
# the update transport is rewritten off HttpClient/HttpResponse.BodySubscriber onto a java.net.HttpURLConnection with a
# manual bounded read.
common/src/main/java/world/thearchive/wdl/update/HttpTransport.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps the static registries through TestRegistries.bootstrap
# rather than building the RegistryAccess.Frozen dev's test reads.
common/src/test/java/world/thearchive/wdl/adapter/ChunkReencodeFidelityTest.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps the static registries through TestRegistries.bootstrap
# rather than building the RegistryAccess.Frozen dev's test reads; the overworld DimensionType is the unnamed
# field_18954.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionMapTallyTest.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps the static registries through TestRegistries.bootstrap
# rather than building the RegistryAccess.Frozen dev's test reads; below 1.14 there is no MerchantOffers type, so the
# offers holder is built from the unnamed class_1145 and class_1144 through method_3557.
common/src/test/java/world/thearchive/wdl/adapter/RiddenMountContentsFoldTest.java

# Below 1.18 the merge test reads chunk block entities from Level.TileEntities, not the flat root block_entities;
# below 1.14 there is no barrel, so the typed-container fixture is a shulker box.
common/src/test/java/world/thearchive/wdl/adapter/ContainerTypeGateMergeTest.java

# Minecraft 1.13.2 bundles fastutil 8.2.1, which has no LongSet.of() factory, so the test builds sets via
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of. It also pins that a second stop after the first one finished finishes nothing further, the order
# the 1.13.2 loader's tick-edge disconnect arrives in behind the flush the wiring runs at the level-teardown edge.
common/src/test/java/world/thearchive/wdl/core/CaptureControllerTest.java

# Absent from dev: pins the 1.13.2 loader's level-teardown edge through the wiring that translates it: a teardown
# with the connection still open ends no download, one with it already closed finishes the download instead, the
# loader's own disconnect callback afterwards finishes nothing further, and neither edge holds the writer. The test
# and the edge it pins are absent from 1.14.4 up, as PlatformBridge.onLevelTeardown is.
common/src/test/java/world/thearchive/wdl/WdlLevelTeardownTest.java

# Implements the 1.13.2 PlatformBridge.onLevelTeardown, as the same no-op registration every other arm of this fake
# bridge is, and answers its isConnectionClosed false: no headless test drives a teardown through the bridge, the
# wiring's own cases calling Wdl.onLevelTeardown directly.
common/src/test/java/world/thearchive/wdl/testsupport/HeadlessPlatformBridge.java

# Its own fake bridge implements the 1.13.2 PlatformBridge.onLevelTeardown as a no-op like its neighbors, and answers
# isConnectionClosed false, no case of the test driving a teardown through the bridge.
common/src/test/java/world/thearchive/wdl/ResumeFlowTest.java

# Minecraft 1.13.2 bundles fastutil 8.2.1, which has no LongSet.of() factory, so the test builds sets via LongSets and
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/CapturedContainersTest.java

# Minecraft 1.13.2 bundles fastutil 8.2.1, which has no LongSet.of() factory, so the test builds sets via LongSets and
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/OutlineClassifierTest.java

# Minecraft 1.13.2 bundles fastutil 8.2.1, which has no LongSet.of() factory, so the test builds sets via LongSets and
# LongOpenHashSet; the Java 8 floor also has no Set.of collection factory (Java 9), so the entity sets are Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/RecoveredCoverageTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected lists are Guava
# ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/ChatCopyTest.java

# Below 1.17 the Java floor is 8, which has no Set.of collection factory (Java 9), so the moved-keys set is a Guava
# ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/ConfigSchemaTest.java

# Below 1.17 the Java floor is 8, where Optional has no isEmpty() (Java 11), so the crafter-drop assertions negate
# isPresent() instead.
common/src/test/java/world/thearchive/wdl/core/ContainerAssociationTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected drain order is a
# Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/ReadyLatchTest.java

# Below 1.17 the Java floor is 8, which has no List.of/Set.of collection factories (Java 9), so the derived-prefix
# list and the empty undeclared-key set use Guava ImmutableList.of/ImmutableSet.of.
common/src/test/java/world/thearchive/wdl/core/SettingsLangTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected tab-id list is a
# Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/SettingsLayoutTest.java

# Below 1.17 the Java floor is 8, which has no List.of/Map.of collection factories (Java 9), so the expected lists use
# Guava ImmutableList.of and the seven-entry refusal map uses ImmutableMap.builder (past ImmutableMap.of's five-pair
# limit).
common/src/test/java/world/thearchive/wdl/core/ToastCopyTest.java

# Below 1.17 the Java floor is 8: no Files.readString/writeString (Java 11), no records (Java 16), no List.of (Java
# 9), so the config reads/writes go through readAllBytes/write with StandardCharsets.UTF_8, DefaultOnBoolean is a
# final class with explicit accessors, and the toggle list is a Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/WdlConfigTest.java

# Below 1.17 the Java floor is 8, which has no no-argument Optional.orElseThrow (Java 10) nor List.of (Java 9), so the
# entry lookup uses Optional.get and the empty SaveChunks lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/browse/DownloadCatalogTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected managed-folder
# list is a Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/browse/DownloadFoldersTest.java

# Below 1.17 the Java floor is 8, where there is no Path.of factory (Java 11), so the save path is built with Paths.get.
common/src/test/java/world/thearchive/wdl/core/browse/SinglePlayerTaintTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected surviving-paths
# lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/export/RestoreOperationTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the expected
# moved-back/relocated/deferred lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/export/RestoreSweepTest.java

# Below 1.17 the Java floor is 8, which has no List.of collection factory (Java 9), so the DownloadCounts/SaveChunks
# and render-input lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/report/DownloadReportFormatterTest.java

# Below 1.17 the Java floor is 8: no List.of (Java 9) nor Files.readString/writeString (Java 11), so the SaveChunks
# lists are Guava ImmutableList.of and the corrupt-timestamp round trip reads/writes via readAllBytes/write with
# StandardCharsets.UTF_8.
common/src/test/java/world/thearchive/wdl/core/report/DownloadReportLogTest.java

# Below 1.17 the Java floor is 8, which has no no-argument Optional.orElseThrow (Java 10) nor List.of (Java 9), so
# SemVer.parse is unwrapped with Optional.get and the release/game-version lists are Guava ImmutableList.of.
common/src/test/java/world/thearchive/wdl/core/update/LatestReleaseTest.java

# Below Forge 40 the world-render hook is RenderWorldLastEvent (no render stage, no frustum), not the
# RenderLevelStageEvent added at 1.18.2; the outline draws on it and uses the shared renderer's null-frustum path.
forge/src/main/java/world/thearchive/wdl/forge/ForgeOutlineRegistrar.java

# A downgrade proxy can hand a 1.13.2 client sections outside the 0..255 column, and a section outside 0..15 crashes
# vanilla's unchecked section index on load; this test pins that the codec writes only the block sections inside
# 0..15 and drops every out-of-range or section-less entry (light is section-resident at 1.13.2, so the 1.14 light-pad
# entry has no home). The scenario and the clamp are below-1.18-only. At 1.13.2 the full status is the unnamed
# ChunkStatus.field_18865, LevelChunkSection takes (int, boolean), and the biomes are the flat 16 by 16 column array.
common/src/test/java/world/thearchive/wdl/adapter/ChunkSectionClampTest.java

# Below 1.18 a chunk's block entities nest under Level.TileEntities, not the flat root block_entities added at 1.18,
# so the in-session stash merge locates a block entity by x/y/z through the Level compound.
common/src/main/java/world/thearchive/wdl/adapter/ContainerMerge.java

# 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than throwing, so the
# tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/EntityTreeWalk.java

# 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than throwing, so the
# tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/NbtMerge.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps through TestRegistries.bootstrap; below 1.15 StringTag
# has no valueOf factory, so the malformed-value fixture uses the constructor; the list accessor is getList(key, 10).
# Carries a carry-list case added at 1.13.2 that dev lacks: a carry into a fresh tag holding no list writes the key.
common/src/test/java/world/thearchive/wdl/adapter/NbtMergeTest.java

# The Java 8 floor cannot bind a pattern variable in a type test, so each instanceof over an NBT read is a local, an
# unbound instanceof and a cast rather than the pattern form dev uses. 1.13.2's ListTag.get answers an out-of-range
# index with a sentinel EndTag rather than throwing, so the tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferScrub.java

# The Java 8 floor cannot bind a pattern variable, so each instanceof over a tag is a local, a test and a cast; and
# below 1.17 entities live inside the region chunk, so the resume scan hands the bank that whole chunk and the record
# side reads Level.Entities where dev reads a top-level Entities, while applyTo takes the flat Entities envelope the
# fold passes. 1.13.2's ListTag.get answers an out-of-range index with a sentinel EndTag rather than throwing, so the
# tag-list walks iterate the list instead of indexing it.
common/src/main/java/world/thearchive/wdl/adapter/RecoveredEntityContent.java

# Below 1.16 there is no RegistryAccess, so the test bootstraps through TestRegistries.bootstrap; below 1.14 there is
# no MerchantOffers type or offers codec, so the fixture builds its offer from the unnamed class_1144 with a plain
# ItemStack buy cost and serializes through class_1145.method_3557; the entity UUID is the UUIDMost/UUIDLeast pair,
# and the pre-1.21.5 accessors are spelled getList(key, 10) and getCompound, with getIntOr a local intOr helper.
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferScrubTest.java

# Below 1.17 entities live inside the region chunk, so the scanned-chunk fixture wraps its entities under
# Level.Entities while the fresh-chunk fixture keeps the flat envelope, the two shapes the bank's two sides take; and
# the merchant fixture uses 1.13.2's plain-ItemStack offer and class_1145.method_3557. The later getIntOr,
# getCompoundOrEmpty and getStringOr accessors are spelled in the pre-1.21.5 forms, getIntOr as a local intOr helper
# because getInt reads an absent key back as a real zero.
common/src/test/java/world/thearchive/wdl/adapter/RecoveredEntityContentTest.java

# Below 1.17 entities live inside the region chunk, so the on-disk oracle reads the host chunk's Level.Entities
# through the mod's own WdlRegionStorage rather than a separate entities/ store (no IOWorker below 1.15, no
# SimpleRegionStorage below 1.20.5), and the fixture seeds a host chunk before each download because a fold with no
# host on disk is a counted loss. Below 1.14 there is no MerchantOffers type, so the merchant fixture builds its offer
# from the unnamed class_1144 and serializes through class_1145.method_3557, and the pre-1.21.5 accessors are spelled
# getList(key, 10), getCompound and getString; LevelStorageSource is an interface at 1.13.2, its class the unnamed
# class_99, and the overworld DimensionType the unnamed field_18954.
common/src/test/java/world/thearchive/wdl/adapter/EntityResumeContentCarryTest.java

# Below 1.21.9 the keybind category key is key.categories.wdl.downloader, vanilla's key.categories.* shape, so the
# external-consumer set names that key. The loader-root loop is skip-if-absent for a loader root rather than dev's
# hard assertTrue, since 1.13.2 ships neither root loader tree; common's own root stays required and scanned.
# key.wdl.peek_hud is enrolled as an external consumer, since the island's WdlForge constructs it outside the
# scanned roots. Keys the 1.13.2 screens never reach (two button tooltips, two screen titles, a text field's
# narration message) sit in BAND_DROPPED_KEYS, their en_us strings kept so the catalog matches dev's.
# The file also carries the Java 8 forms the test tree uses throughout (Guava immutable collections, Paths.get).
common/src/test/java/world/thearchive/wdl/LangKeyCoverageTest.java

# Below Java 21 the pinned Error Prone release, a Java 21 build, cannot load inside the compile toolchain, so the
# Java 8 bands gate NullAway behind errorProneUsable and run annotations-only, where dev runs the checker
# unconditionally and carries no such gate; the loader-merge sentence names no wdl.common-merge plugin, 1.13.2
# having no loader subproject and no such plugin.
build-logic/src/main/kotlin/wdl.nullness-conventions.gradle.kts

# Below Java 11 Checkstyle cannot run on a Java 8 band's toolchain, so this file points the Checkstyle tasks at a
# modern launcher, and PITest at the Java 8 toolchain where mcp_mappings_version marks a classic-MCP band (legacy
# FML cannot static-initialize past Java 8) or at the modern launcher otherwise; dev, a Java 25 band, carries no
# launcher block.
build-logic/src/main/kotlin/wdl.java-conventions.gradle.kts

# The root includes only common: 1.13.2 is Forge-only, its jar built by the island under forge/, and the paragraph
# over include(...) names why NeoForge is absent; dev includes common, fabric and neoforge. It also includes the
# tools/mojmap-bridge build, which composes the intermediary-to-Mojmap mapping jar common's Loom toolchain consumes
# below the Mojmap floor, and a value source that generates that jar through the bridge build's own wrapper at
# settings evaluation, since Loom reads it while configuring common and no task edge can produce it in time.
/settings.gradle.kts

# The apply-false plugin block declares only the catalog-pinned plugins a subproject of the Forge-only 1.13.2 root
# applies, and its notes name those appliers, where dev declares the set for common, fabric and neoforge.
/build.gradle.kts

# No NeoForm or NeoGradle vanilla release exists for 1.13.2, so common builds on Fabric Loom in vanilla mode over
# Legacy Fabric's intermediary and the tools/mojmap-bridge mapping (NeoGradle vanilla on 1.14.4, ModDevGradle on dev):
# remap tasks off, the malformed parameter annotations stripped for javac 8, writeIslandClasspath, no commonJava.
common/build.gradle.kts

# The catalog carries only the plugins some build of the Forge-only 1.13.2 root applies, so it has no moddev or
# mod-publish-plugin entry (dev's carries loom, moddev and mod-publish-plugin for its fabric and neoforge
# subprojects); the tool-library entries match dev's.
gradle/libs.versions.toml

# Deleted: the source-merge consumer plugin's only appliers were the fabric and neoforge subprojects, neither of
# which the Forge-only 1.13.2 root includes, and the island merges common by direct path.
build-logic/src/main/kotlin/wdl.common-merge.gradle.kts

# The deep-band era of the workflow: the Forge island's own build step, a Forge artifact upload, and per-loader upload
# gates where dev uploads its Fabric and NeoForge jars ungated. Against 1.14.4's form the Fabric upload is gated on a
# Fabric jar existing, 1.13.2 building none, and the island's -dev jar is excluded from the Forge artifact.
.github/workflows/ci.yml

# Added at 1.13.2, absent from dev and 1.14.4: the tools/mojmap-bridge included build (its own wrapper, build
# scripts and generator source) that composes the intermediary-to-Mojmap mapping jar common's Loom toolchain
# consumes below the Mojmap floor, generated at settings evaluation through that wrapper.
tools/
