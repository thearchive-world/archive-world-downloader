# Band-divergence allowlist.
#
# checkBandDivergence diffs the working tree against the branch this band was minted from (mint_parent in
# gradle.properties) and against dev, over every tracked path except gradle.properties, this file and the
# per-band plug (common/src/main/java/world/thearchive/wdl/adapter/impl/). A path that differs from both must
# match a pattern below; a pattern that matches no path differing from dev is stale; core/ main sources may not
# differ from the parent and are never listed here, which along the mint chain keeps them one blob on every band.
# A path equal to the parent's is inherited and a path equal to dev's is not this band's divergence; an inherited
# entry stays live while its file differs from dev and is kept. Each entry records why the file legitimately
# carries band-local code, so neither the gate nor a future reader mistakes its difference for a lost fix or a
# bug. Anchor a reason to a version rather than to "this band", say "deleted" for a file dev has and this band
# does not, and re-read every inherited reason at a mint: the gate checks that a pattern is live, never that
# its reason is true. Patterns match as gitignore(5) matches: the last matching pattern is the one credited and a
# directory pattern covers its subtree, so a pattern nested under a registered directory is never credited and
# reads as stale.
#
# Format: gitignore-style per gitignore(5), one path pattern per line, blank lines and # comments ignored.
# Every pattern carries a # reason directly above it (checkBandDivergence fails on a pattern without one), so
# a shared reason across several files is repeated per line rather than written once over a group.

# Below 1.21.2 the registry-load path has no PendingTags step (TagLoader.loadTagsForExistingRegistries and
# Registry.PendingTags are 1.21.2 and above), so this shared test bootstrap loads the WORLDGEN registries from the
# getAccessForLoading base and binds each registry's tags with TagLoader.loadAndBuild plus Registry.bindTags, the
# pre-1.21.2 forms. Below the 1.21 update the tags directory is TagManager.getTagDir rather than
# Registries.tagsDirPath.
common/src/test/java/world/thearchive/wdl/testsupport/TestRegistries.java

# At Fabric API 0.100.8+1.20.6 the client entrypoint registers the HUD overlay through HudRenderCallback.EVENT and the
# peek key through KeyBindingHelper.registerKeyBinding (keybinding.v1), where dev's Fabric API for 26.2 registers a HUD
# element through HudElementRegistry.addLast under an Identifier and the key through KeyMappingHelper.registerKeyMapping
# (keymapping.v1).
fabric/src/main/java/world/thearchive/wdl/fabric/WdlFabricClient.java

# At Fabric API 0.100.8+1.20.6 the platform bridge reads the pause-menu widget list through Screens.getButtons,
# registers the key through KeyBindingHelper.registerKeyBinding and builds the client command tree through
# ClientCommandManager (command.v2), where dev's Fabric API for 26.2 names them Screens.getWidgets,
# KeyMappingHelper.registerKeyMapping and ClientCommands.
fabric/src/main/java/world/thearchive/wdl/fabric/FabricPlatformBridge.java

# Deleted: the client game tests dev carries under fabric/src/gametest/. fabric-client-gametest-api-v1 first shipped
# at Minecraft 1.21.4; below it client_gametest=false in gradle.properties leaves the gametest source set uncreated
# and its run unregistered, so nothing compiles the tree.
fabric/src/gametest/

# Below the 1.21.2 chunk-serialization cut vanilla has no SerializableChunkData.SectionData record, so this
# chunk-snapshot seam defines its own band-stable SectionData carrier for the static-write codec.
common/src/main/java/world/thearchive/wdl/adapter/ChunkSnapshotSource.java

# Reads the captured section through the mod's own ChunkSnapshotSource.SectionData rather than the vanilla
# SerializableChunkData.SectionData that exists only at 1.21.2 and above. Below the 1.21 update there is no
# jukebox_playable item component (a playable disc is a RecordItem instance), and a captured jukebox carries the
# pre-JukeboxSongPlayer playing state (IsPlaying, RecordStartTick, TickCount).
common/src/main/java/world/thearchive/wdl/adapter/InteractionCapture.java

# Below the 1.21 update a captured jukebox's play state is the IsPlaying note-particle flag rather than the
# ticks_since_song_started sidecar, so the RecordItem carry-forward field carries IsPlaying.
common/src/main/java/world/thearchive/wdl/adapter/CapturedBlockField.java

# Below 1.21.2 the entity position wire differs: the absolute-only ClientboundTeleportEntityPacket replaces the
# newer teleport plus ClientboundEntityPositionSyncPacket and PositionMoveRotation, the byte-encoded packet
# rotations are decoded here, and the single BOAT and CHEST_BOAT types replace the per-wood split. Below the 1.21
# update VecDeltaCodec has no getBase accessor, so the packet-visible base position is read via decode(0, 0, 0).
common/src/main/java/world/thearchive/wdl/adapter/EntityPacketCapture.java

# Below 1.21.2 there is no chunk unsaved-listener, so re-capture polls isUnsaved over the keep-hot buffer here; it also
# reads the mod's own ChunkSnapshotSource.SectionData and reconstructs entities through
# EntityPacketCapture.createSpawnEntity, the pre-spawn-reason form. Below the 1.21 update leash state lives on Mob
# rather than the Leashable interface, so the reconstruct path rebinds a saved leash via Mob.setLeashedTo.
common/src/main/java/world/thearchive/wdl/adapter/LiveCaptureSession.java

# Below 1.21.2 the toast host is Minecraft.getToasts (ToastComponent), not getToastManager.
common/src/main/java/world/thearchive/wdl/platform/AbstractPlatformBridge.java

# 1.20.6's adapter/impl has no ItemStackSanitizer, so serialize encodes each offer's sell item directly, without
# repairing a disk-codec-invalid component before the write.
common/src/main/java/world/thearchive/wdl/adapter/MerchantOfferCapture.java

# The chunk fixtures build the mod's own ChunkSnapshotSource.SectionData (vanilla has no
# SerializableChunkData.SectionData below 1.21.2) and read the biome registry via registryOrThrow and the section floor
# via getMinSection.
common/src/test/java/world/thearchive/wdl/testsupport/SyntheticChunks.java

# Below 1.21.2 the Mob constructor reads level.getProfilerSupplier(), so a mob cannot be built against a null level;
# this headless Level supplies the overworld dimension type, the vanilla registries, and an inactive profiler.
common/src/test/java/world/thearchive/wdl/testsupport/HeadlessLevel.java

# Builds the section's biome registry via registryOrThrow, the pre-1.21.2 form (lookupOrThrow returns a
# RegistryLookup, not a Registry, below 1.21.2).
common/src/test/java/world/thearchive/wdl/testsupport/RegistriesSmokeTest.java

# Resolves an item by id via Registry.get, the pre-1.21.2 accessor (renamed getValue at 1.21.2). Below the 1.21
# update ResourceLocation has no parse factory, so an id string is built with the public ResourceLocation(String)
# constructor.
common/src/test/java/world/thearchive/wdl/testsupport/ItemFixtures.java

# Below 1.21.2 ChunkSerializer.read needs a ServerLevel the headless test lacks, so the round-trip proves
# self-consistency by decoding each section's block-state and biome container directly.
common/src/test/java/world/thearchive/wdl/adapter/ChunkRoundTripTest.java

# The snapshot fixture carries the mod's own ChunkSnapshotSource.SectionData rather than the vanilla
# SerializableChunkData.SectionData that exists only at 1.21.2 and above.
common/src/test/java/world/thearchive/wdl/adapter/impl/ChunkCodecImplTest.java

# Below 1.21.2 there is no ChunkPos.CODEC (and below 1.21.5 no CompoundTag.read(key, codec)), so the round trip reads
# the entities-region Position back through getIntArray as the {x, z} int array EntityStorage writes at every version
# (an IntArrayTag through 1.21.4, the same array through ChunkPos.CODEC from 1.21.5).
common/src/test/java/world/thearchive/wdl/adapter/EntityRoundTripTest.java

# Constructs GameRules from a Dynamic via the pre-1.21.2 single-argument constructor (the feature-flag-filtered
# constructor is 1.21.2 and above).
common/src/test/java/world/thearchive/wdl/adapter/LevelDatWorldOutputTest.java

# The headless entity double overrides hurt(DamageSource, float), the pre-1.21.2 damage entry point (the
# ServerLevel-taking hurtServer is 1.21.2 and above).
common/src/test/java/world/thearchive/wdl/adapter/CaptureAnchorTest.java

# The headless entity double overrides hurt(DamageSource, float), the pre-1.21.2 damage entry point (the
# ServerLevel-taking hurtServer is 1.21.2 and above).
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionLossTallyTest.java

# The headless mob double overrides hurt(DamageSource, float) (hurtServer with a ServerLevel is 1.21.2 and above) and
# is built against the HeadlessLevel, since below 1.21.2 the Mob constructor needs a non-null level.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkRootVehicleTest.java

# The geared mob double is built against the HeadlessLevel, since below 1.21.2 the Mob constructor reads
# level.getProfilerSupplier() and cannot take a null level.
common/src/test/java/world/thearchive/wdl/adapter/impl/NaturalEquipmentTest.java

# Deleted. 1.20.6's adapter/impl carries no ItemStackSanitizer for this unit test to guard.
common/src/test/java/world/thearchive/wdl/adapter/impl/ItemStackSanitizerTest.java

# Deleted. The disk-codec-invalid stack fixtures this helper built are unused on 1.20.6, which has no
# ItemStackSanitizer for them to exercise.
common/src/test/java/world/thearchive/wdl/testsupport/BadStacks.java

# Deleted. 1.20.6's adapter/impl has no ItemStackSanitizer to repair a disk-codec-invalid container item, so
# this guard has nothing to test.
common/src/test/java/world/thearchive/wdl/adapter/impl/ContainerSinkItemDegradationTest.java

# Deleted. 1.20.6's adapter/impl has no ItemStackSanitizer to repair a disk-codec-invalid equipped item, so
# this guard has nothing to test.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkItemDegradationTest.java

# Deleted. 1.20.6's adapter/impl has no ItemStackSanitizer to repair a disk-codec-invalid worn item, so this
# guard has nothing to test.
common/src/test/java/world/thearchive/wdl/adapter/impl/PlayerSinkItemDegradationTest.java

# Deleted. Below the 1.21 update leash state lives on Mob rather than the Leashable interface, so the
# LeashData codec-throw this test guards against does not arise on 1.20.6.
common/src/test/java/world/thearchive/wdl/adapter/impl/EntitySinkLeashDegradationTest.java

# The reflection-accessor javadoc names CaptureAnchorTest, not EntitySinkLeashDegradationTest, as the sibling
# reaching Entity.vehicle the same way, since EntitySinkLeashDegradationTest does not exist on 1.20.6.
common/src/test/java/world/thearchive/wdl/adapter/impl/SaddleCaptureTest.java

# Below the 1.21 update the HUD render callback carries a float partial-tick rather than a DeltaTracker, so the
# shared overlay's render entry point takes a float.
common/src/main/java/world/thearchive/wdl/client/WdlHudOverlay.java

# Below the 1.21 update Util.OS has no openPath(Path), so the open-folder affordances call openFile(File).
common/src/main/java/world/thearchive/wdl/client/WdlDownloadsScreen.java

# Below the 1.21 update ResourceLocation has no fromNamespaceAndPath factory, so the HUD-layer id is built with the
# public ResourceLocation(String, String) constructor.
neoforge/src/main/java/world/thearchive/wdl/neoforge/WdlNeoForge.java

# JourneyMap 5.10.0 for 1.20.6 uses the 1.9 API generation, published on jm.gserv.me rather than
# maven.blamejared.com; common's compile-only pin takes the full journeymap-api coordinate with a
# -neoforge-SNAPSHOT suffix, since the 1.9 generation ships one artifact per loader with no -common flavor.
common/build.gradle.kts

# The journeymap_api_coordinate pin 1.20.5-1.9 is the 1.9 JourneyMap API generation, published on jm.gserv.me rather
# than maven.blamejared.com, so the Fabric compile-only pin is the journeymap-api coordinate with a -fabric-SNAPSHOT
# suffix rather than the journeymap-api-fabric artifact.
fabric/build.gradle.kts

# The journeymap_api_coordinate pin 1.20.5-1.9 is the 1.9 JourneyMap API generation, published on jm.gserv.me rather
# than maven.blamejared.com, so the NeoForge compile-only pin is the journeymap-api coordinate with a -neoforge-SNAPSHOT
# suffix rather than the journeymap-api-neoforge artifact.
neoforge/build.gradle.kts

# JourneyMap 5.10.0 for 1.20.6 uses the older 1.9 API generation (journeymap.client.api), so this plugin binds
# the @ClientPlugin annotation and the IClientPlugin.onEvent push model rather than the journeymap.api.v2 forms.
common/src/main/java/world/thearchive/wdl/compat/journeymap/WdlJourneyMapPlugin.java

# The overlay driver binds the 1.9 JourneyMap API: it subscribes the mapping events through IClientAPI.subscribe and
# handles them in onClientEvent, and builds each PolygonOverlay with the 1.9 display-id constructor.
common/src/main/java/world/thearchive/wdl/compat/journeymap/JourneyMapOverlayDriver.java

# The polygon builder binds the 1.9 JourneyMap model package (journeymap.client.api.model) for MapPolygon,
# MapPolygonWithHoles and ShapeProperties.
common/src/main/java/world/thearchive/wdl/compat/journeymap/CoveragePolygons.java

# Below the 1.21 update ResourceLocation has no fromNamespaceAndPath factory, so the datapack-dimension id is built
# with the public ResourceLocation(String, String) constructor.
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionDimensionRebindTest.java

# Below the 1.21 update ResourceLocation has no fromNamespaceAndPath factory, so the datapack-dimension id is built
# with the public ResourceLocation(String, String) constructor.
common/src/test/java/world/thearchive/wdl/adapter/PlayerTagTest.java

# Below the 1.21 update ResourceLocation has no parse factory, so a block-entity id string is built with the public
# ResourceLocation(String) constructor.
common/src/test/java/world/thearchive/wdl/testsupport/FixtureFidelity.java

# Below the 1.21 update a captured jukebox carries the pre-JukeboxSongPlayer playing flag (IsPlaying) rather than
# the ticks_since_song_started sidecar, so these merge fixtures and assertions use that key.
common/src/test/java/world/thearchive/wdl/adapter/ChunkMergeTest.java

# Below the 1.21 update a captured jukebox is marked playing via IsPlaying rather than the ticks_since_song_started
# sidecar, so this round-trip asserts the playing flag.
common/src/test/java/world/thearchive/wdl/adapter/InteractionMergeRoundTripTest.java

# Below 1.21.5 the CompoundTag and ListTag accessors getCompound, getList and getString return the value, where from
# 1.21.5 they return Optional and dev reads through the defaulting getCompoundOrEmpty, getListOrEmpty and getStringOr,
# and dev's getIntOr becomes a local intOr helper because getInt reads an absent key back as a real zero while the
# assertions rely on -1.
common/src/test/java/world/thearchive/wdl/adapter/RecoveredEntityContentTest.java

# Below 1.21.5 the CompoundTag and ListTag accessors getCompound and getList return the value, where from 1.21.5 they
# return Optional and dev reads through the defaulting getCompoundOrEmpty and getListOrEmpty, and dev's getIntOr becomes
# a local intOr helper because getInt reads an absent key back as a real zero while the assertions rely on -1.
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferScrubTest.java

# Below 1.21.5 the CompoundTag and ListTag accessors getCompound, getList and getString return the value, where from
# 1.21.5 they return Optional and dev reads through the defaulting getCompoundOrEmpty, getListOrEmpty and getStringOr.
common/src/test/java/world/thearchive/wdl/adapter/EntityResumeContentCarryTest.java

# Below 1.21.5 the CompoundTag and ListTag accessors getCompound, getList and getString return the value, where from
# 1.21.5 they return Optional and dev reads through the defaulting getCompoundOrEmpty, getListOrEmpty and getStringOr or
# unwraps getList and getCompound with orElseThrow, and below 1.21.5 CompoundTag has no store(key, codec, value), so the
# PRIOR_PLAYER UUID is put as UUIDUtil.CODEC.encodeStart(NbtOps.INSTANCE, PRIOR_PLAYER).getOrThrow().
common/src/test/java/world/thearchive/wdl/adapter/LiveCaptureSessionResumedMountReleaseTest.java

# 1.20.6 has no ItemStackSanitizer, so this file carries no sell-book repair tests. Below 1.21.5 the CompoundTag and
# ListTag accessors getCompound, getList and getInt return the value, where from 1.21.5 they return Optional and dev
# reads through the defaulting getCompoundOrEmpty, getListOrEmpty and getIntOr.
common/src/test/java/world/thearchive/wdl/adapter/MerchantOfferCaptureTest.java
