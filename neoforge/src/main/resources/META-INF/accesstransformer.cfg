# WDL: NeoForge access transformer (auto-loaded at runtime from this path; auto-applied at
# compile time by ModDevGradle from src/main/resources/META-INF/accesstransformer.cfg).
public net.minecraft.world.level.chunk.storage.SerializableChunkData <init>(Lnet/minecraft/core/Registry;Lnet/minecraft/world/level/ChunkPos;IJJLnet/minecraft/world/level/chunk/status/ChunkStatus;Lnet/minecraft/world/level/levelgen/blending/BlendingData$Packed;Lnet/minecraft/world/level/levelgen/BelowZeroRetrogen;Lnet/minecraft/world/level/chunk/UpgradeData;[JLjava/util/Map;Lnet/minecraft/world/level/chunk/ChunkAccess$PackedTicks;[Lit/unimi/dsi/fastutil/shorts/ShortList;ZLjava/util/List;Ljava/util/List;Ljava/util/List;Lnet/minecraft/nbt/CompoundTag;)V

# WDL: the connection packet tee reaches the connection's Netty pipeline through the
# private Connection.channel, the NeoForge analog of the Fabric access widener. Both are read-only,
# visibility-only widenings.
public net.minecraft.network.Connection channel

# WDL: a relative-move packet (the common walk update) carries its entity id in a protected field with
# no public getter, so the tee reads it to feed the move to the accumulator off the Netty thread.
public net.minecraft.network.protocol.game.ClientboundMoveEntityPacket entityId

# WDL: a mount inventory menu names the animal the server opened it for in a non-public field with no
# getter, the NeoForge analog of the Fabric access widener. Read-only, so this is visibility-only and leaves the
# field final.
public net.minecraft.world.inventory.HorseInventoryMenu horse

# WDL: the pause menu row anchors to vanilla's own disconnect button, named in a private field with no getter,
# the NeoForge analog of the Fabric access widener. Read-only, so this is visibility-only.
public net.minecraft.client.gui.screens.PauseScreen disconnectButton
